<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\shared\Free.Shared.Drawing\Free.Shared.Drawing.csproj" />
    <ProjectReference Include="..\..\shared\Free.Shared.IO\Free.Shared.IO.csproj" />
    <ProjectReference Include="..\..\shared\Free.Shared.PageSetup\Free.Shared.PageSetup.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Free.Shared.Drawing" />
  </ItemGroup>

  <ItemGroup>
    <!-- Sheet.ActiveCellWriteObserver, the #164 (R138) diagnostic seam, is internal so it is not
         part of the model's public surface. Only the test assembly that owns that investigation
         needs to see it. -->
    <InternalsVisibleTo Include="FreeX.App.Host.Logic.Tests" />
  </ItemGroup>

</Project>
