<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\FreeX.Core.Model\FreeX.Core.Model.csproj" />
    <ProjectReference Include="..\FreeX.Core.Formula\FreeX.Core.Formula.csproj" />
    <ProjectReference Include="..\..\shared\Free.Shared.Drawing\Free.Shared.Drawing.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Free.Shared.Drawing" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="FreeX.Core.Calc.Tests" />
    <!-- ConditionalFormatRenderEvaluator (print preview / PDF export) reuses the screen renderer's
         rule-condition evaluator (ViewportConditionalFormatEvaluator.MatchesRuleCondition) and its
         Formula-rule matcher (ViewportService.MatchesFormula) instead of re-implementing Formula,
         Top10, Duplicate/Unique, text, DateOccurring, and Blanks/Errors rule matching a second time. -->
    <InternalsVisibleTo Include="FreeX.App.Presentation" />
  </ItemGroup>

</Project>
