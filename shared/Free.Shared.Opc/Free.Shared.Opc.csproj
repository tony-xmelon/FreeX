<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Free.Shared.Drawing\Free.Shared.Drawing.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- FreeX.Core.IO still composes the internal XML normalization primitives. -->
    <InternalsVisibleTo Include="FreeX.Core.IO" />
    <InternalsVisibleTo Include="FreeX.Core.IO.Tests" />
  </ItemGroup>

</Project>
