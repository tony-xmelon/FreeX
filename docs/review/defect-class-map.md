# Defect-class map

The round ledger (`region-coverage.md`) is chronological, so what has been swept is implied rather
than stated. This file makes it explicit: which defect CLASSES have been driven across the codebase,
what each one found, and - the part a chronological log cannot show - which classes are known to be
unswept.

It is a map of coverage, not a proof of absence. A class marked closed means the sweep was designed,
validated against a known positive, and run to completion; it does not mean the code is free of
defects outside that shape.

## Closed - swept, instrument validated, findings fixed

| Class | Rounds | Outcome |
|---|---|---|
| Undo restores value but not structure | r438, r441 | 2 defects |
| Redo mints a fresh identity | r457, r458 | 2 defects |
| Damaged file read as plausibly empty | r448-r454, r467 | 6 defects, 8 readers already sound |
| Non-finite double written to a file | r468, r469, r485, r486 | 11 defects across PDF, XPS, pptx, xlsx |
| Protection bypassed by a mutator | r455, r471 | 11 defects |
| Mutation without change notification / undo | r472 | guard added, no defect |
| Schema element ordering in OOXML | r483, r484, r498 | 2 defects; widened from the zoom writers to every writer in r498 |
| Equal-value setter clears redo | r479, r480 | 1 defect + census premises audited |
| Culture-sensitive NUMBER formatting | r470 | clean; the naive test proved vacuous |
| Culture-sensitive STRING casing/comparison | r490 | clean; 392+178 invariant uses, no bare ToLower |
| Hostile input to dialog planners | r478 | clean; 190 methods, 3,420 invocations |
| Hostile arguments to formula functions | r463 | clean; 4,960 evaluations |
| Recursion depth from a crafted file | r477 | clean; all three readers guarded |
| Zip-slip / archive path traversal | r471 | clean; nothing is extracted to disk |
| Destructive partial save | r471 | clean; temp lease + fsync + move |
| Sibling drift between app shells | r474, r475 | 1 defect; 52 pairs compared |
| Blocking a UI thread on a Task | r489 | latent trap documented; no live path |
| Division by an empty collection | r487, r488 | clean; 52 raw -> 19 real -> 0 |
| Throwing XML navigation on file input | r487 | clean; 8 sites, none file-derived |
| `async void` in production | r491 | clean; 14 non-handler sites, all fully guarded |
| Cancellation token accepted then ignored | r491 | clean; 5 candidates, all declarations or expression-bodied |
| Integer overflow / unbounded size from a file | r492 | 1 defect: an .ods decimal-places count allocated 4 GB |
| Local time where UTC is meant | r493 | clean; 35 sites, all legitimately local |
| Equals without GetHashCode | r493 | closed by the compiler: TreatWarningsAsErrors, no NoWarn for CS0659/CS0661 |
| Undisposed resource / per-paint allocation | r494, r512 | 2 defects: picture fills and picture shapes each decoded a Bitmap per paint |
| Mutable toolkit visual in a static field | r495 | 4 defects (FreeW pens); tripwire added |
| Native interop: handle leaks, wrong-OS calls | r496 | clean; 51 P/Invokes, pairing and dispatch both correct |
| Interop marshalling: buffer size units, struct layout | r514 | clean; only 3 StringBuilder params exist repo-wide (all correct units) plus one HGlobal two-call buffer; NO managed delegate is ever handed to native, so the collected-callback crash class is absent; X11 LP64 layout now guarded |
| Disposable ownership across an async boundary | r515 | clean; all 27 fire-and-forget sites checked by hand -- every one that hands over an object TRANSFERS ownership into the continuation rather than letting a using close under it |
| Culture-sensitive ordering in a file-output path | r515 | FIXED in FreeX .fxl save (observable: mixed-case error codes reorder); FreeP media extensions made ordinal for consistency (not reachable); no culture-less ToLower/ToUpper, no string .Sort() |
| XML entity expansion / external entities (XXE) | r516 | clean; .NET default is DtdProcessing.Prohibit and all 17 explicit settings say Prohibit, all 13 XmlResolver assignments are null; nothing overrides the safe default |
| Catastrophic regex backtracking (ReDoS) | r516 | clean; every user-supplied-pattern path (REGEX* functions, Find/Replace, wildcard criteria) passes FormulaSafetyLimits.RegexTimeout, so the timeout handlers are live rather than decorative |
| Integer overflow in EMU/coordinate math from file input | r516 | clean; conversions widen to long before multiplying, and the one checked((int)) cast is range-bounded to 1584pt upstream |
| Duplicate key built from a file-controlled id | r516 | clean; the one ToDictionary over an untrusted id (XlsxRelationshipReader.LoadTargetsStrict) is caught BY DESIGN -- caller param is rejectDuplicateRelationshipIds, the catch is commented, and a test pins TryCreate returning null. See r516: my "fix" was RETRACTED |
| Shared mutable static collection (thread safety) | r517 | clean; all 12 runtime-mutated statics guarded by a gate lock, [ThreadStatic] isolation, or lock-on-collection; now enforced by a source tripwire with one documented WPF-affinity exemption |
| Undisposed file handle on an exception path | r518 | clean; zero disposable locals created outside a using across all production code |
| Truncated/damaged part in a package | r518 | DIVERGENCE recorded, not a bug: FreeX and FreeW throw XmlException; FreeP opens the deck, blanks the damaged slide and warns (naming the save-over hazard). Repair-on-open in FreeX/FreeW is a product gap vs Office, not a review fix |
| Non-BMP text (surrogate pairs) in text functions | r519 | clean; FreeX counts UTF-16 units exactly as Excel does (LEN=4 for A+emoji+B, LEFT splits the pair), now pinned by test; four DEAD helpers implementing the other semantics removed as a trap |
| Sort stability vs Excel | r520 | clean; FreeX's two List.Sort comparators both end in an OriginalIndex tiebreaker, FreeW's ParagraphSort uses OrderBy and takes direction from the comparer, not by reversing results |
| Untrusted HTML paste (clipboard) | r520 | clean; iterative parser, colspan/rowspan clamped to sheet width so a small paste cannot amplify into a huge allocation |
| Unchecked cast/index in command targets | r520, r521, r522 | FIXED in FreeW EditCommands: 18 TableAt (r520), 8 ParagraphAt (r521), 6 INLINE casts (r522, two also indexing Runs unguarded). Name-based sweeps missed the inline ones; the pattern sweep found them. Two sites outside the file are safe by construction. Also CORRECTS r518 |
| Index captured at construction, dereferenced later | r523 | REFRAMED: the danger is the temporal gap, not the cast. All 42 element casts in production swept; every one outside the command layer is safe because resolution and use are adjacent. Command-layer regressions now blocked by a tripwire |
| Pattern-match indexing without a bounds check | r524 | FIXED 38 sites in FreeW: Blocks[i] is Paragraph p type-checks safely but indexes FIRST. Half-guards that bounds-checked runIndex on the same line and missed the block index. Invisible to every cast-based sweep |
| Captured index dereferenced after the document changed | r525 | FIXED 7 more in FreeW (DeleteParagraph read+RemoveAt+Insert, drawing-group _members coordinates, _members[0] with no emptiness test). Confirmed FreeP/FreeX clean with THEIR collection names -- r524 scan searched Blocks[] and was blind to both |
| Insert at a captured index (upper bound is Count) | r526 | FIXED 1 of 9: DeleteTableRow.Revert checked the lower bound only. The other 8 across all three apps clamp correctly (Math.Clamp/Math.Min, bounded loops, or start at Count and only descend) |
| Hostile index, driven behaviourally (not by regex) | r527 | FIXED 4 sites invisible to five prior sweeps because the index was a FIELD (_paragraphIndex) and every scan required a leading [a-z]. Census test now constructs every FreeW command with an impossible index and requires no throw |
| Hostile index census, FreeP | r528 | clean; 65-67 commands actually exercised, none throws. NOT ported to FreeX: 227 command classes, zero constructible from primitives, so the census would exercise nothing while looking like coverage |
| Equality semantics: mutable dictionary key | r497 | clean; value-equality keys and mutable types are disjoint sets |
| Save idempotence (accumulation, reorder, nondeterminism) | r499 | clean for the in-memory surface; guard added |
| File-controlled loop count (hang, not OOM) | r500 | clean; already guarded in FreeX, no sibling gap |
| Unbounded recursion over file-controlled nesting | r501, r502, r503 | 1 defect (FreeP, fatal); every nesting structure in all three readers enumerated |
| Recursive resolution in EVALUATION (named formulas) | r504 | clean; cycle-detected per (name, scope), returns #REF! as Excel does |
| Decompression bomb (zip bomb) | r505, r506 | clean; honest, lying and zip64-sentinel forms all closed, measured |
| Event-subscription leak (source outlives subscriber) | r507 | clean; 2 static subscriptions, both process-lifetime by design |
| Autosave racing the user's edits | r508 | clean; every shell snapshots on the dispatcher thread |
| Path built from document content (traversal) | r509 | clean; one site, sanitised upstream; reserved names measured harmless |
| Apply throws mid-mutation (torn edit, phantom undo) | r510 | clean; all three buses roll back and push only on success |
| Style inheritance cycle (basedOn chain) | r511 | clean; six walkers, same visited-set idiom; siblings have no cyclic chain |
| Image decoded on every render pass | r512, r513 | FIXED in FreeP Avalonia (shapes, fills) and FreeP WPF (shapes, bullets); FreeW watermark cache moved off a single evicting slot; FreeW's other decodes already cached, converter/import paths are not per-paint |
| Undisposed Avalonia bitmap (no finalizer) | r513 | Avalonia's Bitmap declares no Finalize, so an abandoned one leaks permanently; the remedy is a never-evicting identity cache, NOT eager Dispose, which would risk a queued draw op replaying freed pixels |
| Capture-then-apply index (FreeX) | r529 | clean; cells are KEY-addressed so no positional range exists, and 8 of 9 sheet-index sites are guarded/loop-bounded. MoveSheetCommand.Revert is unguarded but unreachable (sheets removed only by commands on one LIFO stack) and contained by the bus -- recorded with its trigger, not hardened |
| Event subscription multiplying across undo/redo | r530 | clean; exactly ONE event subscription exists in the model+command layers and it already uses remove-first. Adjacent finding: chart position IS snapshot-restored on undo (identity-keyed), but nothing pinned it -- now covered |
| Snapshot aliases live state (undo restores nothing) | r531 | clean in all three apps, by different mechanisms: FreeX copies every dictionary AND its value types are immutable records/strings (the one IReadOnlyList is never cast back or mutated in place); FreeW/FreeP hold two shallow snapshots where sharing element references is correct because only ORDER is mutated |
| Excel exact-answer quirks (1900 leap year, near-zero arithmetic) | r532 | clean and pinned; 1900-02-29 handled explicitly with 1904-system support (279 test refs), and RoundTo15SignificantDigits on every arithmetic result gives Excel's 0.1+0.2-0.3 = 0 |
| Stale or aliased dependency-graph edge | r532 | clean; SetDependencies clears before adding (symmetric for cell AND range precedents), and the cached-plan path shares only frozen/array data the graph never mutates |
| FreeP undo census blind to nested model collections | r533 | FIXED (coverage): Describe now walks animations (+motion path), comments (+replies) and group children recursively; fixture seeds every slide with TWO of each so invented indices land. Exercised 18 -> 19 |
| FreeP census constructor blockers | r534 | blocked types 39 -> 27 via a recursive record/class fallback in the argument factory; twelve more commands now driven through HasEffect+Apply (so the false-no-effect check covers them), but exercised stayed 19 -- constructible is NOT exercised |
| Apply weaker than its own HasEffect precondition | r535 | FIXED in ConvertSmartArtToShapesCommand: HasEffect required Kind==SmartArt, Apply required only that the id exist, so it destroyed a non-SmartArt shape and its animations. Found by the census once r533/r534/r535 made it reachable |
| Census limit: commands taking the previous state as an argument | r536, r537 | NOT checkable, but now DETECTED mechanically (r537): the tell is two parameters of the same non-primitive type, so they are skipped and the rest of the fallback is safe to keep (exercised 34 -> 39, blocked 27 -> 20). CommentMutationCommand and ReplaceCustomShowsCommand take before+after as constructor args, so Revert restoring the invented before is correct by construction; inventing it manufactures false undo failures |
| Census throw bucket assumed harmless | r538 | CHECKED: 5 of 6 are constructor validation rejecting invented arguments (the claimed factory limit); the 6th (SetChartDataTableOptions) throws at execution but cannot tear state -- Apply builds a new settings object and assigns it last |
| FreeW undo correctness (does Revert restore?) | r539 | clean; new census drives every constructible command with VALID arguments and requires Revert to restore the fingerprint exactly -- 10 exercised, 0 failures, neuter-verified. 51 unbuildable / 64 noChange record where its reach ends |
| Census answers enums with their default value | r540 | FIXED in FreeW R539 (last value, not first): every set-an-enum-property command was setting the value it already held and being filed noChange. exercised 10 -> 14. FreeP already stepped past the default; the flaw came from copying the nearer sibling |
| Census fixture contorted to one invented index | r541, r542 | FIXED: the index is a swept SEED (0,1,2), not the constant 0, so each command finds its own target. exercised 14 -> 19 with no fixture change; dissolves the conflict where chart commands need Blocks[0] to be a Paragraph and table commands need it to be a Table |
| FreeW census missing the redo check | r543 | FIXED: Apply-Revert-Apply now verified against the first Apply, matching FreeP. No redo failures among 19 exercised commands; assertion verified to fire, but no behavioural neuter found for FreeW (the one tried was inert) |
| Numeric written outside what the format can represent | r544, r545 | FIXED in FreeP: an infinite/overflowing crop wrote srcRect l=9223372036854775807, but ST_Percentage is an xsd:int, so PowerPoint rejects the file. Writer now clamps to the format range; model has no clamping anywhere upstream |
| Model double cast WIDER than the schema attribute type | r544, r545, r546 | FIXED in FreeP (percentages, angles) and FreeW (16 sites incl. shape/WordArt/group rotation); FreeX clean by construction. Signature: (long) into an xsd:int -- saturation lands outside the type; an (int) cast saturates to a representable value and is safe |
| Non-finite ENTERING the model from a parsed file/clipboard | r547-r552 | FIXED at 6 FreeW reader sites: double.TryParse returns true with Infinity on overflow (1e400), so no hostile token is needed. NumberStyles.Integer sites cannot; Math.Clamp bounds Infinity but passes NaN. FreeX/FreeP readers not yet swept FreeP swept exhaustively in r548 (15 of 17 sites unguarded; r486 had fixed 2). FreeX's 77 still unswept FreeX swept in r549 (13 of 77 fixed; 21 already guarded). Three disguises recorded: Math.Clamp passes NaN, comparison guards fail against NaN, and rot % 360 MANUFACTURES NaN from Infinity. IO layers only -- app/presentation layers unswept r550 adds the SHARED tier (EmuToPixels, OpcCustomDocumentProperties, ZoomPercentPolicy), where one parser serves all three apps. App/presentation layers deliberately not guarded: they parse user input that is range-validated immediately after r551 verified r550's "range-validated downstream" claim: TRUE for consumers that filter (FreeW chart planner, 4 guards), FALSE where the range check uses the REJECT form (x <= 0), which stops neither NaN nor Infinity. FreeX/FreeP dialog layers measured better-guarded and left r552 read the last 8 named files: fixed FreeX's file-controlled chart X cache; AutoFilterChecklistPlanner safe (double.CompareTo is a total order); FreeP's two duration parsers bounded by (int) saturation. IO, shared and dialog layers now all read |
| Non-finite from a subtitle/caption FILE (presentation layer) | r553 | FIXED in PresentationMediaTranscriptPlanner: WebVTT cue position/line percent accepted NaN (reject-form range check) and font-size overflowed via unlimited digits past an exponent-forbidding regex. TTML opacity, clipboard DIP and OA-date paths recorded clean with their mechanism |
| Non-finite from chart-part XML (presentation layer) | r552, r554 | FIXED in FreeX: scatter cached X values (r552) and the error-bar numeric cache (r554), both file-controlled and both feeding axis range. FreeW app layer clean by construction -- its file parsing lives in FreeW.Core.IO, swept in r547 |
| Non-finite from a stored INK annotation | r555 | FIXED in SlideShowInkRenderPlanner: brush width and FreeP thicknessDip/opacity accepted Infinity and NaN while the trace coordinates beside them were already filtered. Math.Max(0.1, x) is NOT a guard -- it propagates NaN |
| Non-numeric literal ranked as a number (Excel divergence) | r556 | FIXED in AutoFilterChecklistPlanner: text "NaN"/"Infinity" sorted in the numeric bucket, NaN ahead of every real number; Excel treats them as text. NOT a crash -- double.CompareTo gives a total order over NaN, so the comparer stays consistent |
| Non-numeric literal ranked as a number -- FreeW half | r556, r557 | FIXED in ParagraphSort.TryParseNumber: "NaN" counted as parseable and sorted ahead of every real number in Word Sort > Number. Same non-crash reasoning as r556 (double.CompareTo is a total order) |
| Non-finite from THEME format-scheme XML | r558 | FIXED in WorkbookTheme: shadow distance/direction, glow and soft-edge radius reached EmuToPixels(double) unguarded -- r550 had guarded only the STRING overload of that same shared helper. Guarding an overload is not guarding a method |
| Digit-run overflow past a SPELLING restriction | r550, r553, r559 | FIXED in ZoomPercentPolicy, ParseWebVttFontSizePx and TableFormulaEvaluator. NumberStyles, regexes and character strips constrain how a number is SPELLED, never how BIG it is -- only IsFinite does that |
| Non-finite reaching a correctly-sized coordinate cast | r560 | FIXED in ConnectionSiteHelper: custom-geometry connection sites saturated to long.MaxValue EMU. Unlike r546 the (long) cast is the RIGHT width for EMU, so the guard belongs at the parse, not the cast |
| Non-numeric literal compared numerically -- merge surface | r561 | FIXED in MailMerge.CompareValues: identical text answered "no match" (NaN != NaN) and two different overlong numbers compared equal. Third surface in the alignment class after r556 and r557 |
| Non-finite entering the model past the evaluator | r562 | FIXED in ExternalLinkModel: an external link's cached sheet data built NumberValue directly, bypassing the evaluator. Disproves r552's claim that a cell value cannot be non-finite -- true of the evaluator, not of the model |
| Non-finite entering the model -- typed and paste doors | r563 | clean; CellEntryParser (4 paths incl. zero-denominator rejection) and PasteCommandFactory (4 branches) all guard. Held by a 32-case behavioural census, neuter-verified on both guard families |
| Non-finite conditional-format THRESHOLD | r564 | FIXED in ConditionalFormatEvaluationMath.TryParseInvariant: a NaN threshold made NotEqual match every cell and every ordering rule match none; LessThan Infinity formatted the whole sheet. The file already carried SetFinite and a test pinning the intended contract |
| Non-finite in a chart SERIES cache | r554, r565 | FIXED in XlsxChartSeriesRangeReader: the plotted-value cache stored Infinity while r554 had guarded only the error-bar cache in another file. Found by a repo-wide census (276 sites, 157 guarded), not by sibling-following |
| Non-finite ICON-SET threshold | r564, r566 | FIXED in SortCommand.TryResolveIconSetBucket: a bare TryParse bypassed the helper r564 guarded, so a NaN threshold gave every cell the lowest icon and -Infinity the highest. All three threshold kinds (Number/Percent/Percentile) guarded |
| Math.Clamp passes NaN -- fourth instance | r547, r548, r555, r567 | FIXED in WordArtWarpPlanner: a warp adjust guide of "val NaN" reached geometry because Math.Clamp bounds Infinity but not NaN. Only the NaN spelling fails -- the other three are genuinely clamped, so sampling one could call the site safe |
| Non-finite sparkline axis bound / line weight | r568 | FIXED in XlsxSparklineMapper. Also recorded: XlsxConditionalFormatClosedXmlMapper must NOT be guarded -- it mirrors ClosedXML's own numeric check by contract, so a finite test there would diverge from the library and reopen a quoting bug |
| Non-finite entering the model from an .ods file | r562, r563, r569 | FIXED in OdsFileAdapter: the THIRD writer bypassing the evaluator. Both the office:value attribute and the text fallback needed guarding -- a value rejected by the first falls through and is re-parsed by the second |
| Non-finite in the chartEx reader (cache + thresholds) | r570 | FIXED but NOT behaviourally tested: FreeX never writes a chartEx part (histograms save as classic chart1.xml), so no save-and-patch fixture can reach it -- the reader exists only for third-party files. Coverage rests on the build and lane sweep |
| One-sided accept form admits Infinity | r551 (corrected), r571 | FIXED in the shared DialogNumericTextPolicy behind 55 dialog call sites. CORRECTS r551: an accept form excludes a non-finite value only when bounded on the side Infinity would exceed -- "value > 0" alone does not |
| One guarded path, unguarded neighbour IN THE SAME METHOD | r554, r555, r560, r570, r572 | FIXED in FontDialogPlanner: r551 guarded kerning and left character spacing and position bare twenty lines above. Reading a FILE to see whether it guards is not a check -- reading each PATH is |
| One-sided accept form on a DURATION | r571 (rule), r573 | FIXED in AnimationPanePlanner and PresentationTransitionCommandPlanner: "seconds >= 0" admits Infinity, and (int)Math.Round(seconds*1000) then saturates to int.MaxValue ms -- about 24.8 days |
| Tripwire watching one SPELLING of its own class | r486 (built), r574 (widened) | The r486 scan matched "out var name" only, missing "out value" / "out double x" -- so r571-r573 found by hand what it existed to catch. Widened + upper-bound rule encoded; found 4 more one-sided guards |
| Non-finite chart scalar (68 call sites) | r574 (named), r575 (fixed) | FIXED in XlsxChartScalarReader.ReadOptionalDouble: manual-layout X/Y/W/H, chart page margins and trendline periods all read unguarded from the chart part. Named in r574 as an aside and nearly left open |
| Non-finite into a cell via the FORMULA door | r562, r563, r569, r576 | PARTLY FIXED: VALUE("NaN") rejected in ValueScalar. Guarding the shared coercion broke 64 tests -- this engine has FUNCTIONS report domain errors. VALUE("1E309") still yields Infinity, pinned by an accessibility test; needs real Excel to settle |

## Known unswept - named so they are a decision, not an oversight

(None outstanding: every class named on this map has been swept. That is NOT a claim that every
possible defect class has been enumerated - see "What the map is for" below. New classes get added
here as they are identified, and the list being empty means the identified ones are done, not that
identification is finished.)

## What the map is for

Two rounds in this stretch (r476, r487) spent their effort on sweeps that produced only false
positives, and r488 established why: a sweep is worth running when its shape has a precise signature
whose violation is definable. The classes above are listed with that in mind - the unswept list is
ordered roughly by how precisely each could be expressed as a signature today.
| Non-finite DATA-VALIDATION bound, persisted | r577 | FIXED in DataValidationNumericBoundText, the ONE parser behind the dialog gate, live enforcement and the save normalizer. A bound of "1E+400" was re-emitted through ToInvariantString, so FreeX wrote the literal text `Infinity` into formula1. Its companion shape check says in its own summary that finite-value policy is the caller's -- and no caller had it |
| Non-finite into a chart cache FreeW itself writes | r547 (read side), r577 (write side) | FIXED in DocxWriter's scatter series: the X cache is built by parsing CATEGORY TEXT, so a sweep over cache PARSING never reached it even though the sibling reader in the same feature was guarded. Third time a sibling was missed for being organised by name/file rather than by behaviour (r565, r567) |
| Non-finite in the SVG parser (4 sites) | r577 | FIXED in SvgIconParser. Filed as low priority on the belief it only reads repo assets -- wrong: FreeW Insert Picture parses a USER-CHOSEN .svg here. Contains both known sub-shapes: BuildRect's reject form `w <= 0 || h <= 0` rejects neither Infinity nor NaN (r551), and both gradient-offset spellings "bounded" with Math.Clamp, which propagates NaN (r547/r555) |
| A guard the scanner cannot SPELL | r574, r577 | NOT A DEFECT, a measurement error, in FOUR modes found this round: the IsFinite displaced past the window by a comment (WorkbookTheme, r558); hidden behind a named helper (AnimationAmountSemantics's IsUsableScale r485, SpreadsheetXmlFileAdapter's IsPositiveFinite); applied at the CALLER (PasteCommandFactory r563, DifFileAdapter); and placed at the function EXIT, 25 lines below the parse (OdsStyleTable.TryParseCm, r549). Same blind spot as r574's tripwire finding -- the rule was right, the spelling watched for was narrow. The argument for encoding a rule in the tripwire over re-running an ad-hoc scan |
| Clamped-only parse (Math.Clamp passes NaN) | r547, r548, r555, r567, r577 (tripwire) | FIXED and now WATCHED: added as a second rule in the r486 tripwire after five rounds of rediscovery. On its first run it found XlsxChartTrendlineErrorBarReader (errBars/val/@val -- a sibling of the CACHE r554 guarded in another file) and XlsxSourceRectangleRatioCodec (a picture srcRect crop). A Math.Min/Max companion scan found nothing |
| Non-finite pivot-cache shared item minted as a NumberValue | r577 | FIXED in PivotTableRefreshService.ParseSharedItemScalarValue: `new NumberValue(number)` straight from a file attribute, another door into the invariant defended at r562/r563/r569/r576. Also fixed: PivotCalculatedExpressionEvaluator.ReadNumber, whose digit-only scanner still overflows on a long enough run of digits |
| Non-finite iterative-calculation threshold | r551 (shape), r577 | FIXED in CalculationOptionsInputParser.TryParseMaxChange: the reject form `parsed < 0` rejects neither Infinity (converges instantly) nor NaN (never converges). Shared by both shells' Options dialogs |
| Non-finite AUTOFILTER threshold | r564 (CF), r577 (filter) | FIXED in PersistedCustomFilterCriterion (a persisted customFilters/@val from the file: NaN hid every row, and notEqual matched every row) and in FilterCriterionInputParser (the typed side, which reported no error at all). Same defect as r564's conditional-format threshold, one feature over |
| Unguarded sibling twenty lines from a guarded one | r554, r555, r560, r570, r572, r577 | FIXED in SortCommand.TryParseLiteralThreshold -- r566 guarded TryResolveIconSetBucket in THE SAME FILE. The class keeps recurring because a file-level check answers a question nobody asked: what needs reading is each PATH |
| The same rule implemented TWICE, both unguarded | r577 | FIXED in both PersistedCustomFilterCriterion (Commands) and XlsxWorksheetAutoFilterCustomFilterMatcher (IO) -- two implementations of "customFilters/@val -> a numeric predicate". Neither file points at the other, so only a mechanical census finds the twin; sibling-following by file or by name cannot |
| Non-finite scenario value applied to cells | r577 | FIXED in XlsxWorksheetScenarioMapper.ParseValue: a scenario is APPLIED to cells, so this minted a cell value that is not a number -- the same invariant door as r562/r563/r569/r576, in a feature none of those touched |
| A characterization test read as a specification | r576 (the reverse), r577 | The srcRect fix broke XlsxSourceRectangleRatioCodecTests.ParseAndFormat_PreserveExistingNonFiniteBehavior, which pinned Parse("Infinity")==1. Unlike r576's 64 tests (a deliberate design, so the GUARD narrowed), this one came from a "centralize ... conversion" REFACTOR commit and asserted Format(NaN) equalled `unchecked((int)nan)` -- the undefined conversion itself, so it could not fail if that changed. Rule: read the commit that introduced the test before deciding which of the two is in the way |
| Non-finite through a one-door attribute reader | r571, r575, r578 | FIXED in XlsxXmlAttributeReader.ReadDoubleAttribute: 33 call sites, every one a BOUNDED quantity from an xlsx part (page margins, pivot group bounds, filter comparison values, top-10 counts, calcPr/@iterateDelta). One guard covers all 33, as r575 did for 68 and r571 for 55 |
| Reject form collapses a whole pivot field to "NaN-NaN" | r551 (shape), r578 | FIXED in PivotTableRefreshService's numeric-range bucketing: `interval <= 0` rejects neither Infinity nor NaN, and BOTH end in NaN (0 * Infinity is NaN too), so every value landed in one bucket whose label was the literal text NaN-NaN. Total, not partial |
| Closing a shared door leaves the bespoke sibling open | r578 | The ReadDoubleAttribute guard covered ReadPivotValueFilters but not ReadNativePivotFilterDoubleValue, which fills the SAME PivotValueFilterModel.ComparisonValue for the x14 shape -- the fix would have created the asymmetry it was meant to end. Rule: before treating a one-door guard as covering its call sites, search by the MODEL FIELD being filled, not the reader's name |
| A screen that is not a screen (Math.Abs < threshold) | r547 (Clamp), r578 (Abs) | FIXED in XlsxColorReader.ReadTint: WorkbookThemeTint.Apply looks like it screens its input with `Math.Abs(tint) < NeutralTintThreshold`, but that is FALSE for NaN, so a non-finite tint reached the HSL luminance maths and the colour silently changed. Same shape as Math.Clamp passing NaN, different disguise |
| Two-sided REJECT range admits NaN | r574 (accept rule), r579 | FIXED in NumberFormatColorMapper (format-string theme tint -> HSL luminance maths) and AnimationPanePlanner (FreeP Smooth Start/End, where NaN became 0 via the saturating int cast). An ACCEPT form with an upper bound excludes Infinity AND NaN; a two-sided REJECT form excludes only Infinity. Now a third tripwire rule. Also verified by probe: "NaN" parses even under AllowLeadingSign|AllowDecimalPoint -- .NET checks the special symbols independently of the style flags |
| A tripwire silenced by the comment explaining the fix | r577 (as false positive), r579 (as false negative) | The new reject-range rule PASSED with its fix reverted: the multi-line comment documenting the fix pushed the guard outside the 400-char window. All three rules now scan comment-stripped source. Rule: a tripwire is not trustworthy until reverting the fix it was written for is shown to make it FAIL -- green and vacuous look identical |
| Arithmetic manufactures a NaN nobody parsed | r549, r580 | FIXED in TryParseElapsedHmsText (TIMEVALUE): the regex admits digits only, so nothing non-finite can be SPELLED past it, but it does not bound the digit COUNT -- hours overflows to Infinity and `totalDays - Math.Floor(totalDays)` is Infinity - Infinity = NaN, returned as a NumberValue. Third time a regex was mistaken for a bound (cf. NumberStyles, r550/r553/r559): a pattern constrains spelling, not magnitude |
| Infinity in a COMPARISON is not a defect | r580 | NOT A DEFECT, and the boundary of this program: NumberFormatter.TryParseCondition's `[>1e400]` parses to Infinity, but `value > Infinity` is false for every finite value -- which is the correct answer, since no finite number is greater than 10^400. NaN is unreachable there (the regex forbids it). Genuine defects are non-finite values STORED, PERSISTED, fed to arithmetic that manufactures NaN, or a NaN reaching a comparison; a finite-vs-Infinity comparison is none of those |
| Save-side normalizer round-trips a threshold through double | r577 (DV bounds), r581 (CF thresholds) | FIXED in NormalizeNumericCfvoValueForSave, the documented single write choke point for a conditional-format threshold: "1E+400" was re-emitted as the literal text Infinity into the cfvo, which the schema cannot express. Same shape as r577's data-validation bound, different feature; found only because the search key changed from the PARSE to the CONSEQUENCE. Both of its parses needed guarding -- guarding one lets the value fall through to the other (r555) |
| Model-invariant door: parsed double -> NumberValue | r562-r577 (closed), r581 (verified) | CONVERGED: a repo-wide scan for a parsed double flowing into new NumberValue with no finite test finds NO remaining sites in any of the three apps. This is the one sub-class with a clean closure signal rather than a coverage claim |
| Non-finite double: five consequence shapes | r562-r581 | LENS DRY: scanning by consequence rather than by unguarded parse, all five shapes (-> NumberValue, -> file, -> NaN comparison, -> saturating cast, -> NaN-manufacturing arithmetic) have no remaining unguarded sites in any of the three apps, and the sharpest stays dry at 4x the scan window. A coverage measure, not a proof: a consequence beyond the window or behind a helper is invisible |
| Int parse FAILS where a double parse overflows | r582 | NEW LENS: int.TryParse returns false on overflow, so the risk is the caller's FALLBACK, not a poisoned value. ReadIntAttribute returns null for both "absent" and "present but unreadable", so no caller can tell a corrupt file from a default one. Of 42 `?? x` sites, 11 use -1 WITH a reject (deliberate sentinel discipline) and 11 use 0, which is the correct OOXML default for most. Whether a corrupt attribute should be defaulted or refused needs real Excel; recorded, not guessed |
| Table id guarded, column id not, same file | r582 | FIXED in XlsxStructuredTableWriter: three places already say an id is positive (the table-id write, the column synthesizer's Enumerable.Range(1,...), the reader's `if (id <= 0) return false`), but the column write had no rule -- so an absent/unreadable column id was written back as id="0", twice over for two such columns. Fix assigns the smallest UNUSED positive id; a positional index+1 collides with a valid id later in the list, and the test catches that variant |
| One bad attribute makes the whole workbook unopenable | r365, r366, r369, r583 | FIXED for 17 of 19 by XlsxOutOfRangeIntegerAttributeNormalizer. Found by a MUTATION PROBE, not a scan: no FreeX parse site is involved, because FreeX hands the part to DocumentFormat.OpenXml, which throws OverflowException/FormatException and aborts the load. One rule on the value (a decimal-digit string that does not fit in uint) covers all of them with no per-attribute type table. The 2 survivors are required IDENTITY attributes that cannot be defaulted away; for them the fix converts an unhandled library exception into FreeX's own typed error |
| A scan cannot see a defect in a library you delegate to | r583 | METHOD: the r582 static lens read as correct-by-design, and the probe on the same surface found 19 defects. When the risky operation happens inside a dependency, the only lens that reaches it is running real (malformed) input through the real loader |
| Any malformed value in a typed attribute aborts the load | r583 (oversized), r584 (all shapes) | FIXED by XlsxMalformedTypedAttributeNormalizer: 98 aborted loads across 320 mutants became 12, all confined to the two required identity attributes and all now FreeX's own typed WorkbookInvalidException rather than an unhandled OpenXml exception. Needs a TABLE where r583 needed one rule, because "true" is legal in a boolean and fatal in an integer -- and the table is evidence (each entry demonstrated by the probe, each type established by a discriminating "true" mutation) rather than a reading of the schema |
| A probe can correct the fix, not just find the defect | r584 | METHOD: the first table typed every integer attribute as signed; re-running the probe showed seven still aborting on "-1" because they are xsd:unsignedInt. The kind split into Unsigned/Signed followed from the evidence. A careful reading of the fix would not have caught it |
| Deleting a REQUIRED attribute moves the failure, it does not fix it | r583 (misdiagnosed), r585 (fixed) | FIXED for comment/@authorId and cellStyle/@xfId by REPLACING the unreadable value with the schema's safe default (0) rather than removing it -- both are indexes whose zero entry always exists when the part does. r583 had filed xfId as "unrepairable" on the evidence of a drop that failed; the diagnosis was the remedy's, not the attribute's. sheet/@sheetId genuinely cannot be repaired this way: it must be UNIQUE, so no constant is safe |
| A probe covers only what its fixture contains | r585 | METHOD: growing the fixture by a chart, a CF, comments, a hyperlink and a merge took the surface from 384 to 455 mutants and immediately found two more aborting attributes (color/@theme, comment/@authorId). The r584 claim that "the tripwire keeps the table from rotting" was tested rather than asserted, and the table was two entries short the first time the surface grew |
| Two normalizers disagreeing over one attribute | r585 | FIXED by sharing the required-attribute policy (TryGetRequiredDefault): the universal r583 rule runs first and DELETED an oversized cellStyle/@xfId before the typed r584 rule could repair it, so xfId="4294967296" stayed unopenable while xfId="yes" opened. When two passes can both touch an attribute, the policy must live in one place |
| A neuter is not a green run | r512 (neuter rule), r585 | PROCESS: r583's tightened tolerance was run ONCE, with the fix neutered, to prove it bit -- and never run green afterwards. The full lane caught it. A neuter proves a test CAN fail; it says nothing about whether it passes. Both directions are separate evidence |
| FreeX saves a workbook it cannot reload | r586 | FIXED in both directions: ToggleWorksheetAutoFilterCommand now REJECTS a filter over a structured table (Excel has no such state), and CreateStructuredTableCommand CLEARS an overlapping worksheet filter (Excel replaces it with the table's own). ClosedXML refuses the combination on read, so a user selecting a table and clicking Filter could produce a file this application cannot open. Undo restores the cleared filter |
| A missed symmetry the file itself documents | r586 | CreateStructuredTableCommand's merge guard says in its own comment that it mirrors MergeCellsCommand "enforced from the other direction (merge created first, table attempted second)". The AutoFilter rule needed the same treatment from both sides and had it from neither -- the reasoning was written down and simply not applied to the next invariant |
| A uniform probe result means a broken probe | r566, r586 | METHOD: the r586 fixture reported 525 of 530 mutants failing, including attributes that passed in r585. Loading the UNMUTATED fixture showed it did not load either. Validate the probe before believing it, and treat suspicious uniformity as the cheapest signal that something upstream is broken -- the artefact then turned out to BE the defect |
| Guarding the known paths is not making a state unreachable | r586 (commands), r587 (writer) | FIXED at the WRITE chokepoint: XlsxWorksheetAutoFilterXmlMapper.Save suppresses a worksheet autoFilter overlapping a structured table, so the unreadable file is UNWRITABLE rather than merely unreachable by the two commands r586 guarded. A model reaches the same state by other roads -- an .fxl load, a paste, a command not yet written -- and the probe proved it by building the model directly. Overlap is computed on parsed GridRanges, so a partial overlap is caught too |
| Bounding a finding is worth as much as finding it | r587 | A pairwise probe over every ordered pair of overlapping entities (table, autofilter, merge, data validation, conditional format, hyperlink -- 30 pairs) found that table+autofilter is the ONLY combination FreeX can save but not reload. That makes r586 a complete diagnosis rather than the first instance of something wider, which is a different and more useful claim than "no more found" |
| Aborted-load class is LIBRARY-induced, not format-induced | r583, r584, r588 | ANSWERED by running the same probe against readers this repo writes itself: FreeW's DocxReader (250 mutants) and FreeP's PptxPackageReader (515) abort on NOTHING, where FreeX's ClosedXML-backed reader aborted 19 then 98 times. Justifies FreeX's two normalizers as compensation for a dependency's strictness, and justifies NOT adding them to FreeW/FreeP. Both probes kept as tripwires for the day either reader adopts a strict parser |
| Save -> load -> save is idempotent | r588 | CLEAN: two round trips over a rich workbook (formula, merge, comment, hyperlink, table, DV, CF, chart, autofilter) leave the model identical to one trip, with both saves the same byte length. No drift, no second-save loss |
| Undo completeness, audited at runtime | r586 (the defect), r589 (the audit) | ADDED: every constructible command that succeeds is applied, reverted, and required to fingerprint back. 78 commands, 251 pairs, 0 violations. Three fields excluded as monotonic BY DESIGN (ContentVersion, NextStructuredTableIdWatermark, StyleCount) -- without that calibration it reports 40 by-design violations, which is worse than no audit. Reverting r586's restore makes it fire, so it catches the defect it was built for |
| Snapshot-based undo is invisible to a static scan | r589 | METHOD, and the fifth blind spot of this kind: a per-class Apply/Revert scan produced nine candidates and all nine were false positives, because undo here restores through a snapshot or a differently-named setter and names no member at all. The pattern that makes the code robust is the pattern the scan cannot see -- which is why the runtime fingerprint audit exists |
| Enriching a harness fixture can silently REMOVE coverage | r590 | METHOD: adding a structured table to the shared command-audit fixture could have landed on the range the argument bank uses, making CreateStructuredTableCommand fail on "cannot overlap another table" instead of reaching the clear-the-filter branch -- the audit would stay green and quietly stop catching r586. Capability was re-verified by neutering that fix again. A coverage increase must be shown not to have removed coverage, and "still green" does not show it |
| Redo mints a fresh identity, yielding a different workbook | R16/R17/R83 (sheet ids), r591 (the rest) | FIXED in CreateStructuredTableCommand (table id AND name), CreateStyledStructuredTableCommand (Apply built a brand-new inner command each call, throwing away the cached id -- so the delegation covered undo but NOT redo, correcting r589) and AddChartSheetCommand (R17 had stabilised the chart SHEET id and left the CHART's own Guid fresh -- half the fix). Found by adding the redo half to the r589 fingerprint audit: 12 violations across 4 commands, all one shape |
| A known gap pinned so it cannot widen | r591 | DuplicateSheetCommand re-clones on every apply, so the copy's CF, DV, picture and table take fresh ids on redo. Stabilising them threads minted ids through five helpers in a method five earlier rounds have worked over, so the audit allows those FOUR EXACT paths and nothing else: the drift cannot grow, and the allowance goes stale visibly when it is fixed. Better than a vague TODO and than a fix rushed through that method |
| Raw framework exception shown to the user for a damaged file | r382 (the mapping), r592 (two shapes it missed) | FIXED by extending IsMissingPackagePartFailure: a missing xl/_rels/workbook.xml.rels threw ArgumentOutOfRangeException from OpenXml's GetPartById and a missing worksheet .rels threw KeyNotFoundException from a bare Dictionary indexer. Neither matched r382's InvalidOperationException-message or NullReferenceException shapes, and the shell shows exception.Message VERBATIM -- so the user read "Index was out of range" for a corrupt file. Found by deleting each package part in turn |
| A damaged file loads with content silently gone | r448-r450, r592 | PINNED, not fixed: removing a chart part or its drawing rels loads successfully with the chart absent, because ReadChartParts has three bare `continue`s and no warnings channel reaches it. Severity stated precisely -- the chart is ALREADY gone from the file, so this is a failure to REPORT damage rather than corruption. The two paths are pinned exactly so the silence cannot spread to other content |
| Declared-but-unresolvable chart dropped in silence | r592 (found, pinned), r593 (fixed) | FIXED by threading a warnings channel three signatures (LoadSheetXmlLayout already had one) and reporting at all FOUR silent exits, not just the two the probe named: blank relationship id, unresolvable target, missing chart part, missing rels part. The last warns ONLY when the drawing declares charts, since a shape-only drawing legitimately has no rels part -- and a second test pins that an undamaged workbook warns nothing, because a noisy channel stops being read |
| A neuter can be silently ineffective | r512, r593 | METHOD: neutering a warning by replacing its TEXT with "" left the test passing, correctly, because an empty warning is still a warning and the assertion counts warnings. Earlier failures in this class did not COMPILE and announced themselves; this one compiled, ran and passed. A neuter must change the thing the assertion MEASURES, not merely the code near it |
| Declared-but-unresolvable chart dropped in silence (FreeW) | r593 (FreeX), r594 (FreeW) | FENCED, not fixed: removing word/_rels/document.xml.rels drops the chart run entirely and removing word/charts/chart1.xml keeps the run without its chart. Same class as r593, reached by completely different code -- so r588's "FreeW is immune to the library-induced abort class" licensed no conclusion here. Fixing needs a new public ReadWithWarnings API and a shell surface FreeW does not have (it warns on SAVE only), which is a product decision; the two paths are pinned so the silence cannot spread |
| A lens generalises even when a conclusion does not | r588, r594 | METHOD: r588 proved the aborted-load class was library-induced and FreeW immune. Carrying that CONCLUSION forward would have skipped FreeW here; carrying the LENS found two real losses. Different classes have different reach, and immunity to one says nothing about another |
