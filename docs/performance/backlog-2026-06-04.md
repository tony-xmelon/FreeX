# FreeX Performance Backlog

**Last updated:** 2026-06-05
**Status:** Current working backlog after the June 4 end-to-end performance pass, loaded-workbook patch-save slices, deterministic XLSX IO benchmark fixture, load package-fact reuse, follow-up XLSX save patch expansion/snapshot carry-forward, existing-source-style patch saves, direct row/column dimension patch saves, merge-range patch saves, existing internal-hyperlink patch saves, existing legacy-note text patch saves, structured-table-safe scalar patch saves, save-path fallback attribution, chart/pivot/custom-view/rich-shared-string package guard expansion, compressed style-only load storage, app-shaped XLSX IO benchmarking, open-recalc trust of cached XLSX values, streaming feature-inspection XML scans, and source-snapshot open-path diagnostics/reuse.

This document records the remaining performance work that is worth pursuing after the practical low-risk UI/model/host pass. The next active focus is file IO, with XLSX open/save as the highest priority because it is the user-visible compatibility path and still trails Excel on large workbooks.

## Active Priority: XLSX Open/Save

Goal: move FreeX XLSX open/save closer to Excel while preserving the current fidelity contract, package-retention guarantees, and Excel openability smoke coverage.

Current constraints:

- `XlsxFileAdapter` still uses ClosedXML for workbook materialization and save, so large improvements may require bypassing ClosedXML in selected hot paths rather than tuning around it.
- XLSX save also performs package post-processing to retain native metadata. Optimizations must not weaken [formats/xlsx-corpus-report.md](../formats/xlsx-corpus-report.md) guarantees.
- Benchmark timing from xUnit microbenchmarks is noisy; allocation and repeated relative comparisons are more reliable than a single run.

Completed in this pass:

- Added external workbook stage/save benchmarks gated by `FREEX_IO_BENCHMARK_PATHS`.
- Added app-load integration so `OpenWorkbookLoader` can use the `XlsxFileAdapter` load pass feature report instead of opening and inspecting real XLSX files separately.
- Added worksheet schema-normalizer preflight so already ordered worksheets skip full `XDocument` loads during save post-processing.
- Measured `XLSX_SAVE_STYLE_ONLY` improvement from `mean_ms=741.24 allocated_bytes=174,363,856` to latest verification `mean_ms=526.84 allocated_bytes=112,295,376`; best post-change run observed `mean_ms=484.71`.
- Added a loaded-workbook source-package patch-save path for small existing literal-cell edits. The fast path patches worksheet XML in the original XLSX package only when a restored model fingerprint proves the rest of the workbook is unchanged.
- Measured Partner Dashboard edited-save improvement from the current branch baseline `elapsed_ms=34,040.55 allocated_bytes=5,066,374,640` to latest merged-main verification `elapsed_ms=1,338.00 allocated_bytes=37,079,032` for a one-cell existing literal edit; best post-change run observed `elapsed_ms=1,336.11`. Earlier June 4 measurements before the package-patch slice ranged from `elapsed_ms=43,965.35` to `32,336.62`.
- Added deterministic generated style-heavy XLSX load/save benchmarks with 3 sheets, 400 rows per sheet, 12 value columns, and 160 style-only columns per sheet (`192,000` style-only cells). Latest verification: `XLSX_LOAD_GENERATED_STYLE_HEAVY mean_ms=1,404.08 allocated_bytes=350,996,024`; `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY mean_ms=559.37 allocated_bytes=117,024,760`.
- Extended loaded-workbook patch-save to unchanged formula cells whose cached values changed. The patch preserves `<f>` and calc-chain/package parts and only rewrites cached value metadata. Latest deterministic verification: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FORMULA_CACHE mean_ms=309.90 allocated_bytes=117,022,552`.
- Reused load-time sheet XML facts to drive ClosedXML package preparation: duplicate style-only worksheet paths, unsupported conditional-formatting requirements, dynamic-filter cleanup, and pivot package presence now reuse existing package/layout inspection when trusted and fall back to the old scans when incomplete. Latest repeat verification: `XLSX_LOAD_GENERATED_STYLE_HEAVY mean_ms=956.37 allocated_bytes=351,027,728`; `XLSX_LOAD_IGNORED_ERROR_STYLE_ONLY_METADATA mean_ms=438.93 allocated_bytes=131,985,640`. Best post-change run observed `XLSX_LOAD_GENERATED_STYLE_HEAVY mean_ms=890.59`.
- Measured Partner Dashboard open at `elapsed_ms=16,378.75 allocated_bytes=879,876,344` in the end-to-end external load benchmark, with a repeat run at `elapsed_ms=15,965.42`. The staged benchmark after prior package warm-up measured raw ClosedXML load failure on source XML, sanitized ClosedXML load at `elapsed_ms=13,150.22`, style-stripped sanitized ClosedXML load at `elapsed_ms=2,918.80`, and full FreeX load at `elapsed_ms=6,844.27`.
- Extended loaded-workbook patch-save to simple formula-text edits, inserted literal cells, and cleared cells. Formula-text edits rewrite ordinary `<f>` elements, update cached values, and remove calc-chain entries; shared/attributed formulas still fall back. Inserted literal cells and cleared cells keep the package patch path for common typing/deleting edits. Latest deterministic verification on the generated style-heavy fixture after carrying the patch baseline forward instead of rescanning the patched package: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY mean_ms=358.87 allocated_bytes=110,105,552`; `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FORMULA_CACHE mean_ms=167.35 allocated_bytes=110,096,288`; `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FORMULA_TEXT mean_ms=418.75 allocated_bytes=111,829,232`; `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_NEW_CELL mean_ms=171.56 allocated_bytes=116,176,984`; `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_CLEARED_CELL mean_ms=170.57 allocated_bytes=116,172,136`; forced new-style full fallback `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FULL_FALLBACK mean_ms=1,069.88 allocated_bytes=511,665,392`.
- Extended loaded-workbook patch-save to existing-source-style cell edits. The patch baseline now maps FreeX `StyleId` values back to unambiguous source worksheet `s` indexes from the load-time sheet XML facts, then patches only worksheet cell `s` attributes; brand-new styles still fall back so `styles.xml` mutation stays explicit. Latest focused verification on the generated style-heavy fixture: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_EXISTING_STYLE mean_ms=257.55 allocated_bytes=120,374,176`; same-run forced new-style fallback pair observed `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FULL_FALLBACK mean_ms=1,680.25 allocated_bytes=548,122,880`. A clean current-main load comparison measured `XLSX_LOAD_GENERATED_STYLE_HEAVY allocated_bytes=539,271,952`; branch load remained comparable at `allocated_bytes=539,969,928`.
- Extended loaded-workbook patch-save to direct row/column dimension edits: row heights, hidden rows, column widths, and hidden columns now patch worksheet XML directly when defaults, outlines, grouped/collapsed state, filter-hidden rows, and unrelated workbook state remain unchanged. Latest deterministic verification: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_DIMENSIONS mean_ms=238.36 allocated_bytes=116,233,280`; same-run forced new-style fallback observed `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FULL_FALLBACK mean_ms=1,803.52 allocated_bytes=548,136,176`.
- Extended loaded-workbook patch-save to merge-range edits. The patch path now rewrites the worksheet `<mergeCells>` block in the original package, preserving existing merge container/native attributes and per-merge attributes for retained references while adding/removing modeled merge refs. Latest deterministic verification: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_MERGED_REGIONS mean_ms=301.94 allocated_bytes=116,224,072`; same-run forced new-style fallback observed `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FULL_FALLBACK mean_ms=1,859.36 allocated_bytes=548,123,696`.
- Extended loaded-workbook patch-save to existing single-cell internal hyperlink edits. The patch path updates worksheet `<hyperlink>` `location`/`tooltip` attributes directly while preserving `display`, container attributes, custom link attributes, and avoiding worksheet relationship edits; external/range/add/remove hyperlink changes still fall back. Latest deterministic verification: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_INTERNAL_HYPERLINK mean_ms=274.35 allocated_bytes=116,238,032`; same-run forced new-style fallback observed `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FULL_FALLBACK mean_ms=2,163.81 allocated_bytes=696,605,864`.
- Extended loaded-workbook patch-save to existing legacy note/comment text edits. The patch path updates only the matching `xl/comments*.xml` text node while preserving authors, comment attributes, worksheet relationships, and unchanged note-only VML; added/deleted/moved notes, rich multi-run note text, non-note VML, header/footer VML, and broader drawing graphs still fall back. Latest deterministic verification: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_LEGACY_COMMENT mean_ms=121.50 allocated_bytes=6,089,344`; same-run forced new-style fallback observed `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FULL_FALLBACK mean_ms=2,093.98 allocated_bytes=697,364,896`.
- Tightened the package patch eligibility guard so it allows empty `tableParts count="0"` placeholders from ClosedXML/Excel, unchanged note-only `legacyDrawing` VML that maps one-to-one to existing comment refs, and simple unchanged table relationship graphs, but still declines unsupported/malformed table relationships, normal drawings, header/footer VML, charts, pivots, custom views/properties, revision metadata, and rich shared-string font cases that require full compatibility post-processing.
- Extended loaded-workbook patch-save to structured-table-safe scalar edits. The patch path now allows existing literal scalar cell edits in table workbooks when table definitions, relationships, package parts, and model metadata are unchanged; it rejects table headers, totals rows, inserted/deleted/style/formula changes, filtered/native-filtered/sorted tables, calculated-column cells, query/external table parts, table part relationships, and other table definition changes. Latest deterministic verification: `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_STRUCTURED_TABLE mean_ms=226.88 allocated_bytes=116,320,520 save_path=source_patch save_reason=patch_applied`; same-run forced new-style fallback observed `XLSX_SAVE_LOADED_GENERATED_STYLE_HEAVY_FULL_FALLBACK mean_ms=1,544.24 allocated_bytes=696,015,232 save_path=full_save save_reason=change_new_style`.
- Added XLSX save-path diagnostics so benchmark output and focused tests can distinguish source copy, source-package patch, and full-save fallback. The diagnostics report `save_path`, `save_reason`, total patch changes, and per-feature patch counts; current fallback reasons include no source package, package/workbook guard blocks such as drawings/pivots/table parts, baseline cell-limit failures, unsupported mutation families, change-limit failures, and patch-apply failures. The external edited-save benchmark now prints these fields for real workbook measurements.
- Indexed target ZIP entries inside XLSX package metadata merging so fallback native-metadata preservation avoids repeated case-insensitive archive walks while copying, deleting, relationship-filtering, and content-type checks.
- Used the new `save_path`/`save_reason` diagnostics to walk the Partner Dashboard edited-save blockers from `workbook_postprocessing_charts` to `workbook_postprocessing_pivots`, `package_guard_custom_workbook_views`, `package_guard_custom_sheet_views`, and `package_guard_rich_shared_string_fonts`. The patch-save guard now allows unchanged classic chart graphs, simple worksheet-range pivots, modeled custom views, native-only custom-view cleanup, and in-place rich shared-string font sanitization while still rejecting chart/pivot source cell edits, invalid custom-view IDs, remapped pivot worksheet package paths, and unsupported drawing/pivot features. Latest Partner Dashboard edited-save verification after syncing current `main`: `elapsed_ms=2,554.46 allocated_bytes=28,184,864 save_path=source_patch save_reason=patch_applied patch_changes=1`, down from the immediately preceding full-save measurement `elapsed_ms=43,210.51 allocated_bytes=5,914,637,312 save_path=full_save save_reason=package_guard_custom_workbook_views` and the initial chart/pivot full-save blockers around `42,792-68,736 ms`.
- Added compressed style-only run storage behind the existing `Sheet` style-only APIs and changed XLSX load replay to coalesce adjacent same-style blank cells into row runs instead of inserting every blank into the mutable dictionary. Focused and full verification passed for Core.Model and Core.IO. In the comparable external staged benchmark lane, current `main` measured Partner Dashboard `FREEX_FULL_LOAD elapsed_ms=10,300.50 allocated_bytes=1,641,092,312`; this branch measured `elapsed_ms=8,297.52 allocated_bytes=1,612,550,400`. Direct single-process xUnit external load remains noisy (`21,566-24,134 ms` observed on this branch), so app-level open timing should remain the deciding measurement for the next load slice.
- Rechecked Partner Dashboard edited-save after compressed style-only load storage. The save path still uses `source_patch`; latest observed run: `elapsed_ms=1,013.73 allocated_bytes=28,174,376 save_path=source_patch save_reason=patch_applied patch_changes=1`, with prior same-branch repeats in the `2,554-3,516 ms` range. This keeps the formerly unacceptable `~44s` save case close to, and sometimes faster than, the current load timing envelope.
- Added `tools/FreeX.AppIoBench`, an app-shaped XLSX open/edit/save probe that drives `OpenWorkbookLoader` and `SaveWorkbookWriter`, supports real or no-op formula recalculation delegates, and emits `APP_XLSX_OPEN` / `APP_XLSX_SAVE` records with elapsed time, allocation, progress count, and save-path diagnostics.
- Fixed a style-only run enumeration edge case where a run-backed overlay or tombstone at the final column could continue past the run end. XLSX load can create this shape when ClosedXML materializes blank styled cells before the adapter installs compressed row runs. The fix also removes redundant overlays after run installation and exposes compressed runs for fingerprinting without expanding every blank styled cell.
- Changed loaded XLSX source snapshot rebasing so automatic open recalculation becomes the patch-save baseline instead of appearing as thousands of formula-cache edits during save. The app-shaped Partner Dashboard probe with a real recalculation delegate changed from `APP_XLSX_OPEN elapsed_ms=46,468.47 allocated_bytes=19,634,480,296` plus `APP_XLSX_SAVE elapsed_ms=1,177.70 save_path=source_patch` to the next open-skip measurements below; this confirms the prior `~44s` save symptom was formula-cache noise entering the save diff.
- Open now trusts cached formula values for normal XLSX/XLSM/XLTX/XLTM packages and only runs the expensive full-workbook recalculation when workbook `fullCalcOnLoad`, workbook `forceFullCalc`, or sheet `sheetCalcPr fullCalcOnLoad` explicitly asks for it. Focused app-host tests cover default cached-value trust and each full-calc trigger. App-shaped Partner Dashboard measurements with the production recalculation delegate now skip the full recalc and measured `APP_XLSX_OPEN elapsed_ms=21,871.86 allocated_bytes=2,192,682,632` / `APP_XLSX_SAVE elapsed_ms=1,831.46 save_path=source_patch`, with a warm repeat at `APP_XLSX_OPEN elapsed_ms=20,450.47 allocated_bytes=2,194,043,624` / `APP_XLSX_SAVE elapsed_ms=1,899.90 save_path=source_patch`.
- Replaced full `XDocument` allocation with streaming `XmlReader` scans for feature-inspector checks that only need to know whether simple XML elements/attributes exist in connections, worksheets, drawings, and custom properties. Focused feature-inspector coverage passed. Partner Dashboard app-shaped open allocation dropped from roughly `2.19 GB` to `1.69 GB` in repeat runs (`APP_XLSX_OPEN elapsed_ms=22,641.29 allocated_bytes=1,688,110,744` and warm `elapsed_ms=22,098.67 allocated_bytes=1,687,420,608`); elapsed time did not improve on the busy workstation, so the value is memory-pressure reduction rather than a proven wall-clock win.
- Added internal XLSX load-phase diagnostics and surfaced them in `FreeX.AppIoBench`. The first Partner Dashboard phase split on this slice measured `APP_XLSX_OPEN elapsed_ms=21,278.91 allocated_bytes=1,688,299,064`, with adapter core load `21,088.96 ms / 1,687,868,656 bytes`; the largest remaining open phases were `closedxml_load_ms=7,987.15`, `source_snapshot_ms=6,818.69 allocated_bytes=903,578,488`, `sheet_xml_layout_ms=2,873.72`, and `package_metadata_ms=2,448.47`.
- Deferred the source-package patch eligibility guard from open to the first patch-save attempt while keeping the cell patch baseline eager so pre-save user edits are still detected. This moved pure save-prep package graph scans out of open without weakening patch-save correctness. Partner Dashboard recheck: `APP_XLSX_OPEN elapsed_ms=19,330.77 allocated_bytes=1,358,726,936 load_source_snapshot_ms=5,257.37 allocated_bytes=576,477,352`; first edited save now pays that lazy guard and measured `APP_XLSX_SAVE elapsed_ms=5,112.71 allocated_bytes=419,444,600 save_path=source_patch`.
- Reused load-time chart package parts while building the source snapshot chart-source index and skipped source hyperlink XML reads for sheets with no loaded hyperlinks. The latest Partner Dashboard app-shaped run measured `APP_XLSX_OPEN elapsed_ms=19,443.67 allocated_bytes=983,289,960 load_source_snapshot_ms=3,680.03 allocated_bytes=201,328,112` and `APP_XLSX_SAVE elapsed_ms=3,721.33 allocated_bytes=419,444,520 save_path=source_patch`. Timings remain noisy on the busy workstation, but source-snapshot allocation dropped from about `904 MB` to `201 MB` and total open allocation dropped from about `1.69 GB` to `983 MB`.
- Split loaded-package fingerprints into the full source-copy fingerprint and a cheaper patch-validation fingerprint. Patch validation now skips occupied-cell JSON serialization because `XlsxCellPatchBaseline` already carries original occupied cell/dimension/merge/hyperlink/comment/table state; the full source-copy fingerprint is unchanged for small unmodified workbooks. Partner Dashboard app-shaped repeats from this branch measured `APP_XLSX_OPEN elapsed_ms=17,020.98 allocated_bytes=978,135,272 load_source_snapshot_ms=3,247.17 allocated_bytes=193,366,664` with `APP_XLSX_SAVE elapsed_ms=3,356.78 allocated_bytes=418,035,488 save_path=source_patch`, then a noisy repeat at `APP_XLSX_OPEN elapsed_ms=26,451.79 allocated_bytes=975,101,544 load_source_snapshot_ms=6,413.26 allocated_bytes=193,418,632` with `APP_XLSX_SAVE elapsed_ms=4,200.89 allocated_bytes=418,040,112 save_path=source_patch`. Final rebased verification measured `APP_XLSX_OPEN elapsed_ms=17,753.73 allocated_bytes=976,520,056 load_source_snapshot_ms=3,722.11 allocated_bytes=193,706,976` and `APP_XLSX_SAVE elapsed_ms=3,410.05 allocated_bytes=418,048,968 save_path=source_patch`. The practical save band for the formerly `~44s` Partner Dashboard one-cell edit is now about `3.4-4.2s` in this app-shaped lane, below the current load timing envelope.
- Reused the current patch-validation fingerprint for existing-cell-only patch saves after it proves no unrelated metadata changed, instead of hashing once for validation and hashing again to refresh the carried-forward baseline. This keeps inserted/deleted cells, dimensions, merges, hyperlinks, and comments on the conservative recompute path. Partner Dashboard app-shaped repeats measured `APP_XLSX_SAVE elapsed_ms=2,859.65 allocated_bytes=387,894,976 save_path=source_patch` and `APP_XLSX_SAVE elapsed_ms=3,209.95 allocated_bytes=387,884,064 save_path=source_patch`, with corresponding opens at `18,470.27 ms` and `20,811.65 ms`.
- Deferred `XlsxCellPatchBaseline` materialization out of open and into the command-bus pre-mutation path. Saves refuse lazy baselines with `patch_blocked_deferred_baseline_not_materialized`, so direct model mutations cannot be accidentally captured as the original state. `FreeX.AppIoBench` now reports the prepare phase separately. Partner Dashboard app-shaped repeats measured `APP_XLSX_OPEN elapsed_ms=16,306.72 allocated_bytes=782,740,408 load_source_snapshot_ms=13.53 allocated_bytes=336`, `APP_XLSX_PREPARE_EDIT elapsed_ms=5,386.39 allocated_bytes=517,067,232`, and `APP_XLSX_SAVE elapsed_ms=2,763.66 allocated_bytes=387,886,864 save_path=source_patch`; repeat measured `APP_XLSX_OPEN elapsed_ms=14,985.78 allocated_bytes=784,209,552 load_source_snapshot_ms=18.96 allocated_bytes=336`, `APP_XLSX_PREPARE_EDIT elapsed_ms=5,236.63 allocated_bytes=517,082,696`, and `APP_XLSX_SAVE elapsed_ms=2,870.94 allocated_bytes=387,874,080 save_path=source_patch`. This improves perceived open latency and keeps save fast, but the first edit now carries a measured `~5.2-5.4s` prepare cost until compact load-time baseline facts are carried forward.
- Carried compact load-time baseline facts forward without retaining full `SheetXmlLayout` XML graphs: worksheet paths, explicit source style indexes, and a parsed chart-source range index now feed the first-edit baseline builder, and the facts are released once the real baseline is materialized. `XlsxPatchCell` is now a readonly record struct to avoid per-cell snapshot object allocations. Partner Dashboard app-shaped repeats measured `APP_XLSX_OPEN elapsed_ms=16,500.00 allocated_bytes=782,358,512 load_source_snapshot_ms=34.49 allocated_bytes=55,672`, `APP_XLSX_PREPARE_EDIT elapsed_ms=3,530.33 allocated_bytes=192,218,264`, and `APP_XLSX_SAVE elapsed_ms=2,726.08 allocated_bytes=387,882,840 save_path=source_patch`; repeat measured `APP_XLSX_OPEN elapsed_ms=16,066.55 allocated_bytes=783,611,288 load_source_snapshot_ms=34.43 allocated_bytes=55,672`, `APP_XLSX_PREPARE_EDIT elapsed_ms=3,362.29 allocated_bytes=192,334,128`, and `APP_XLSX_SAVE elapsed_ms=2,796.97 allocated_bytes=387,882,744 save_path=source_patch`. The redundant first-edit XML/style allocation is gone; the remaining `~3.3-3.5s` prepare cost is the actual in-memory patch baseline construction.
- Replaced the first-edit patch baseline's per-sheet duplicate coordinate dictionaries with sorted `XlsxPatchCellEntry` arrays plus binary-search lookups. This keeps inserted/updated/deleted cell detection equivalent while avoiding rehydrating another large hash table over the same occupied cell map. Partner Dashboard app-shaped verification measured `APP_XLSX_PREPARE_EDIT elapsed_ms=3,335.57 allocated_bytes=191,361,448`, with save still on `source_patch` at `APP_XLSX_SAVE elapsed_ms=3,031.94 allocated_bytes=387,892,856`. The slice is a modest allocation improvement because the remaining prepare cost is dominated by snapshotting and patch-validation fingerprinting.
- Completed the ClosedXML load sanitizer hint path by carrying chart-ex package presence from the existing load package inspection and letting sanitizer requirements return directly from complete hints without opening a second ZIP archive. Partner Dashboard app-shaped repeats measured `APP_XLSX_OPEN elapsed_ms=14,036.94 allocated_bytes=782,900,224 load_package_metadata_ms=2,454.53 load_sheet_xml_layout_ms=2,767.44 load_closedxml_load_ms=7,593.52`, then `APP_XLSX_OPEN elapsed_ms=14,079.74 allocated_bytes=783,629,368 load_package_metadata_ms=2,458.92 load_sheet_xml_layout_ms=2,732.86 load_closedxml_load_ms=7,691.19`. First-edit prepare measured `3,345.80-3,348.08 ms / 191.2-191.3 MB`; save remained `source_patch`, with one normal run at `2,721.07 ms` and one OS-write noisy run at `4,084.63 ms`.
- Added a pruned worksheet XML layout loader: `sheetData` row/cell facts now stream through `XmlReader`, while non-cell worksheet-level metadata stays in a small `XDocument` for the existing drawing, conditional-formatting, validation, hyperlink, view, and native-metadata readers. The loader keeps the full-document fallback for parse failures and retains source-metadata preflight coverage for row/cell native metadata. Partner Dashboard app-shaped baseline before this slice measured `APP_XLSX_OPEN elapsed_ms=15,045.54 allocated_bytes=782,914,400 load_sheet_xml_layout_ms=2,611.40 load_sheet_xml_layout_allocated_bytes=305,902,144`; final verification measured `APP_XLSX_OPEN elapsed_ms=15,691.73 allocated_bytes=591,841,472 load_sheet_xml_layout_ms=1,439.31 load_sheet_xml_layout_allocated_bytes=115,537,312`, with `APP_XLSX_SAVE elapsed_ms=4,213.81 allocated_bytes=387,878,640 save_path=source_patch`. Total open elapsed remained noisy because `load_closedxml_load_ms` ranged above the earlier baseline during this run, but the targeted worksheet-layout phase dropped about `1.17s` and `190 MB`.
- Reduced the transient package handed to ClosedXML by stripping modern worksheet drawing/chart parts after FreeX has read charts, pictures, text boxes, and shapes natively from the original package. The sanitizer keeps legacy VML drawings/media so ClosedXML can still load comment shapes. `FreeX.AppIoBench` now splits `load_closedxml_load` into `load_closedxml_package_prep` and `load_closedxml_workbook_open`. Partner Dashboard app-shaped repeats measured `APP_XLSX_OPEN elapsed_ms=5,861.59 allocated_bytes=629,666,640 load_closedxml_load_ms=3,201.02 load_closedxml_package_prep_ms=801.75 load_closedxml_workbook_open_ms=2,398.61`, then `APP_XLSX_OPEN elapsed_ms=5,347.59 allocated_bytes=629,662,608 load_closedxml_load_ms=2,641.30 load_closedxml_package_prep_ms=613.19 load_closedxml_workbook_open_ms=2,027.56`. Final pre-merge post-hint sample measured `APP_XLSX_OPEN elapsed_ms=5,783.63`, `APP_XLSX_PREPARE_EDIT elapsed_ms=1,587.64`, and `APP_XLSX_SAVE elapsed_ms=1,063.00 save_path=source_patch`; benchmark tests reported generated style-heavy load `mean_ms=478.91 allocated_bytes=138,755,792` and Partner Dashboard adapter load `elapsed_ms=4,034.72 allocated_bytes=609,783,944`. After merging latest `main`, cold-process samples were slower (`APP_XLSX_OPEN elapsed_ms=16,211.45` then `14,514.82`, save `3,934.57` then `3,622.61`), but a direct probe confirmed the transient package has modern drawings/charts stripped (`drawings=0`, `charts=0`) and repeated ClosedXML opens over the same sanitized bytes fell from `7,151.97 ms` to `1,113.53 ms`. The next open slice should treat first-open ClosedXML/JIT and package-prep cold cost as separate from the now-fixed drawing feed.
- Avoided a second full transient-package copy when style-only stripping already produced an owned package for ClosedXML. The load sanitizer now defaults to copy-on-write but can mutate the owned style-stripped package in place before ClosedXML opens it, preserving the original source package used for save/round-trip fidelity. The Partner Dashboard external stage lane measured `STYLE_STRIPPED_SANITIZED_CLOSEDXML_LOAD elapsed_ms=1,983.59 allocated_bytes=383,480,656` and `FREEX_FULL_LOAD elapsed_ms=4,532.75 allocated_bytes=566,613,800`; a noisy app-shaped sample measured `load_closedxml_package_prep_ms=1,656.54 allocated_bytes=147,363,368` with `save_path=source_patch`.
- Added `FreeX.AppIoBench --open-only --repeat <N>` so cold and same-process warm XLSX opens can be measured without edit/save noise. Partner Dashboard repeat-three measurement showed `APP_XLSX_OPEN` falling from `14,125.94 ms` to `5,093.17 ms` to `3,703.58 ms`; the split confirms first-open `load_closedxml_workbook_open_ms=7,334.60` drops to `1,133.01` and then `1,013.05`, while warm `load_closedxml_package_prep_ms` remains `~0.84-1.25s`.
- Added `FreeX.AppIoBench --prewarm xlsx` to test whether synthetic in-process XLSX warmups move the first real open. A tiny warmup cost `5,444.72 ms / 94,593,504 bytes` and still opened Partner Dashboard at `15,174.09 ms` with `load_closedxml_workbook_open_ms=7,462.64`; a larger style-heavy warmup cost `7,307.82 ms / 169,323,056 bytes` and still opened at `15,498.72 ms` with `load_closedxml_workbook_open_ms=7,780.39`. This does not justify an app startup/idle prewarm hook; the cold cost appears tied to opening the real workbook shape rather than generic XLSX JIT alone.
- Fused the ClosedXML transient package writer so style-only worksheet stripping and sanitizer package edits happen in one `ZipArchiveMode.Create` pass when both are needed. The fused path keeps the original package untouched for save fidelity, removes modern drawing/chart/pivot/relationship/content-type/worksheet XML entries in the same write, and falls back to the existing two-step helpers if the fused write fails. Partner Dashboard external stage verification measured `STYLE_STRIPPED_SANITIZED_CLOSEDXML_LOAD elapsed_ms=1,326.27 allocated_bytes=346,474,800` and `FREEX_FULL_LOAD elapsed_ms=1,942.01 allocated_bytes=529,546,496`. App-shaped repeat-three open measured `APP_XLSX_OPEN elapsed_ms=5,362.08`, `2,709.85`, and `1,810.32`, with `load_closedxml_package_prep_ms=554.96`, `357.26`, and `278.00`; edited save remained `source_patch` at `APP_XLSX_SAVE elapsed_ms=1,748.16-1,801.85`.

Primary measurement commands:

```powershell
$env:FREEX_RUN_BENCHMARK_TESTS='1'
dotnet test tests\FreeX.Core.IO.Tests\FreeX.Core.IO.Tests.csproj --configuration Release --filter "FullyQualifiedName~XlsxFileAdapterPerformanceTests" --logger "console;verbosity=normal"
```

For real workbook checks, set `FREEX_IO_BENCHMARK_PATHS` to one or more local `.xlsx` files and rerun the external-load benchmark lane:

```powershell
$env:FREEX_RUN_BENCHMARK_TESTS='1'
$env:FREEX_IO_BENCHMARK_PATHS='E:\path\to\large.xlsx'
dotnet test tests\FreeX.Core.IO.Tests\FreeX.Core.IO.Tests.csproj --configuration Release --filter "FullyQualifiedName~XlsxFileAdapterPerformanceTests.Benchmark_LoadExternalWorkbook_ReportsTiming" --logger "console;verbosity=normal"
```

For external load stages and edited-save timing:

```powershell
$env:FREEX_RUN_BENCHMARK_TESTS='1'
$env:FREEX_IO_BENCHMARK_PATHS='E:\path\to\large.xlsx'
dotnet test tests\FreeX.Core.IO.Tests\FreeX.Core.IO.Tests.csproj --configuration Release --filter "FullyQualifiedName~XlsxFileAdapterPerformanceTests.Benchmark_LoadExternalWorkbookStages_ReportsTiming|FullyQualifiedName~XlsxFileAdapterPerformanceTests.Benchmark_SaveExternalLoadedWorkbook_ReportsTiming" --logger "console;verbosity=detailed"
```

For app-shaped XLSX open/edit/save timing through `OpenWorkbookLoader` and `SaveWorkbookWriter`:

```powershell
$log = Join-Path $env:TEMP 'freex-app-io-bench-partner.log'
.\tools\FreeX.AppIoBench\bin\Release\net10.0-windows10.0.19041.0\FreeX.AppIoBench.exe --path 'E:\path\to\large.xlsx' --edit existing-literal --recalc real --log $log
Get-Content -LiteralPath $log | Select-String 'APP_XLSX_OPEN|APP_XLSX_SAVE'

$repeatLog = Join-Path $env:TEMP ('freex-appiobench-repeat-' + [Guid]::NewGuid().ToString('N') + '.log')
.\tools\FreeX.AppIoBench\bin\Release\net10.0-windows10.0.19041.0\FreeX.AppIoBench.exe --path 'E:\path\to\large.xlsx' --open-only --repeat 3 --recalc real --log $repeatLog
Get-Content -LiteralPath $repeatLog | Select-String 'APP_XLSX_OPEN'

$prewarmLog = Join-Path $env:TEMP ('freex-appiobench-prewarm-' + [Guid]::NewGuid().ToString('N') + '.log')
.\tools\FreeX.AppIoBench\bin\Release\net10.0-windows10.0.19041.0\FreeX.AppIoBench.exe --path 'E:\path\to\large.xlsx' --prewarm xlsx --open-only --recalc real --log $prewarmLog
Get-Content -LiteralPath $prewarmLog | Select-String 'APP_XLSX_PREWARM|APP_XLSX_OPEN'
```

Candidate work, in priority order:

1. Continue reducing XLSX open time in the measured hot phases now left after drawing-strip and fused package prep: `package_metadata`, warm `closedxml_workbook_open`, and any remaining worksheet-layout tail. Prefer slices that remove duplicate package scans or feed less XML to ClosedXML while preserving native-reader fidelity.
2. Trim any remaining `closedxml_package_prep` tail only when profiling shows a concrete duplicate worksheet/content-type/relationship rewrite; the fused transient writer has removed the largest known duplicate ZIP pass.
3. Keep delayed app idle prewarm rejected unless a future real-workbook-shaped prewarm proves a clear first-open win. Current synthetic `--prewarm xlsx` measurements add `5.4-7.3s` of idle work without improving the Partner Dashboard first-open timing.
4. Continue trimming first-edit baseline preparation only when profiles identify a concrete remaining duplicate. Compact load-time facts and sorted patch-cell arrays are already in place; deeper wins likely need narrower model-state fingerprints or command-scoped mutation facts, which should stay conservative because direct model mutations must not be captured as baseline state.
5. Finish the remaining ClosedXML package-prep cleanups for workbooks that actually need sanitization: pass worksheet path sets into conditional-format/dynamic-filter cleanup, combine those worksheet rewrites into one pass, and evaluate native CF/DV readers before stripping more worksheet XML from the transient ClosedXML package.
6. Prototype a guarded ClosedXML bypass for value/formula materialization only after the smaller native-reader prerequisites land. The drawing-strip slice shows big wins are available by shrinking ClosedXML input, but a full bypass still carries higher semantic risk.
7. Re-run the app-shaped edited-save benchmark whenever new real-world slow workbooks appear and use `save_path`/`save_reason` to choose the next save slice. The current Partner Dashboard one-cell edit reaches `source_patch`; remaining XLSX save work should be driven by new fallback reasons rather than broad guard relaxation.
8. Extend package patching beyond scalar value/formula-text/new-cell/clear-cell/existing-style/dimension/merge/internal-hyperlink/legacy-note/structured-table-safe/chart-safe/pivot-safe edits. Highest-value next slices, in practical order, are shared/array formulas and richer worksheet metadata. New styles still need explicit `styles.xml` mutation and should remain fallback until that is guarded and benchmarked.
9. Split XLSX fallback saves further into ClosedXML workbook emission, modeled feature writers, native metadata merge/post-processing, package validation, and stream/file replacement when diagnostics show an unavoidable full save, then batch ZIP/XML post-processing where the timing shows repeated archive update passes dominate.
10. Continue folding feature-inspection/reporting into trusted package facts where the load path already inspected the same package entries; keep public `XlsxFeatureInspector.Inspect` behavior intact for standalone callers and corpus warnings.
11. Compare `.fxl`, CSV, and SpreadsheetML after XLSX has a broader deterministic fixture set, but keep XLSX first.

## Remaining Cross-App Backlog

These items remain valid but are behind XLSX IO for now:

- **App.UI drawing-object repainting:** consider bitmap or frozen-layer caching for stable drawing-object layers. This needs careful invalidation and visual-regression coverage.
- **App.Host viewport batching:** batch vertical/horizontal scroll reveal updates so a single offscreen navigation does not trigger duplicate viewport refresh work. Split panes and side-by-side sync make this medium risk.
- **Core.Commands dense undo paths:** row/column shift undo can likely move existing cells back instead of re-materializing all shifted `CellStateSnapshot` instances. This is correctness-heavy because undo history and formula restore ordering are involved.
- **Core.Calc conditional-format formulas:** cache more static formula results for viewport conditional formatting. Volatile functions, current-cell-sensitive formulas, structured references, and relative references need strict guards.
- **Core.IO non-XLSX formats:** SpreadsheetML and CSV have measurable load/save work remaining, but XLSX is the priority because it carries the compatibility burden.

## Guardrails

- Keep every optimization benchmark-backed.
- Prefer deterministic fixtures before private workbook measurements.
- Preserve Excel openability, package-health, and corpus-retention tests.
- Reject changes that only reduce allocation while worsening wall-clock time on the priority scenario.
- Integrate small, verified slices rather than accumulating a large IO branch.
