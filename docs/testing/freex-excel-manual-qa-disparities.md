# FreeX / Microsoft Excel Manual QA Disparities

**Run:** `tools/ux-parity-runs/20260830-122624`  
**Protocol:** Excel and FreeX remain open concurrently on separate monitors. Each starts from a byte-identical Excel-authored corpus copy; every action is performed in Excel first, then FreeX.

| ID | Severity | Surface | Expected Excel behavior | Observed FreeX behavior | Evidence | Owner / status |
|---|---|---|---|---|---|---|
| UX-QA-SETUP-001 | P1 | Bootstrap runtime | The current Release host launches with the installed .NET runtime. | Resolved: child-process runtime environment now removes developer-local `DOTNET_ROOT*` values, allowing the installed host runtime to launch FreeX. | Initial `freex-startup-attempt1.stderr.log`, then `20260830-131520/ux-parity-run.json`. | Resolved in the UX runner. |
| UX-QA-SETUP-002 | P2 | Concurrent corpus opening | Both applications can start from equivalent workbook state. | Resolved: runner creates separately writable, byte-identical Excel-authored copies. | User-captured `Open Error`; `20260830-131520/ux-parity-run.json` records matching SHA-256 and `copiesByteIdentical=true`. | Resolved in the UX runner. |
| UX-QA-SETUP-003 | P1 | Dynamic-array workbook open/save | A normal Excel `FILTER` dynamic-array formula should not be misrepresented as an excluded linked-data workflow. | Resolved in `a7061e1891`: the inspector distinguishes service-connected `_linkedentity` values from formula-created `_array`, `_localImage`, `_webImage`, and `_error` rich values. A real Excel-authored `FILTER` probe then opened in FreeX without an unsupported-XLSX dialog. | `dynamic-array-probe.xlsx` versus `plain-formula-probe.xlsx` in `20260830-131114`; focused IO lane 144/144; UIA window `dynamic-array-probe - FreeX`. | Resolved; retain genuine linked-data warning coverage. |
| UX-QA-HARNESS-001 | P1 | Manual desktop evidence | Every injected desktop action must have exact foreground ownership and a nonblank matching window capture. | Resolved for the resumed session: both apps now provide foreground ownership and nonblank captures. The original 20260830 run remains invalid evidence; new guarded evidence begins in 20260831-093247. | `20260830-131520/excel-diagnostic.png`, `freex-diagnostic.png`; `20260831-093247/*-live.png` and grid captures. | Resolved; preserve foreground guard for every case. |
| UX-QA-ITER1-001 | P2 | Keyboard grid edit and save | From `UX Overview`, `Ctrl+PgDn`, `Ctrl+G`, `H12`, `Enter`, type `Keyboard parity 001`, `Enter`, `Ctrl+S` selects Grid Basics, writes `H12`, and saves without an excluded-feature prompt. | Content parity passed after accepting the historical warning. Both the clean core corpus and an actual dynamic-array probe now launch without that warning, but the full interaction must be rerun when foreground-guarded desktop capture is available. | `excel-after-keyboard-iteration.xlsx` and `freex-after-keyboard-confirmed-save.xlsx` both contain `H12=Keyboard parity 001`; package hashes differ as expected from application metadata. | Behavior repaired; visual/mouse rerun blocked by `UX-QA-HARNESS-001`. |
| UX-QA-ITER1-002 | P2 | Region AutoFilter popup | Excel's table-filter popup has an explicit transactional value list with `OK` and `Cancel`; it opens from the Region header after the same mouse route. | Resolved: the fresh live FreeX popup now exposes both `OK` and `Cancel` controls after the same route. | Initial evidence: `20260831-093247/excel-region-filter-menu-corrected.png`, `freex-region-filter-menu-corrected.png`; passing revalidation: `20260831-141009/excel-autofilter-revalidation.png`, `freex-autofilter-revalidation.png`. | Resolved by `608a5023e1`; paired UIA/visual revalidation passed. |
| UX-QA-ITER2-001 | P3 | Calculated date display | On the formula sheet, Excel displays `=DATE(2026,7,1)+7` as `7/8/2026` in the default cell presentation. | Resolved: FreeX now renders `7/8/2026` in the fresh live corpus. | Initial evidence: `20260831-093247/excel-formula-edit-mode.png`, `freex-formula-edit-mode.png`; passing revalidation: `20260831-140645/excel-formula-revalidation.png`, `freex-formula-revalidation.png`. | Resolved by `f99d7eb8f4`; focused tests and live paired revalidation passed. |
| UX-QA-ITER2-002 | P3 | Formula edit Name Box | While editing `B2` with F2, Excel displays `SUM` in its edit-state Name Box. | Resolved: FreeX now displays `SUM` while editing the same formula. | Initial evidence: `20260831-093247/excel-formula-edit-mode.png`, `freex-formula-edit-mode.png`; passing revalidation: `20260831-140645/excel-formula-revalidation.png`, `freex-formula-revalidation.png`. | Resolved by `f99d7eb8f4`; focused tests and live paired revalidation passed. |
| UX-QA-ITER3-001 | P1 | Excel-authored clustered-column chart placement | The `Charts` tab presents the embedded `Revenue by region` chart fully within the viewport. | Resolved: the chart is now fully visible with no left-edge clipping in the fresh live corpus. | Initial/failed evidence: `20260831-093247/*charts*`, `20260831-140432/*charts*`; passing revalidation: `20260831-142414/freex-charts-followup.png`. | Resolved by `3b001e4cd8`; focused UI test 9/9 and live revalidation passed. |
| UX-QA-ITER12-001 | P2 | Excel-authored clustered-column default styling | Excel renders the chart with its default bold dark title, Office default series palette, and automatic value axis `0-7000` with `1000` major units. | Resolved: FreeX retains the resolved placement/title and now matches the palette and automatic axis range/major units in the fresh Excel-authored corpus. | Initial `20260831-142414/freex-charts-followup.png`, `20260831-144227/excel-charts-title-revalidation.png`, `freex-charts-title-revalidation.png`; passing revalidation `20260831-151324/excel-chart-palette-axis-revalidation.png`, `freex-chart-palette-axis-revalidation.png`. | Resolved by `f01babdb2c`; worker Release UI build passed and focused chart suite passed 241/241, followed by live paired revalidation. |
| UX-QA-ITER4-001 | P1 | File backstage keyboard entry | `Alt+F` opens a complete keyboard-visible File surface with navigation and command categories. | Resolved: FreeX now opens a coherent navigable backstage with `New`, `Open`, `Info`, `Save`, `Save As`, `Print`, `Export`, `Close`, `Account`, and `Options`. | Initial `20260831-093247/excel-file-keyboard.png`, `freex-file-keyboard.png`; passing behavioral revalidation `20260831-152505/excel-backstage-revalidation.png`, `freex-backstage-revalidation.png` with paired UIA command inventory. | Resolved for keyboard routing and command availability; visual parity remains `UX-QA-ITER22-001`. |
| UX-QA-ITER5-001 | P2 | Ribbon Alt keytips | `Alt` presents a legible, non-overlapping map of keyboard access keys across the Office chrome, tabs, and Home commands. | Resolved: fresh FreeX captures show the top-level bare-Alt labels for File, Home, Insert, Draw, Page Layout, Formulas, Data, Review, View, and Help, matching Excel's visible ribbon-tab letter map. | Initial `20260831-095401/excel-ribbon-alt-keytips.png`, `freex-ribbon-alt-keytips.png`; prior `20260831-144227/excel-keytips-revalidation.png`, `freex-keytips-revalidation.png`; passing live revalidation `20260831-160540/excel-keytips-postrepair.png`, `freex-keytips-postrepair.png`. | Resolved by `272105fa7d`; focused production-tab tests passed 6/6 and the rebuilt-host paired visual revalidation passed. |
| UX-QA-ITER16-001 | P2 | Find Next visual workflow | In Excel, `Ctrl+F`, `smoke`, Enter selects `UX Overview!B5` while the compact Find dialog stays open; result enumeration remains an explicit Find All action. | Resolved: FreeX selects `B5`, retains a compact dialog, and only surfaces status `Match 1 of 1` until the user explicitly invokes Find All. | Initial `20260831-143234/excel-find-next.png`, `freex-find-next.png`; passing revalidation `20260831-150243/excel-find-next-revalidation.png`, `freex-find-next-revalidation.png`, plus UIA confirms no result rows. | Resolved by `277da7202c`; worker focused host tests passed 23/23 and live paired revalidation passed. |
| UX-QA-ITER20-001 | P2 | F6 chrome-region focus order | Starting from the worksheet, repeated `F6` should cycle the same major focusable chrome regions in the same order as Excel. | Excel UIA sequence is sheet tab `UX Overview` -> Name Box `A1` -> status `Cell Mode Ready` -> ribbon tab `Home`; rebuilt FreeX remains `freex-workbook - FreeX` window -> Name Box -> window -> Home. The first repair is integrated but not validated: the combined focused run has 12 passes and 4 failures because `StatusZoomOutButton` never receives focus (`StatusZoomControls_TabNavigationCyclesWithinZoomCluster`, `F6StatusBarFocus_StartsAtZoomOutAndTabsThroughFooterControls`, `EscapeFromStatusBarFocus_ReturnsFocusToWorksheet`, `StatusZoomPercent_IsKeyboardReachableAfterZoomButtons`). | Initial `20260831-150622/excel-f6-cycle.png`, `freex-f6-cycle.png`; post-integration focused result `tests/FreeX.App.Host.Tests/TestResults/ux-f6-backstage-focused.trx`; paired UIA revalidation `20260831-160540/f6-postrepair-focus.json` and per-step captures. | Reassigned to a no-desktop status-bar focus regression repair; retain the Excel-oriented shell order, make the four focused tests pass, then rerun the exact live sequence. |
| UX-QA-ITER22-001 | P2 | File backstage visual composition | The File surface uses Office's light backstage composition, restrained left navigation, high-density recent/template content, and matching typography/spacing. | The layout repair moves FreeX much closer to the light Office backstage composition and restrained left navigation, but it remains visually different: FreeX has only a blank-workbook tile, a more compact/left-weighted main content area, a different recent-list density, and still exposes repeated long local paths. | Initial `20260831-152505/excel-backstage-revalidation.png`, `freex-backstage-revalidation.png`; repaired visual recheck `20260831-160540/excel-backstage-postrepair.png`, `freex-backstage-postrepair.png`. | Keep open for visual-density/template/recent-list refinement; retain the resolved routing coverage. |
| UX-QA-ITER23-001 | P1 | Sheet-tab context menu mouse route | A right-click directly on the `Grid Basics` sheet tab opens Excel's sheet-tab menu with Insert, Delete, Rename, Move or Copy, Tab Color, Hide/Unhide, and related commands. | Resolved: the identical UIA-bounded FreeX right-click now opens a populated sheet-tab command menu with Insert Sheet, Delete Sheet, Rename, Move or Copy, Protect Sheet, Tab Color, Hide/Unhide, Select All Sheets, and Group Sheets. | Initial `20260831-152848/excel-sheet-tab-context.png`, `freex-sheet-tab-context.png`; passing revalidation `20260831-161752/excel-sheet-tab-context-postrepair.png`, `freex-sheet-tab-context-postrepair.png`. | Resolved by `bf650eefc3`; focused sheet-tab tests passed 30/30 and paired mouse revalidation passed. Follow-up composition differences are `UX-QA-ITER26-001`. |
| UX-QA-ITER24-001 | P2 | Cell keyboard context-menu command surface | With `A1` selected, `Shift+F10` should present a command surface matching the equivalent Excel cell context menu, including direct clipboard variants and independently discoverable grid commands. | Resolved for the implemented FreeX surface: the live menu now exposes direct Paste, Paste Special, Insert Copied Cells, Insert, Delete, Clear Contents, and sort commands rather than hiding them in broad category submenus. Unsupported Excel-only commands are not claimed as implemented. | Initial `20260831-154129/excel-shift-f10.png`, `freex-shift-f10.png`; passing revalidation `20260831-161752/excel-shift-f10-postrepair.png`, `freex-shift-f10-postrepair.png`. | Resolved by `4e441d82d5`; worker planner and keyboard tests passed 90/90, followed by paired `Shift+F10` revalidation. |
| UX-QA-ITER26-001 | P3 | Sheet-tab context-menu composition | Once the sheet-tab menu opens, the command names, availability, ordering, grouping, and visual density should match the analogous Excel menu as closely as the implemented FreeX feature set permits. | FreeX now opens the functional menu, but differs in composition: it exposes `Settings...` and `Group Sheets` where Excel exposes `Link to this Sheet`, uses `Insert Sheet`/`Delete Sheet` labels, and has different spacing, iconography, and disabled-state treatment. | `20260831-161752/excel-sheet-tab-context-postrepair.png`, `freex-sheet-tab-context-postrepair.png`. | Assign a no-desktop menu-composition refinement task. Preserve the resolved right-click route; compare only supported commands and avoid adding placeholder actions for unsupported Excel features. |
