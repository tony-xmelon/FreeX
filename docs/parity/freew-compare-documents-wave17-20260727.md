# FreeW Compare Documents Wave 17

Branch: `codex/freew-compare-documents-wave17-20260727`  
Base: `origin/main` at `1333a1953`

## Diagnosis

`compare-documents.tab-more` was a genuine product layout mismatch. The WPF authority renders the expanded More section without a frame and across the dialog width. Avalonia was using the default Fluent `Expander`, which added a gray framed panel constrained to roughly 204 px and reflowed the settings content. Both harnesses captured the intended expanded state at 560x720, so this was not a capture-state or target-size error.

## Metrics

| Scenario | Before changed pixels | After changed pixels | Before mean delta | After mean delta |
| --- | ---: | ---: | ---: | ---: |
| `compare-documents.initial` | 5.60% | 4.84% | 4.50 | 4.24 |
| `compare-documents.populated` | 5.63% | 4.87% | 4.50 | 4.24 |
| `compare-documents.tab-more` | 27.40% | 7.25% | 10.49 | 7.46 |
| `compare-documents.validation-error` | 5.85% | 5.09% | 4.53 | 4.27 |

The remaining visual delta is the shared Avalonia/WPF control rendering difference. The generated report also retains the existing focus/default/cancel semantic differences from the paired capture adapters; they were not hidden or reclassified.

Evidence: `docs/parity/freew-compare-documents-wave17-20260727/`, generated by `FreeW.DialogVisualHarness` with four paired WPF/Avalonia captures and freshness metadata. The canonical report was regenerated from the established WPF/Avalonia artifact manifests; its diff contains only these four Compare Documents rows plus generator hashes/counts. Canonical freshness/check passes, and the three pre-existing Avalonia cell-shading adapter gaps remain outside this slice.
