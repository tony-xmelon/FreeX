# Avalonia parity Wave112 integration (2026-08-02)

## Scope

- FreeX: align `dialog.Options.EaseOfAccess` with the WPF authority using fresh equal-size Linux Docker and WPF captures.
- FreeW: close the anomalous initial Icon Picker SVG viewport delta without weakening the visual classification.
- FreeP: replace the admitted `continuousBlockProcess` generic SmartArt path with dedicated shared live geometry.

## Delivered

- FreeX now shares explicit Ease of Access layout metrics, preserves WPF-equivalent disabled checkbox contrast only on that page, and captures a deterministic 744x521 pair. The triage score improved from `0.097761` to `0.012617`; normalized pixel difference is `1.40%`.
- FreeW now applies the WPF authority's 32px-to-38px SVG viewport scale to all 61 initial Icon Picker thumbnails. The initial row improved from `13.54% / 19.68` changed pixels/mean delta to `12.10% / 15.36`; populated and validation remain visual passes. The canonical 295-row report was route-merged, changing only `icon-picker.initial`.
- FreeP Continuous Block Process now emits dedicated shared rounded blocks and ordered connectors, regenerates stable drawing-cache roles, and round-trips through schema-valid PPTX. WPF and Avalonia remain thin consumers of the same compositor plan and admission was not broadened.

## Focused verification

- FreeX: 4 Avalonia tests and 2 WPF tests passed; WPF build and Linux Docker/Xvfb capture passed; generated dialog evidence check passed.
- FreeW: 2 Avalonia Icon Picker tests, 12 WPF icon catalog tests, and 13 source parity tests passed; 3/3 states captured on each host.
- FreeP: 357 presentation tests, 2 WPF host routing tests, and 2 Avalonia routing/gallery tests passed; focused Release build completed with zero warnings or errors.

## Remaining

- FreeX's next paired visual outlier is `dialog.Options.AddIns`; remaining Ease of Access residuals are platform text/control rasterization.
- FreeW Icon Picker initial remains an honest visual mismatch due SVG parser/rasterizer stroke and antialiasing differences; Options AutoCorrect and Page Setup remain larger non-legal dialog candidates.
- FreeP still needs broader dedicated SmartArt layouts, richer native effect/style semantics, and PowerPoint-authoritative visual baselines.
