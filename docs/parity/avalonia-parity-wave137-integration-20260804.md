# Avalonia/WPF parity wave 137 integration

Date: 2026-08-04

## Scope

Wave 137 advanced one bounded parity residual in each application:

- FreeX: aligned the Avalonia Select Data Source dialog with the committed WPF authority.
- FreeW: aligned the Avalonia Export backstage scrollbar with the existing route-local WPF treatment.
- FreeP: admitted one audited imported `process1` SmartArt node-and-connector cache through the shared compositor path.

## Results

- FreeX `dialog.SelectDataSource` triage improved from `0.075491` to `0.028971`; sample delta improved from `0.036019` to `0.025350`, luma delta from `0.005998` to `0.001976`, and non-background delta from `0.033371` to `0.001542`.
- FreeW `backstage-export.open` changed pixels improved from `45,833` to `40,578`, changed ratio from `0.1364077381` to `0.1207678571`, and mean channel delta from `10.8529494` to `10.3659375`.
- FreeP remains at zero actionable WPF/Avalonia command-profile gaps and, after the final `main` sync, reports `647/647` shared-profile commands with `109` workflow-evidence rows.
- Dialog evidence remains fully paired at `94/94`, with zero nonblank failures, zero expected-size mismatches, and zero high-delta candidates at the `0.4` triage threshold.

## Verification

- FreeX focused integration tests: `2/2` passed.
- FreeW Avalonia backstage tests: `40/40` passed.
- FreeW presentation backstage tests: `75/75` passed.
- FreeP focused worker tests: `530/530` passed before the final negative-test amendment; the amended SmartArt reader lane passed `279/279`.
- FreeP command inventory and dialog visual-evidence freshness checks passed.
- The cross-app dashboard was regenerated from the integrated evidence.

## Evidence boundaries

The committed WPF dialog frame remains the FreeX authority because this Windows host intermittently produces blank WPF `RenderTargetBitmap` captures; blank frames were rejected and never promoted. FreeW Export remains classified as a genuine visual mismatch after the measurable improvement. FreeP `process1` admission is intentionally limited to the exact audited five-node chain and nine-role cache; malformed hierarchy, missing roles, changed geometry or text order, effects, pictures, and richer caches retain fallback behavior.
