# Avalonia parity Wave113 integration (2026-08-02)

## Scope

- FreeX: align `dialog.Options.AddIns` with the WPF authority using shared layout metrics and fresh equal-size WPF/Linux captures.
- FreeW: align the Options AutoCorrect tab, state policy, action row, and semantic evidence without renderer-specific test artifacts.
- FreeP: replace the admitted `segmentedProcess` generic SmartArt path with dedicated shared live geometry.

## Delivered

- FreeX AddIns now uses shared section metrics, an enabled owned deferred-action route, and deterministic 744x521 captures. Its triage score improved from `0.097051` to `0.012806`, an 86.8% reduction.
- FreeW AutoCorrect now shares WPF-authority dialog metrics and state policy across both hosts. The paired row improved from 16.633% changed pixels / 11.841746 mean delta to 11.876% / 10.057894, and the action-button-order mismatch is closed. Both harnesses now exclude invisible or unnamed framework buttons instead of adding product controls solely for evidence matching; all 287 unrelated report rows remained unchanged.
- FreeP Segmented Process now emits dedicated shared vertical rectangular segments and ordered down-arrow relationships, regenerates stable drawing-cache roles, and round-trips through schema-valid PPTX. WPF and Avalonia remain thin consumers of the same shared plan, with no admission expansion.

## Focused verification

- FreeX: 1 Avalonia AddIns test and 1 WPF AddIns test passed in the integration worktree; fresh WPF and Linux Docker/Xvfb captures and the generated evidence check passed on the worker branch.
- FreeW: 17 shared planner tests, 11 WPF host/semantic tests, and 6 Avalonia Options tests passed in the integration worktree; the worker captured 8 WPF and 6 Avalonia Options scenarios.
- FreeP: 5 presentation, 2 WPF host, and 1 Avalonia host Segmented Process tests passed in the integration worktree; the worker's broader SmartArt lanes passed 390 shared, 73 WPF, 3 Avalonia, and 11 inventory checks.

## Remaining

- FreeX's next paired visual outlier is `dialog.FindReplace` at `0.094953`; AddIns residuals are native text/control rasterization.
- FreeW Options AutoCorrect remains a genuine visual mismatch from native renderer differences; Page Setup and other high-delta paired dialogs remain candidates for further alignment.
- FreeP still needs broader dedicated SmartArt families, richer native effect/style semantics, and PowerPoint-authoritative visual baselines.
