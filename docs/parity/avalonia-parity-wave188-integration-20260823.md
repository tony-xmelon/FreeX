# Avalonia Parity Wave 188 Integration

Date: 2026-08-23

Wave 188 completes one accepted bounded slice per application and brings the
cumulative processed app-slice count to **564**. Generated command inventories
continue to report zero actionable Avalonia-missing commands across FreeX,
FreeW, and FreeP. This wave advances production Linux workflow evidence,
WPF-authoritative dialog registration, and Office-backed rendering fidelity.

## FreeX

Numeric AutoFilter physical evidence advances from Wave187's startup blocker
to **2/2 passed** production Linux/Docker/X11 workflows. The runner now waits
for the requested workbook title before calibration, recognizes both numeric
result ids, and uses a minimal numeric OOXML fixture. A valid B1 run then
exposed a production input bug: the rendered inner border consumed Linux/X11
pointer input before `Button.Click`.

The product keeps `Button.Click` for keyboard and accessibility activation and
adds a left-button-only glyph bridge. Greater Than 50 retained `75,100`, saved
`operator=greaterThan,value=50`, and reopened identically. Equals 50 retained
`50`, saved the legal empty equality operator with `value=50`, and reopened
identically. Right-click remains excluded by a focused source/behavior guard.

Focused verification: Core.IO `R38/R65/R98` passed **20/20**; Avalonia/source
tests passed **17/17**; presentation AutoFilter tests passed **35/35**; the
Avalonia Release build passed with zero warnings and errors.

## FreeW

The accepted Font dialog correction aligns Avalonia label line height,
right-lane registration, bottom tab cadence, and action-row cadence with the
WPF authority. Fresh same-source three-state captures improve from **61,396**
to **58,705** aggregate changed pixels, a **4.383%** relative reduction. WPF
and Avalonia painted bounds now both measure **421 x 321**. All three rows
remain genuine visual mismatches because native text/control rasterization is
still different.

Focused verification: Font and Legal Notices Avalonia controls passed
**17/17**; shared planner tests passed **31/31**; both capture harnesses built
cleanly; canonical refresh remains 512 scenarios, 141 genuine mismatches, 80
passes, and 70 classified Avalonia extensions. The attempted Legal Notices
tail was measured, rejected as raster noise, and fully reverted.

## FreeP

The imported `IncreasingCircleProcess` slide-09 SmartArt correction improves
WPF/Office from **1.6516%** to **0.9662%** and WPF/Avalonia from **1.6609%** to
**1.6009%**; Avalonia/Office remains **1.6879%**. The WPF raster compensation
is not keyed by text. The compositor emits an explicit semantic flag only for
an unsupported imported IncreasingCircle cache with the exact richer
12-shape topology: three ellipses, three chords, six rectangles, and four
text-bearing rectangles. Ordinary authored phase-label controls stay on the
generic renderer path.

Focused verification: presentation tests passed **5,389/5,389**, Avalonia
rendering tests passed **285/285**, and RenderCompare built cleanly. Decks 06,
14, and 26 plus neighboring slides 08 and 10 remained stable. The canonical
53-slide corpus summary remains unchanged because only the direct target and
controls were rerendered consistently.

## Integration Gates

- Cross-app dashboard generation, check mode, schema validation, and evidence
  aggregation guards passed.
- Repository preflight, serialized full Release build, and the default non-UI
  lane are run from the integrated branch before push; final results are
  recorded here before commit.

## Remaining

- FreeX: date, color, mixed-type, multi-column, and criteria clear/reapply
  physical AutoFilter workflows.
- FreeW: Font native text/control rasterization, Legal Notices glyph/template
  rasterization, then pagination, drawing/object, chart, table, and WordArt.
- FreeP: the Avalonia slide-09 imported SmartArt text raster residual or a new
  Surface3D topology, while preserving direct Office and renderer-pair metrics.
