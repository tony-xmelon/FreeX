# FreeP run baseline PowerPoint comparison - 2026-07-20

## Scope

`23-run-baseline.pptx` is a two-shape COM fixture generated by
`tools/FreeP.GenerateFixtures`. The upper centered text line contains authored
DrawingML baseline tokens `-25000` for the subscript `2` and `30000` for the
superscript `2`. The lower shape contains the same-font plain-text control
`H2O + x2`. The fixture bytes are retained at
`tools/FreeP.RenderCompare/corpus/23-run-baseline.pptx`; its SHA-256 is
`532895289200A146B02C2FC5CE314957591A17079CF51BBDABA06DC01BB81757`.

## Fresh COM evidence

PowerPoint COM exported 1/1 slide at 1280x720. The current Release renderers
were compared with that export:

| Pair | Whole-slide mean channel diff |
| --- | ---: |
| WPF vs PowerPoint | 0.0328% |
| Avalonia vs PowerPoint | 0.0872% |

The baseline text ROI `(350,130)-(700,220)` measured:

| Host | ROI mean channel diff |
| --- | ---: |
| WPF | 0.7468% |
| Avalonia | 0.8734% |

The plain control ROI `(350,370)-(700,460)` measured `0.2188%` for WPF and
`1.3969%` for Avalonia. The baseline route therefore improves the authored
subscript/superscript raster without changing the plain control path. The
shared `BaselineRunFontScale=0.67` is retained for both hosts. A fresh
Avalonia-only `0.80` probe was rejected: baseline ROI worsened to `1.1065%`,
whole-slide error to `0.0917%`, and Avalonia-vs-PowerPoint to `0.0856%`; the
control remained byte/ROI stable.

## Verification

- PowerPoint COM export: 1/1 slide.
- WPF focused host lane: 40/40 compile-first and 40/40 no-build.
- Avalonia focused lane: 39/39 compile-first and 39/39 no-build.
- Presentation planner lane: 40/40 compile-first and 40/40 no-build.
