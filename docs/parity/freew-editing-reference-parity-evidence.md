# FreeW editing and reference parity evidence

Generated by `tools/Generate-FreeWEditingReferenceParityEvidence.ps1` from explicit semantic and lifecycle contracts validated against current WPF/Avalonia source tokens.

## Summary

- Workflows: 7
- Behavior aligned: 7
- Functional gaps: 0
- Remaining visual/human evidence items: 2

## Workflows

| Workflow | Surfaces / modality | Lifecycle | Validation and result | Status |
| --- | --- | --- | --- | --- |
| `references.notes` | WPF: Bottom Notes pane plus insertion and numbering-options dialogs; Avalonia: Bottom Notes pane plus insertion and numbering-options dialogs; modeless pane; owner-centered modal prompts | Insertion applies only after nonempty OK; the pane toggles, selects, loads rich content, and keeps Apply/Delete explicit; note navigation wraps; cancel/close do not mutate. | Shared numbering planner validates all six footnote/endnote format, start, and restart values. Insertion, pane edit/delete, and six-value options apply through document commands; Avalonia edit/delete/options are one-step undoable and invalidate layout. | behavior-aligned |
| `insert.date-time` | WPF: Date and Time dialog with five culture-derived choices and automatic-update option; Avalonia: Date and Time dialog with five culture-derived choices and automatic-update option; owner-centered modal | One DateTime and CurrentCulture are captured before opening; OK inserts the selected static value or DATE/TIME complex field; cancel, Escape, and close are no-ops. | Choice index is constrained to the five generated formats; DATE versus TIME follows the selected format. Static text inserts the captured display value; automatic update inserts the matching complex-field instruction with the same cached result. | behavior-aligned |
| `layout.text-to-table` | WPF: Delimiter choice dialog over selected paragraphs; Avalonia: Delimiter choice dialog over selected paragraphs; owner-centered modal | Tab/comma/semicolon choice converts all selected paragraphs; cancel, Escape, and close do not mutate. | TableTextConversionDialogPlanner supplies supported delimiters and default choice; ragged rows are preserved by the shared conversion model. One replace-blocks command creates the table, places the caret in the first cell, and undoes in one step. | behavior-aligned |
| `references.table-of-authorities` | WPF: Table of Authorities options dialog; Avalonia: Table of Authorities options dialog; owner-centered modal | OK applies planner-built options; cancel, Escape, and close do not insert; refresh rebuilds the existing region with WPF-equivalent defaults. | Shared planner owns category, passim, formatting, and tab-leader choices. Shared region planner inserts or refreshes the authority paragraphs and page references through the editor command bus. | behavior-aligned |
| `home.multilevel-list` | WPF: Define Multilevel List dialog; Avalonia: Define Multilevel List dialog; owner-centered modal | Current level formats seed the dialog; invalid OK remains open and focuses the failed start value; valid OK applies; cancel, Escape, and close do not mutate. | Shared planner owns number-format choices, current-state projection, positive start validation, and result construction. The selected starts and number formats update the document multilevel definition through one undoable command. | behavior-aligned |
| `insert.bookmark-manager` | WPF: Bookmark Manager dialog with Go To, Delete, and refreshed selection; Avalonia: Bookmark Manager dialog with Go To, Delete, and refreshed selection; owner-centered modal | The list is refreshed from document order; Go To moves to the selected bookmark; Delete refreshes and retains a valid selection; close does not mutate. | Actions require a selected bookmark; the existing freew.bookmark Add/Go dialog remains separate. Go To moves the caret; Avalonia Delete uses an undoable bookmark-removal command and refreshes the manager list. | behavior-aligned |
| `review.thesaurus` | WPF: Modeless right Thesaurus pane; Avalonia: Modeless right Thesaurus pane; modeless toggle pane | Ribbon or Shift+F7 toggles the pane; showing and caret refresh rebuild lookup results; Replace edits the caret word and refreshes; Copy is asynchronous in Avalonia. | Shared presentation planner normalizes the caret word and supplies sense/action rows and empty-result status. Replace routes through the editor command bus; Copy writes only to the platform clipboard and does not mutate the document. | behavior-aligned |

## Exact Remaining Gaps

- **paired-app-surface-captures** (visual-evidence; `references.notes`, `insert.date-time`, `layout.text-to-table`, `references.table-of-authorities`, `home.multilevel-list`, `insert.bookmark-manager`, `review.thesaurus`): Capture matching WPF/Avalonia opened, populated, validation, and cancel states at 96 DPI; compare dimensions, focus, enabled/default controls, pane docking, overflow, and nonblank pixels.
- **native-clipboard-focus-smoke** (human-smoke; `review.thesaurus`): Confirm asynchronous Copy reaches the native clipboard and focus returns after Replace on a packaged desktop run; platform clipboard behavior is not suitable for pixel equivalence.

No functional or semantic gap remains in these seven workflows; the listed work is visual/native-environment evidence only.
