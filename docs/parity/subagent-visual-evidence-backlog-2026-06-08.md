# Ribbon Visual Evidence Backlog - 2026-06-08

This checklist maps completed parity subagent notes to the remaining live FreeX-vs-Excel capture work. It is scoped to command families where product or source-test fixes already exist, but the linked notes do not yet provide foreground screenshot/video evidence against Excel.

## Capture Rules

- Capture each item as paired FreeX and Excel evidence at the same zoom/window size where practical.
- Prefer screenshots for static ribbon/dialog state and short video only when the command changes visible UI state, focus, or external launch behavior.
- Keep deferred/excluded features out of the pass unless the capture is meant to show an honest absence or disabled/excluded affordance.

## Checklist

| Priority | Command family | Completed doc / coverage source | Existing fix or test coverage | Missing evidence | Exact UI state / selection to capture |
|---|---|---|---|---|---|
| P0 | Insert > Charts advanced families and picker | `docs/parity/subagent-insert-charts-2026-06-07.md`; `docs/parity/menu-toolbar.md` chart rows | Insert-ribbon whitelist, chart picker categories, advanced galleries, and collapsed Charts keytip routing now include authorable 3D/chartEx families while Map stays hidden/deferred. | Side-by-side FreeX-vs-Excel screenshots proving the visible Insert tab and Insert Chart dialog expose comparable advanced chart choices; one short FreeX mutation clip for an advanced choice. | Use a worksheet with headers and numeric data selected, e.g. `A1:D6`. Capture `Insert` tab with the Charts group expanded/visible; collapsed Charts overflow opened if window width forces it; `Insert Chart` dialog on `All Charts` with `Waterfall`, `Funnel`, `Histogram/Pareto`, `Treemap`, and `Box and Whisker` categories visible; then select one advanced chart such as `Waterfall` and capture the inserted chart selected on the sheet. |
| P0 | PivotTable Analyze contextual Show commands | `docs/parity/subagent-contextual-table-pivot-ribbons-2026-06-07.md`; `docs/parity/shortcuts.md` keytip note | PivotTable Analyze > Show now exposes `+/- Buttons` and `Field Headers`, routes through `ConfigurePivotTableOptionsCommand`, and has catalog/source tests for names, keytips, handlers, and group order. | Side-by-side screenshot evidence for contextual tab visibility and Show group layout; short FreeX clip or before/after pair proving each toggle updates the modeled pivot display. | Open a workbook with an existing PivotTable. Select a cell inside the PivotTable so contextual tabs appear. Capture `PivotTable Analyze` with the Show group containing `Field List`, `+/- Buttons`, and `Field Headers`; capture the same state after toggling `+/- Buttons` off, and after toggling `Field Headers` off, with the pivot still selected. |
| P1 | Data > Get Data / Refresh All import-refresh surface | `docs/parity/subagent-data-import-refresh-2026-06-08.md`; `docs/parity/menu-toolbar.md` Data rows | `Get Data` tooltip/help now names local `.csv`, `.txt`, `.tsv`, `.tab`, and SpreadsheetML `.xml`; `Get Data` and `Refresh All` have stable automation IDs/help text; `Refresh All` routing and QAT metadata are source-tested; fake `Queries & Connections` and `From Text/CSV` placeholders are intentionally absent. | Side-by-side FreeX-vs-Excel screenshots showing the honest FreeX surface versus Excel's broader Get & Transform surface; one FreeX screenshot/clip of local import entry. | Use a normal worksheet selection such as `A1`. Capture the `Data` tab with `Get Data`, the `Queries & Connections` group label, and `Refresh All` visible. Open/hover or inspect the `Get Data` affordance so its local-format help is visible where possible. Capture the file-open/import prompt after invoking `Get Data`. Capture `Refresh All` enabled on the Data tab and, if practical, on QAT customization/search as evidence of command availability. |
| P1 | Help tab, About, diagnostics, and Legal Notices | `docs/parity/subagent-help-about-legal-2026-06-07.md`; `docs/parity/command-surface.md` Help rows | Help commands now have stable UIA names/help text; `Copy Diagnostics` and `About FreeX` expose automation metadata; Legal Notices generated tabs and Close button have stable UIA metadata and source tests. | Side-by-side FreeX-vs-Excel screenshots for Help/Legal surface where Excel has an equivalent About/license experience; FreeX live foreground proof for owned message box focus return and legal-notice visual layout; browser-launch behavior needs guarded live validation. | Capture the `Help` tab with `Help`, `Send Feedback`, `Copy Diagnostics`, `Check for Updates`, `About FreeX`, and `Legal Notices` visible. Invoke `About FreeX` and capture the owned message box, then close it and capture focus returned to FreeX. Invoke `Legal Notices` and capture tabs for project/license/privacy/third-party notices with the `Close` button visible. For browser-launch commands, record a short guarded clip showing the command attempts the expected external URL or shows the owned failure warning when blocked. |
| P2 | Shortcut-backed visual menus that already have live host coverage but lack Excel side-by-side stills | `docs/parity/shortcuts.md` rows for `Alt+Down`, `Ctrl+Q`, `Shift+F10`, and ribbon keytips | Live host/source coverage exists for keyboard routing, focus, nested menu keytips, Quick Analysis previews, worksheet context menus, and command-scope keytips. | Side-by-side screenshots against Excel for visual parity of the menu/chrome appearance, not additional routing proof. | For `Alt+Down`, select a filtered table header with mixed text/number/date values and open the AutoFilter menu with a nested filter-family submenu visible. For `Ctrl+Q`, select `A1:D6` numeric data and capture Quick Analysis with Formatting and Charts previews, including one in-grid preview. For `Shift+F10`, capture active-cell context menu, row-selection context menu, and object-aware context menu with a selected picture/shape. For keytips, press `Alt` on Home and then a nested menu path such as Conditional Formatting or Borders so top-level and submenu keytips are visible. |

## Explicit Non-Targets For This Evidence Pass

- `Map` chart authoring and true 3D surface mesh rendering remain deferred/hidden and should not be counted as missing screenshots for the Insert Charts slice.
- Table-connected slicer authoring and separate PivotTable expand-field/collapse-field commands remain functional gaps, not visual-evidence gaps.
- Excel-only database/web/Power Query connectors, recent sources, existing connections, and connection properties are excluded from the Data import capture except as contrast in the Excel reference screenshot.
- Full Excel threaded-conversation UI, cloud Help/support/training, and Microsoft account/cloud surfaces are excluded or partial by design; captures should focus on the FreeX-owned local command surface.
