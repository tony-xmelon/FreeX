# FreeP PowerPoint Parity Status - Updated 2026-07-04

Status owner: FreeP parity orchestration. This report supersedes the 2026-06-27 pre-dedup FreeP snapshot. It is still a worker handoff and implementation plan, not a claim of full Microsoft PowerPoint parity.

Current integration context: `origin/main` now includes the post-dedup FreeP implementation wave through initial chart workbook formula/range retention evidence, SmartArt live-layout breadth, animation behavior-delay import, Backstage print planner parity, text autofit line-spacing anchoring, chart data-table cell-bounds planning, chart data-table fill fidelity, shared print output-option planning, shared animation-pane playback controls, deterministic animation playback session planning, the first visible Avalonia print-options pane, Avalonia table-cell rich subrange formatting metadata routing, source-part-based multi-chart workbook/externalData retention, deterministic alt-text suggestion planning, shared hyperlink accessibility issue detection, shared print preview planning, OMML limit layout parity, richer authored chart workbook formula-retention corpus evidence, real `19-chart-labels.pptx` chart-data semantic edit retention, scatter/bubble authored formula-range retention for series names, X/Y values, and bubble-size refs, shared slideshow ink overlay render planning, shared table-cell initial selection planning, native print package execution descriptors/materialization, shared table rich-edit result selection planning, media caption-track metadata-aware accessibility planning, comment/reply mention descriptors, chart `c:varyColors` IO/planning, chart axis `c:numFmt` preservation/planning, OMML radical hidden-degree handling, caption-track package emission, shared caption-byte transcript planning, shared Split/Random Bars/Grow-Shrink slideshow execution planning, shared active table-cell paragraph-alignment routing, shared active table-cell bullet/numbering/indent/outdent routing, shared merged/split-table accessibility diagnostics, and shared list-punctuation proofing diagnostics. The newest FreeP parity commits include `3bbd88428` / `d04df404b` for host-ready native print package execution descriptors and materialization, `7b0be7ed5` / `304c9be24` for shared table-cell rich-edit selection/result planning consumed by WPF and Avalonia, `1b3aeb55a` / `fd2d0867b` for modeled media caption/subtitle tracks that suppress false missing-caption diagnostics in shared WPF/Avalonia accessibility panes, `1c72eb593` for renderer-neutral comment/reply mention descriptors, `4bd341794` for chart `c:varyColors` preservation/render-planning, `42751cd41` for chart axis `c:numFmt` preservation and renderer-neutral axis-label format metadata, `acb5c8a93` for OMML `m:radPr/m:degHide` CT_OnOff semantics, `98a6830d0` for emitting modeled caption tracks back into PPTX packages, `30cde103f` for shared WebVTT/SRT transcript planning from authored caption bytes, `658a4ebdb` for shared advanced slideshow animation effect planning consumed by WPF/Avalonia, `fbe294651` for shared active table-cell paragraph alignment consumed by WPF/Avalonia, `60d00a731` for shared active table-cell bullet/indent/outdent formatting consumed by WPF/Avalonia command routes, `b13234502` for shared merged/split-table accessibility diagnostics, `645f26e87` for shared active table-cell numbering toggle routing, and `c3120c7b` for shared comma/semicolon/colon list-punctuation proofing diagnostics. This report remains a worker handoff and implementation plan, not a claim of full Microsoft PowerPoint parity.

## Current Evidence

- Primary repo root: `C:\Users\anton\OneDrive\Documents\FreeX\FreeX`.
- This report was refreshed from the current FreeP parity integration wave after syncing `main` with `origin/main`.
- `AGENTS.md` still requires isolated worktrees, no feature edits in `main`, frequent sync, subagents for independent work, and concrete validation before integration.
- `PowerPoint.Application` COM is not registered on this machine, so PowerPoint-authoritative visual baselines cannot be generated locally.
- 2026-07-04 worker branch `codex/freep-stacked-vertical-text-20260704` adds shared `TextLayoutPlanner` stacked upright glyph placement for `eaVert`, `wordArtVert`, and `wordArtVertRtl`, with WPF/Avalonia renderers consuming the shared placements. `Vertical` and `Vertical270` remain on the existing whole-block rotation route. `wordArtVertRtl` keeps logical single-column top-to-bottom ordering pending a PowerPoint COM visual baseline.
- 2026-07-04 worker branch `codex/freep-omml-box-borderbox-20260704` adds shared OMML `m:box` and `m:borderBox` parsing/layout/render-planning so transparent boxes preserve nested math and border boxes emit neutral visible-side line ops consumed by WPF and Avalonia. Follow-up branch `codex/freep-omml-borderbox-strikes-20260704` carries `m:borderBoxPr` horizontal, vertical, BLTR, and TLBR strike variants into the same shared line plan. PowerPoint COM visual baselines remain deferred.
- 2026-07-13 worker branch `codex/freep-omml-structure-depth-20260713` strengthens the shared `m:borderBox` geometry contract for hidden horizontal edges plus TLBR strike endpoints. WPF and Avalonia consume the same neutral `MathBoxRenderPlanner` line coordinates; PowerPoint COM math baselines remain deferred.
- 2026-07-04 worker branch `codex/freep-omml-spre-20260704` adds shared OMML `m:sPre` pre-subscript/pre-superscript parsing and layout through `MathNode.PreSubSup` and the neutral `MathBox` tree. WPF and Avalonia should consume it through existing math recursion; PowerPoint COM math baselines and broader equation alignment semantics remain deferred.
- 2026-07-04 worker branch `codex/freep-omml-phant-20260704` adds shared OMML `m:phant` parsing/layout through `MathNode.Phantom`, including hidden metric reservation plus zero width/ascent/descent flags. Follow-up branch `codex/freep-omml-phant-transp-20260704` consumes `m:transp` for bounded binary/relation transparent phantom spacing in shared row layout; full spacing-class fidelity and PowerPoint COM visual baselines remain deferred.
- 2026-07-07 worker branch `codex/freep-omml-transp-spacing-20260707` expands shared `m:phantPr/m:transp` spacing consumption to bounded single-token binary, relation, large-operator, and punctuation classes in `MathLayoutEngine`; ambiguous multi-character and structured phantom bases intentionally remain packed until PowerPoint-authoritative spacing evidence is available.
- 2026-07-04 worker branch `codex/freep-omml-eqarr-alignment-20260704` adds shared OMML `m:eqArr/m:e` alignment-point semantics by parsing direct `m:aln` markers as invisible per-row metadata and aligning marked rows in `MathLayoutEngine`. WPF and Avalonia continue to consume the neutral `MathBox` tree; PowerPoint COM math visual baselines remain deferred.
- 2026-07-13 worker branch `codex/freep-omml-eqarrpr-20260713` adds shared OMML `m:eqArrPr` base-justification and row-spacing metadata for equation arrays, reusing the shared matrix-style stacked-row gap/baseline resolver while preserving row ordering and direct `m:aln` alignment points. WPF and Avalonia continue to consume the neutral `MathBox` tree; PowerPoint COM math visual baselines remain deferred.
- 2026-07-04 worker branch `codex/freep-omml-skewed-fraction-20260704` adds shared OMML skewed-fraction layout for `m:fPr/m:type="skw"`: `MathLayoutEngine` now offsets numerator/denominator around a renderer-neutral diagonal `MathBox.Line` consumed by WPF and Avalonia through the existing math draw-op path. PowerPoint COM math baselines remain deferred.
- 2026-07-13 worker branch `codex/freep-omml-layout-evidence-20260713` adds shared OMML `m:groupChr` position-aware default glyph handling, so missing `m:chr` resolves to U+23DE for top/default group characters and U+23DF for bottom group characters. WPF and Avalonia consume the same neutral glyph plan; PowerPoint COM math baselines remain deferred.
- 2026-07-13 worker branch `codex/freep-omml-math-alphabet-20260713` adds shared OMML `m:rPr/m:scr` math alphabet handling for roman, script, fraktur, double-struck, sans-serif, and monospace runs. `MathLayoutEngine` maps supported ASCII letters/digits to deterministic mathematical alphanumeric Unicode glyphs before the existing neutral draw plan reaches WPF/Avalonia, while unsupported characters and default/unknown styles remain unchanged. PowerPoint COM math baselines remain deferred.
- 2026-07-04 worker branch `codex/freep-accessibility-chart-title-20260704` adds shared accessibility diagnostics for chart shapes without chart titles. WPF and Avalonia consume the same selectable warning row with an `Add Chart Title` action label and no invented mutation command; broader accessibility-checker parity and PowerPoint-authoritative review baselines remain deferred.
- 2026-07-04 worker branch `codex/freep-accessibility-unclear-hyperlink-20260704` adds shared accessibility diagnostics for unclear hyperlink display text such as generic phrases and raw URLs. WPF and Avalonia checker panes consume the same selectable Hyperlink rows with the shared Edit Hyperlink action; richer hyperlink authoring and full accessibility-checker parity remain deferred.
- 2026-07-04 worker branch `codex/freep-review-comment-reply-resolution-20260704` adds modern comment author identity persistence: `SlideComment` and `SlideCommentReply` now retain modern comment/reply ids plus `p188:author` id/userId/providerId metadata, shared comment descriptors expose those fields to WPF/Avalonia consumers, PPTX read/write preserves imported PowerPoint author/comment/reply ids, and clone/mutation paths keep the metadata through edit/reply/resolve/reopen workflows. Rich mention UI, cloud/coauthor presence, and PowerPoint-authoritative review baselines remain deferred.
- 2026-07-04 commit `2c084d016` adds shared custom-show launch planning: `SlideShowCustomShowPlanner` now builds launch plans and route choices for named custom shows plus All Slides fallback, with WPF/Avalonia slideshow windows consuming the shared route descriptors. Full custom-show authoring and PowerPoint visual baselines remain deferred.
- 2026-07-04 commit `02cbe1b79` adds shared presenter session-summary planning: `SlideShowPresenterSessionSummaryPlanner` combines recording-session segment/artifact evidence and ink stroke/laser overlay state into a renderer-neutral summary consumed by WPF and Avalonia slideshow windows. Real capture backends, captured-media persistence, subtitles, and PowerPoint baselines remain deferred.
- 2026-07-04 branch `codex/freep-proofing-punctuation-spacing-20260704` adds shared proofing diagnostics for whitespace before punctuation and missing spaces after sentence punctuation, with correction suggestions flowing through the existing WPF/Avalonia proofing mutation route and guards for decimals, URLs, web addresses, and email tokens.
- 2026-07-04 commit `c3120c7b` adds shared proofing diagnostics for missing spaces after comma, semicolon, and colon list punctuation. The same proofing correction route now suggests punctuation-plus-space replacements while guarding numbers, times, URLs, email/mailto tokens, existing whitespace, and adjacent punctuation.
- 2026-07-05 commit `4e39cd88` adds shared proofing diagnostics for extra interior horizontal spaces. Runs of two or more spaces/tabs bounded by non-whitespace text now suggest a single-space correction through the existing WPF/Avalonia proofing mutation route while preserving line-start indentation, trailing whitespace, and newline boundaries.
- 2026-07-13 branch `codex/freep-proofing-terminal-punctuation-20260713` adds shared proofing diagnostics for repeated terminal `!`/`?` punctuation such as `Hello!!`, `Why??`, and overlong mixed runs such as `Wait?!?`. Suggested single-mark replacements flow through the existing proofing mutation route, while ellipses, decimal/version-like numbers, URL/web/email/mailto tokens, and common two-character mixed emphasis such as `?!`/`!?` remain guarded.
- 2026-07-04 branch `codex/freep-custom-show-authoring-20260704` adds shared custom-show authoring helpers: side-effect-free authoring summaries, non-empty/unique name validation, create/rename/delete, slide membership/order updates, missing-slide normalization, id allocation, and thin WPF/Avalonia host adapters over the shared planner. Rich visible authoring UI and PowerPoint visual baselines remain deferred.
- 2026-07-04 branch `codex/freep-native-print-handoff-20260704` adds shared native print handoff planning over `PresentationPrintOutputPackagePlan`: printable PDF packages now expose package-ready handoff metadata, suggested temporary file names, page/content/layout/range/options summaries, no-slide and host-deferred statuses, and WPF/Avalonia consume host-deferred capabilities until a real native print adapter is wired. Real OS printer dialog execution and PowerPoint print visual baselines remain deferred.
- 2026-07-04 branch `codex/freep-omml-nary-20260704` adds shared OMML n-ary hidden-limit handling: `m:naryPr/m:subHide` and `m:supHide` now suppress lower/upper limits before the neutral `MathNode.Nary` tree reaches layout/render planning, with WPF/Avalonia continuing to consume the shared `MathBoxRenderPlanner` output. PowerPoint COM math baselines remain deferred.
- 2026-07-04 branch `codex/freep-video-export-handoff-20260704` adds shared video export handoff planning over the existing video frame package path: package-ready status, host encoder capabilities, deferred MP4/narration/camera/media capture status, and WPF/Avalonia host-deferred state now come from shared code. Real MP4 encoding and PowerPoint video export baselines remain deferred.
- 2026-07-05 branch `codex/freep-presenter-persistence-depth-20260705` adds shared presenter ink persistence route metadata: retained ink generated during a named custom-show slideshow now records the source `Slide.Id` and custom-show name in the generated ink part, with WPF and Avalonia slideshow windows passing the same playback route into the shared planner. Authored PowerPoint ink fixtures, real device capture, captured media persistence, and PowerPoint COM baselines remain deferred.
- 2026-07-04 branch `codex/freep-missing-slide-title-a11y-20260704` adds a shared accessibility checker diagnostic for non-decorative video media without closed captions/subtitles, projected as selectable Media rows with a read-only Select Media action label through existing WPF/Avalonia checker routes. Follow-up caption-track metadata modeling now suppresses that diagnostic for captioned media; caption authoring/emission and PowerPoint-authoritative accessibility baselines remain deferred.
- 2026-07-04 branch `codex/freep-native-print-execution-20260704` deepens the shared native print package path: `PresentationPrintOutputPackageExecutor` now validates host-ready PDF bytes, builds an execution descriptor with suggested document/job names, and materializes printable packages atomically for WPF/Avalonia host handoff. Actual OS print dialog execution remains deferred.
- 2026-07-04 branch `codex/freep-table-rich-editor-fidelity-20260704` adds shared table-cell rich-edit result planning. `TableCellEditPlanner` now preserves/clamps active selections, exposes selected run ranges, and returns post-format rich text plans consumed by the Avalonia overlay while WPF preserves native `RichTextBox` selection around formatting commands.
- 2026-07-04 branch `codex/freep-media-caption-track-20260704` adds media caption/subtitle track metadata to the shared model, reads relationship-backed caption metadata from PPTX slide parts, and makes the shared video-caption accessibility diagnostic ignore captioned media in both WPF and Avalonia panes. Follow-up writer work now emits modeled caption relationships/parts when bytes or external targets are available; follow-up transcript work parses authored WebVTT/SRT caption bytes into a renderer-neutral plan consumed by WPF and Avalonia review/accessibility state. Real audio transcription remains deferred.
- 2026-07-04 commit `658a4ebdb` adds shared slideshow playback planning for imported Split, Random Bars, Grow, and Shrink animation presets, carrying neutral effect kinds, direction, timing, and peak-scale metadata into thin WPF/Avalonia effect helpers. The effects are deterministic approximations pending PowerPoint visual baselines and exact multi-region/random behavior.
- 2026-07-04 commit `fbe294651` adds a shared active table-cell paragraph-alignment route: `TableCellEditPlanner` builds undoable alignment plans with result selection/rich-edit metadata, `EditingSession` exposes the route, and WPF/Avalonia commands/adapters consume it without duplicating paragraph policy.
- 2026-07-04 commit `60d00a731` adds shared active table-cell bullet toggle plus indent/outdent routing. `TableCellEditPlanner` builds undoable mutation plans for selected cell paragraphs, `EditingSession` exposes the route, WPF registers the shared command IDs, and Avalonia maps overlay selection/result plans through thin table-cell edit adapters. PowerPoint bullet galleries, numbering presets, image bullets, and richer visible ribbon controls remain deferred.
- 2026-07-04 commit `645f26e87` adds a shared active table-cell numbering toggle. It uses the same `TableCellEditPlanner`/`EditingSession` route as bullets and indent, defaults to PowerPoint-style Arabic-period auto numbering with `startAt=1`, and is consumed through thin WPF/Avalonia command and overlay adapters. Later preset work covers bounded Roman/alpha variants; visible galleries and image bullets remain deferred.
- 2026-07-04 commit `47b5d2c5` adds shared active table-cell list preset descriptors for disc bullets, Arabic numbering, upper/lower Roman numbering, and upper/lower alpha numbering. WPF and Avalonia route contextual bullet/numbering command values through the same shared `TableCellEditPlanner` mutation path while preserving the existing toggle behavior when no preset is supplied. Visible PowerPoint-style list galleries and image bullets remain deferred.
- 2026-07-04 commit `b13234502` adds shared accessibility diagnostics for merged or split table cells. `PresentationReviewWorkflowPlanner` now exposes a `Review Table Structure` action label for merged/split table rows, with coverage for row-span and vertical-merge cells and WPF/Avalonia continuing to consume the same checker row plan through thin panes.
- 2026-07-04 commit `adfd69967` adds shared low-quality alt-text accessibility diagnostics for filename-like descriptions and duplicate non-decorative object alt text. WPF and Avalonia consume the same selectable Alt text rows through existing checker and alt-text pane routes; generated AI alt text and broader accessibility-checker parity remain deferred.
- The repo-wide parity scope at `docs/parity/2026-06-27-avalonia-wpf-parity-scope.md` has already been refreshed for the large dedup wave.
- Current `origin/main` adds focused shared text-autofit, chart data-table, print-output, animation-pane playback, Avalonia print-pane, table-cell rich subrange formatting and initial-selection planning, chart workbook source-retention, alt-text suggestion, hyperlink accessibility, print-preview planning, OMML limit layout, richer chart workbook formula-retention evidence, real chart-labels semantic-edit retention evidence, and slideshow ink overlay render planning: `TextLayoutPlanner` anchors runtime-normal-autofit text using line-spacing-adjusted height, `ChartRenderPlanner` emits renderer-neutral chart data-table cell bounds consumed by WPF and Avalonia clipping paths, `ChartDataTableSettings.BackgroundFill` plus shared `ChartFillPlan` carry PPTX `c:dTable`/`c:spPr` fills into both renderers, `ChartDataTableSettings.BorderOutline` and `TextStyle` carry solid border color/width/dash plus font size/color/bold/italic/family through package IO, shared primitive planning, and WPF/Avalonia pen/text consumption, shared export/print planners describe package/output-option choices before hosts render thin UI, shared animation playback controls and deterministic session plans now drive WPF/Avalonia panes, Avalonia now projects the shared print backstage/options plan visibly, the Avalonia table-cell edit adapter carries rich subrange formatting metadata through overlay tests, `TableCellEditPlanner.BeginEdit` now returns shared `InCanvasEditorTextSelection` initial-selection plans, `ChartShape` now carries source chart-part metadata so PPTX read/write can preserve workbook/externalData relationships by source part in non-sequential multi-chart decks, `PresentationReviewWorkflowPlanner` derives deterministic alt-text suggestions from picture metadata and table row samples, the same shared review planner flags text-run hyperlinks without ScreenTips for WPF/Avalonia adapters, shared print preview planning describes full-slide, notes-page, and handout preview routes before WPF/Avalonia project them thinly, shared OMML parsing/layout now carries `m:limLow`/`m:limUpp` as `MathNode.Limit` through `MathLayoutEngine` and neutral `MathBox` recursion, `PptxPackageRetentionTests` now exercise a richer in-memory PowerPoint-style chart workbook fixture with formulas, defined names, calc-chain/source workbook sidecars, content types, relationships, and unrelated custom XML retention, plus `19-chart-labels.pptx` coverage that edits only the middle chart while preserving neighboring `Sheet1!` formula refs and workbook sidecars, and `SlideShowInkExecutionPlanner` emits renderer-neutral stroke and laser overlay primitives consumed by WPF and Avalonia through thin renderers.

## 2026-07-03 Integration Checkpoint

Thirty-seven additional shared-first FreeP parity slices are integrated on `origin/main`:

- `f72700ee7` / `98b32eb3a` fixes text autofit line-spacing anchoring in shared `TextLayoutPlanner`, so runtime `normalAutofit` line-spacing reduction affects middle and bottom anchored text consistently before WPF/Avalonia renderers consume the plan.
- `98f164068` / `a671cf224` strengthens chart data-table primitive planning by exposing raw `CellBounds` for each data-table cell, with WPF and Avalonia renderers clipping cell text/legend-key painting to the shared bounds instead of deriving per-renderer geometry.
- `33e8a8d00` / `1cf5782e8` preserves chart data-table background fills through shared `ChartDataTableSettings.BackgroundFill`, PPTX `c:dTable`/`c:spPr` fill import/export, shared `ChartFillPlan` primitives, and WPF/Avalonia renderer consumption. 2026-07-03 follow-ups confirmed and tightened the modeled data-table styling path beyond fills: border visibility toggles, solid border color/width/dash, gradient border strokes, and text size/color/bold/italic/font-family now have shared planner and WPF/Avalonia renderer-consumption evidence, with package round-trip evidence already covering modeled border/text styling and retained gradient outlines.
- `3ce4a627e` / `768665315` adds shared print output-option descriptors and package executor planning, with WPF Backstage consuming the shared choices through a thin UI route and evidence captured in `docs/parity/freep-print-output-options-2026-07-03.md`.
- `feea23558` / `5db6fba25` adds shared animation-pane playback control descriptors for Preview, Play From Selected, Play All, and disabled Stop state, with WPF and Avalonia panes rendering the same enablement/tooltips/intents.
- `574f01bf0` / `cf0afa1aa` adds deterministic shared animation playback session planning for Preview, Play From Selected, Play All, and Stop, with WPF and Avalonia projecting shared running/stopped state through thin adapters and tests. A 2026-07-03 execution-evidence pass verifies that both slideshow hosts also route click-step playback through `SlideShowPlaybackPlanner.PlanAnimationStep` into concrete timed Appear/Fade/FlyIn/Wipe/Zoom/Pulse/Spin/MotionPath helpers plus fallback emphasis flash routes. Advanced effect variants, full PowerPoint visual baselines, recording/timeline authoring, deeper persistence evidence, and COM baselines remain open.
- `9ca40f94f` / `7226cd032` / `d8f0e9f31` / `b061f2c17` adds the first visible Avalonia print-options pane over `PresentationPrintBackstagePlan`, including shared option summary lines, layout choices, and range choices while native printer execution remains deferred.
- `93fd36353` / `570b94da0` routes table-cell bold/italic/underline rich subrange formatting metadata through the Avalonia table-cell edit adapter and in-canvas overlay path, with `TableCellEditPlannerTests`, `SlideCanvasGeometryPlannerTests`, and `SlideCanvasAvaloniaTests` covering the planner/adapter/overlay route. Richer editor fidelity comparable to WPF `RichTextBox` remains open.
- `96e5660d2` / `8b05ba186` adds source chart-part metadata to `ChartShape` and preserves chart workbook/externalData relationships by source part, including non-sequential multi-chart decks. `PptxPackageRetentionTests` and `ChartDataCommandTests` cover the source-retention behavior, while PowerPoint-authored workbook parity, formulas, and range semantics remain open.
- `2b48bda91` / `90d22bbd6` enriches shared `PresentationReviewWorkflowPlanner` alt-text suggestion planning from available picture metadata, including image type, crop, effects, frame, table first-row/sample-row text, and chart-data metadata such as chart type, series names, category samples, and value counts when available. WPF and Avalonia remain thin consumers of the shared plan; this is deterministic suggestion planning, not full generated AI alt text, a full accessibility checker, or a PowerPoint COM baseline.
- `c7b082d45` / `7c42fab3d` detects text-run hyperlinks without ScreenTips as shared accessibility issues, including hyperlinks inside table cells, and exposes the same Insert Link action summary through WPF and Avalonia adapter tests. This closes the first text-hyperlink accessibility gap, not full hyperlink authoring UI or full accessibility-checker parity.
- `a0f3f9522` / `5184517b6` adds shared print preview planning metadata for full-slide, notes-page, and handout print routes, including notes overflow continuation pages. WPF Backstage and Avalonia print options project that shared state through thin host routes; video and COM visual parity remain deferred.
- `99604b28b` adds shared native print handoff planning over printable PDF packages, including package-ready metadata, suggested temporary file names, page/content/layout/range/options summaries, no-slide handling, and host-deferred capabilities consumed by WPF and Avalonia. Actual OS/native printer adapter execution and PowerPoint print visual baselines remain deferred.
- `3bbd88428` / `d04df404b` deepens that native print path into a host-ready package execution descriptor. Shared validation now checks package readiness, PDF header/EOF markers, byte counts, content type, default extension, suggested document/job names, and atomic materialization; WPF and Avalonia retain only thin host-deferred handoff state over the shared descriptor.
- `f8f53092e` adds shared OMML n-ary hidden-limit handling for `m:naryPr/m:subHide` and `m:supHide`, keeping hidden lower/upper limits out of the neutral renderer draw plan while preserving WPF/Avalonia thin math consumption.
- `7f71caf93` adds shared video export handoff planning over `PresentationVideoFramePackagePlan`, with WPF/Avalonia exposing host-deferred MP4 encoder/capture state rather than claiming real video encoding is available.
- `a0497b7f0` adds shared video-without-captions accessibility diagnostics for non-decorative video media, with WPF/Avalonia checker panes consuming selectable Media rows and no invented caption mutation route.
- `0af37cfb4` / `a21d7465c` adds OMML limit layout parity by parsing `m:limLow` and `m:limUpp` into shared `MathNode.Limit` nodes and laying them out in `MathLayoutEngine`. 2026-07-04 follow-ups add shared `m:eqArr` equation-array parsing/stacked row layout with direct `m:aln` row alignment points and shared `m:sPre` pre-subscript/pre-superscript parsing/layout through the same neutral `MathBox` tree. Existing WPF and Avalonia math renderers consume these constructs through recursion, so this improves shared equation coverage without claiming complete equation parity or PowerPoint COM visual-baseline parity.
- `7df6c06ad` / `1cc71cde4` expands the package semantic corpus with a richer in-memory PowerPoint-style chart workbook fixture containing formulas, defined names, calc-chain/source workbook sidecars, content types, relationships, and unrelated custom XML. A 2026-07-03 package-retention follow-up broadens that fixture to two authored chart series per workbook, quoted sheet names, global and local defined names, nontrivial workbook formulas, byte-for-byte preservation of the unedited authored workbook, and regenerated `ChartData` category/name/value ranges for both edited series. A real-corpus follow-up now covers `19-chart-labels.pptx`: editing chart 2 regenerates only `chartWorkbook2.xlsx`/`ChartData` ranges while charts 1 and 3 retain their authored `Sheet1!` formula refs, package relationships, and original workbook sidecars byte-for-byte. A 2026-07-04 bounded package fixture adds scatter and bubble coverage for authored series-name, X-value, Y-value, and bubble-size `c:f` refs, preserving the unedited scatter workbook byte-for-byte while regenerating only the edited bubble chart workbook/cache. Broader real PowerPoint-authored workbook formula/range parity and corpus depth remain open.
- `ab10efb7d` / `364b6dcc6` adds shared slideshow ink overlay render planning. `SlideShowInkExecutionPlanner` now emits renderer-neutral overlay primitives for committed and active stroke paths plus transient laser pointer dots, including slide-to-canvas projection, scaled stroke thickness/radius, round line caps/joins, opacity-aware ink color, and laser dot outline styling. WPF and Avalonia consume the same shared primitive plan through thin slideshow-window renderers. Narration/audio/video capture, full recording execution, subtitles/richer custom-show workflows, and PowerPoint COM visual baselines remain deferred.
- `61d42d48a` / `6295865a2` adds shared table-cell initial selection fidelity. `InCanvasEditorTextSelection` and `TableCellEditStartPlan.InitialSelection` now describe the editor selection that hosts should apply; `TableCellEditPlanner.BeginEdit` selects the full populated cell text and uses a collapsed start caret for empty cells, while WPF and Avalonia table-cell editors project the shared plan instead of hardcoded local `SelectAll`. This does not claim full rich editor parity.
- `7b0be7ed5` / `304c9be24` preserves table-cell rich-edit selection through shared post-format result plans. `TableCellEditPlanner` now returns effective selections, selected run ranges, and result rich-text plans; Avalonia consumes those plans directly in the in-canvas overlay, while WPF preserves the active `RichTextBox` selection around native formatting commands.
- `18ad714be` adds PowerPoint-authored chart `a:pattFill` preservation for series, data points, and markers. `PptxChartReader`/`PptxChartWriter` now retain `ShapeFill.Pattern`, `SlideCompositor` carries it as `ResolvedFill.PatternFill` through shared `ChartFillPlan` metadata, and WPF/Avalonia chart brush adapters consume the same pattern brush helpers used by normal shapes. A 2026-07-03 worker slice now carries gradient data-table border strokes through shared `ChartStrokePlan.Fill` into WPF/Avalonia gradient pens. Broader chart/workbook parity remains open.
- `0713f644f` / `8cd050fbb` consumes shared OMML `m:phantPr/m:transp` for transparent phantom spacing in `MathLayoutEngine`, adding deterministic row advance for simple binary and relation operator phantom runs while preserving hidden glyph behavior. The 2026-07-07 follow-up expands the same shared row layout to bounded single-token large-operator and punctuation spacing classes, with ambiguous multi-character and structured phantom bases left packed. WPF and Avalonia continue to consume the neutral math box tree through thin renderers; full OfficeMath spacing-table fidelity and PowerPoint COM math baselines remain open.
- `5f21f1fb5` / `e97836bf8` adds a shared chart-title accessibility diagnostic in `PresentationReviewWorkflowPlanner`. Chart shapes with blank titles now emit selectable warning rows categorized as Chart with an `Add Chart Title` action label, and WPF/Avalonia checker panes consume the shared row without renderer-local policy.
- `2c084d016` adds shared custom-show launch planning through `SlideShowCustomShowPlanner`, including disabled states for missing/empty custom shows and host route descriptors consumed by WPF/Avalonia slideshow windows.
- `916b4427f` adds real shared OMML skewed-fraction layout for `m:fPr/m:type="skw"` by emitting offset numerator/denominator boxes around a renderer-neutral diagonal line. WPF and Avalonia keep using the shared `MathBoxRenderPlanner` draw-op route.
- `1a9b0ffad` adds shared unclear-hyperlink accessibility diagnostics for generic linked text and raw URL display text, including table-cell hyperlinks, with WPF/Avalonia checker panes consuming the shared Hyperlink row and Edit Hyperlink action.
- `1b3aeb55a` / `fd2d0867b` models media caption/subtitle tracks and reads PowerPoint caption-track metadata from slide relationships, so shared accessibility planning no longer flags captioned video media as missing captions in WPF or Avalonia. Caption authoring and writer-side emission remain open.
- `1c72eb593` adds shared comment mention detail descriptors for root comments and replies, including offsets, labels, display text, and normalized identity keys, with WPF host-adapter evidence and a focused parity note at `docs/parity/freep-comment-mention-detail-2026-07-04.md`.
- `4bd341794` preserves chart `c:varyColors` decisions in shared `ChartShape.VaryColors`, round-trips them through chart IO, and uses the shared chart planner to vary fallback point colors without overriding explicit series or point fills. Real corpus coverage now checks `06-charts.pptx`, `18-chart-types.pptx`, and `19-chart-labels.pptx`.
- `acb5c8a93` honors OMML `m:radPr/m:degHide` as a CT_OnOff flag, so bare/on values suppress radical degree nodes before WPF/Avalonia consume the neutral math render plan while explicit off/false values preserve the degree.
- `98a6830d0` emits modeled caption/subtitle tracks back into PPTX packages as caption relationships, caption metadata under media `nvPr`, content-type defaults, and internal caption parts when bytes or preserved package bytes exist; external caption tracks remain relationship-backed without embedded bytes.
- `02cbe1b79` adds shared presenter session-summary planning for recording segments/media artifacts and ink/laser overlay state, with WPF/Avalonia slideshow windows consuming the same summary model.
- `9f4ddb418` adds shared proofing punctuation-spacing diagnostics and corrections for whitespace before punctuation and missing spaces after `.`, `!`, and `?`, while guarding decimals, URLs, web addresses, and email tokens.
- `c8fc45c59` adds shared custom-show authoring summaries and mutation helpers for create, rename, delete, and slide membership/order edits, with thin WPF/Avalonia host adapter methods.

Validation on the integrated `main` included focused shared planner, Avalonia renderer/headless, WPF host/chart/animation tests, authored chart data-table fill round-trip package coverage, chart `varyColors` and axis `c:numFmt` corpus/round-trip/planner coverage, animation playback session planner/adapter tests, review workflow planner/adapter/headless coverage for deterministic alt-text suggestions and hyperlink ScreenTip issues, print preview/package planner coverage, native print package execution descriptor/materialization coverage, media caption-track reader/accessibility/emission/transcript coverage, `OmmlParserTests`, `MathLayoutEngineTests`, `SlideCanvasMathBaselineTests`, `SlideShowInkExecutionPlannerTests`, WPF/Avalonia slideshow ink renderer source-guard tests, `TableCellEditPlannerTests`, `CanvasEditingTests`, `SlideCanvasGeometryPlannerTests`, `SlideCanvasAvaloniaTests`, `PptxPackageRetentionTests`, `ChartDataCommandTests`, and `dotnet build FreeP.slnx --configuration Release -m:1 /nr:false` on the current tip.

## 2026-07-02 Integration Checkpoint

The first post-dedup FreeP parity implementation wave is integrated on `origin/main` through FreeP commit `bdb54681c`, including `bc1d49b57` shared table-cell planning, `3451f903f` Avalonia table-cell overlay wiring, `322ada635` presenter recording/ink intents, `267cc43b2` shared image export execution, `12f5e7571` review workflow host adapters, `2cf014219` text placeholder inset inheritance, `e69302b84` shared chart axis-title planning, `c74c06aca` shared bullet slot placement, `cc66c5e20` image export UI flow, `cfc8af03` persistent shape alt text, `bb5187fab` shared animation pane/timeline planning, `f63758c53` shared handout layout planning, `c538fbf70` shared layout-picker choices, `07e38b06e` shared chart series-style planning, `d5e7ff7cf` shared handout PDF render planning, `4af5fd126` shared accessibility issue action summaries, `ab5caca20` shared tab-stop text planning, `b789ad9b2` shared chart gridline stroke planning, `3411d4649` deeper shared layout-picker evidence, `bc9d88092` alt-text title/decorative-object metadata planning, `001a43631` WPF/Avalonia layout-picker UI evidence, `b90683e08` chart workbook package sidecar preservation, `8d36dc695` WPF/Avalonia chart gridline stroke renderer consumption, `830cf0234` shared alt-text pane planning exposed by WPF/Avalonia, `ad5706162` Avalonia slide-pane reorder depth, `2fc59c214` PowerPoint-style layout gallery chrome evidence, `4bbdfb235` first Avalonia rendered comments-pane evidence, `108c31951` first visible WPF/Avalonia alt-text pane UI, `8ae7d5356` shared OMML math layout/rendering, `4939785ba` shared reading-order review planning, `2b5c43c1e` PPTX `presProps`/`viewProps`/`printerSettings` retention, `ecb0fe532` notes-page preview workflow evidence, `1edbc14c3` visible WPF/Avalonia reading-order panes, `a1cd9fcb4` OMML parser defaults, n-ary/sup layout, and WPF/Avalonia math baseline tests, `dde641589` top-level reading-order move execution, `282ff6947` shared modern comment Resolve/Reopen execution, `08ecdcee6` shared selected-comment pane state, `be40a8c56` visible Avalonia animation-pane UI, and `e7a5115a9` shared visible comment Delete execution. It closed the initial evidence and shared-planning gates that were blocking reliable WPF/Avalonia parity work:

- Added a generated FreeP WPF/Avalonia command inventory at `docs/parity/freep-command-parity-inventory.md` and `.json`.
- Added 21-deck PPTX corpus retention contract tests for package-preservation evidence, including a deterministic FreeP-authored comments/notes fixture.
- Fixed `tools/FreeP.RenderCompare --avalonia-compare` trust reporting so PowerPoint export failure is not reported as success, and added corpus summary output.
- Moved chart legend, axis-title, and series-style planning, text render route planning, and canvas drag reduction into shared FreeP presentation-layer planners consumed by WPF and Avalonia.
- Expanded the Avalonia ribbon profile from the initial small surface to 87 shared command ids. The generated command inventory currently reports 93 total commands, 87 shared, 0 actionable WPF/Avalonia missing commands, 0 explicit Avalonia gaps, 0 known deferred commands, 6 platform-only Avalonia shell commands, and 0 command-id aliases.
- Added shared presenter/slideshow state for current slide, next slide, notes text, start time, elapsed time, fullscreen intent, and monitor intent, with thin WPF and Avalonia slideshow-window adapters.
- Added a shared export planner for PDF/image/print descriptors, PDF dialog/picker plans, command IDs, and Backstage export plan; WPF and Avalonia now consume the same PDF and notes-page PDF export routes with thin native file-picker/write adapters.
- Added shared export/print depth plans for image and video descriptors, slide-range normalization, full-page slide printing, notes pages, PowerPoint-style handout slide-count options, and concrete handout page layout geometry.
- Added shared selected-slide PNG image export execution. `PresentationImageExportExecutor` owns slide-range normalization through the shared planner, deterministic output naming, atomic writes, empty-deck no-op behavior, and non-empty renderer output validation; WPF and Avalonia provide only thin render callback and folder-picker hooks.
- Added WPF and Avalonia image export UI flows over the shared executor. WPF exposes the shared Images export action through Backstage with a native folder picker, while Avalonia exposes the same command through the File ribbon group with a platform folder picker.
- Added shared PowerPoint-style handout layout planning for 1, 2, 3, 4, 6, and 9 slides per page, including page count, slide-index mapping, slide slots, and 3-up writing-line areas. WPF and Avalonia expose or refresh the shared plan as thin adapter state; actual handout PDF/print rendering and native print preview remain deferred.
- Added shared handout PDF render planning. `PresentationHandoutPdfExporter` now builds portable PDF draw-op pages from the handout layout plan, scales existing slide draw ops into thumbnail slots, adds thumbnail borders, and emits 3-up writing lines. It is shared production groundwork, not PowerPoint visual-baseline parity.
- Improved shared placeholder text fidelity by resolving text-frame insets through shape, layout placeholder, and master placeholder inheritance before WPF and Avalonia render plans consume them.
- Moved bullet slot placement into `TextLayoutPlanner`: bullets now carry shared text/font/color and X/Y placement from the paragraph plan, while WPF and Avalonia renderers consume `placement.Bullet` instead of recalculating hanging-indent math.
- Moved tab-stop layout into `TextLayoutPlanner`: explicit/default stops plus left, right, center, and decimal alignment now produce shared run segment placements consumed by WPF and Avalonia. A mixed-run decimal-tab guard keeps the decimal prefix measured with each segment's actual run style.
- Added shared chart gridline stroke planning and renderer consumption. Chart scene output now carries major gridline stroke intent through the shared planner boundary, and WPF/Avalonia renderers create gridline pens from `ChartMajorGridLinePrimitivePlan` instead of hard-coding the stroke locally.
- Added a shared transition command planner and exposed Transition commands through WPF and Avalonia thin adapters. WPF and Avalonia now route transition gallery, duration, advance-after, advance-on-click, and apply-to-all commands through the shared planner.
- Added a shared Design command planner and exposed theme plus slide-size commands through WPF and Avalonia thin adapters. Custom slide size is represented as a shared callback intent pending the full dialog.
- Added shared design/layout picker choices for slide layouts, theme variants, and slide size presets with WPF/Avalonia thin picker-state consumers.
- Deepened shared layout-picker evidence: layout choices now carry stable display order, master grouping labels, placeholder counts, current-selection state, and no-current-slide apply gating, with WPF and Avalonia adapter tests proving both hosts consume the same enriched picker plan.
- Added actual WPF and Avalonia layout-picker UI evidence: the Layout command now opens a visible in-app picker strip populated from the shared `PresentationLayoutPickerPlan`, disables the current layout choice, applies selected layouts through `TryApplyLayoutChoice`, refreshes the canvas/slide pane, and collapses after apply. This proves the picker is no longer only recorded adapter state; PowerPoint-style thumbnail gallery visuals and richer grouping remain deferred.
- Advanced the layout picker from a text-only strip to shared PowerPoint-style gallery evidence. `PresentationLayoutPickerPlan` now carries master-group sections, thumbnail placeholder slots, and explicit selected/disabled chrome; both WPF and Avalonia render grouped thumbnail tiles from the same plan and focused tests assert the shared grouping/chrome reaches each host.
- Added a shared Animations command planner and exposed effect, timing, ordering, and animation-pane commands through WPF and Avalonia thin adapters. Timing commands remain conservative when no selected value or selected-shape animation exists.
- Added shared animation pane/timeline planning for selected-slide animation rows, selected animation normalization, trigger/duration/delay display, timeline start/end calculations, reorder availability, and preview intent. WPF renders its pane rows from the shared plan, and Avalonia now opens a visible side pane over the same `AnimationPaneTimelinePlan`, including row order, shape/effect text, trigger, duration, delay, start/end timing, selected state, preview availability, and move earlier/later state. WPF and Avalonia now expose inline trigger/duration/delay pane controls over shared timing mutation plans, including invalid/no-op state and undoable apply behavior. Shared effect-options descriptors now cover the supported Fly In, Wipe, Zoom, Split, and Random Bars variants, and both WPF/Avalonia panes apply direction mutations through the same undoable planner route. Authoritative PowerPoint COM visual baselines, advanced effect variants, advanced timeline/trigger sequencing, pane-level playback/preview depth, authoring workflows, and deeper playback/PPTX timing/effect persistence evidence remain deferred.
- Added a shared comments/review/accessibility/alt-text workflow planner that describes legacy comment pane actions, comment mutation intents, shared comment mutation-plan application, accessibility issue summaries, issue action summaries, alt-text request/update descriptors, and proofing scopes.
- Added first Avalonia rendered comments-pane evidence and shared selected-thread state. The shared comments command now opens a compact in-app pane populated from `PresentationCommentPanePlan` descriptors, selected-thread state, and action state, with focused WPF/Avalonia coverage proving both hosts consume the shared selected-comment plan instead of only recording adapter state.
- Added shared modern comment Resolve/Reopen/Delete execution. `PresentationReviewWorkflowPlanner.TryApplyCommentMutationPlan` now applies enabled comment mutation plans, and the visible/selectable WPF and Avalonia comments panes route Resolve/Reopen through the same shared resolve/reopen plans with thin host refresh logic.
- Added visible comment Delete execution. The WPF Delete comment command, Avalonia ribbon command, and Avalonia visible pane Delete action now consume `PresentationReviewWorkflowPlanner.BuildDeleteCommentPlan`, shared mutation-plan application, and `NormalizeCommentSelectionAfterMutation` before refreshing host state.
- Added first modern comment reply-chain/action-state evidence and visible Add/Edit execution evidence. `SlideComment` carries reply rows, `PresentationCommentPanePlan` exposes reply descriptors, reply counts, mention counts, and Reply action enablement, shared Add/Edit mutation plans apply through `TryApplyCommentMutationPlan`, and WPF/Avalonia panes render shared reply rows plus visible add/edit inputs with thin host mutation routes. Rich mention UI, full modern comment UI parity, proofing/accessibility depth, and PowerPoint-authoritative review baselines remain deferred.
- Added persistent shape alt-text model/IO for PowerPoint `cNvPr descr`, plus an undoable shared command seam and thin WPF/Avalonia adapter apply routes.
- Added the first alt-text metadata expansion: `AlternativeTextTitle` and `IsDecorative` live in the shared model, clone/undo paths, PowerPoint reader/writer, review planner, and WPF/Avalonia adapters. PowerPoint `cNvPr title` and Microsoft `adec:decorative` metadata now round-trip, decorative objects are excluded from missing-alt-text issues, and writer coverage pins the `a:hlinkClick` before `a:extLst` child order when a decorative linked shape is saved.
- Added shared alt-text pane planning and first visible WPF/Avalonia pane UI. `PresentationReviewWorkflowPlanner` now describes title and description field state, required-description validation, decorative-object behavior, Apply/Decorative/Close actions, and normalized mutation output. WPF and Avalonia render in-app panes over that shared plan with title/description fields, decorative toggling, Apply/Close actions, and shared mutation routes. Richer generated suggestions, full modern comments UI depth, full proofing execution, and full accessibility checking remain deferred.
- Added shared reading-order planning, first visible WPF/Avalonia reading-order panes, and selected-shape Move Earlier/Move Later mutation for both top-level shapes and sibling children inside groups. `PresentationReviewWorkflowPlanner` now describes current-slide reading-order items, selected-shape state, headings/messages, move earlier/later action descriptors, and mutation plans; `TryApplyReadingOrderMove` applies enabled moves through recursive sibling-list planning and `EditingSession.MoveSelectedShapeInReadingOrder`. Both hosts render shared-plan-backed panes with thin button handlers, and focused WPF/Avalonia evidence covers nested group-child movement plus boundary disablement.
- Added a shared table-cell edit planner for selected-cell normalization, edit start placement, commit/cancel routing, undo-aware commit, and enabled-state. WPF consumes it, and Avalonia now has active-cell highlighting plus an in-canvas plain-text edit overlay with shared commit/undo/cancel routing and merge-anchor normalization.
- Added shared active table-cell bold/italic/underline formatting plans. `TableCellEditPlanner` now builds undo-aware mutation plans over the active/normalized cell text runs, `EditingSession` exposes those plans for host adapters, WPF ribbon fallback routes selected table-cell formatting through the shared policy when no rich editor is open, and Avalonia routes its ribbon/overlay formatting through the same shared policy while mirroring visible overlay state. The follow-up Avalonia adapter slice carries rich subrange formatting metadata through the in-canvas overlay path, and the latest table-cell selection slice adds shared initial selection planning. Richer editor fidelity and parity with the WPF `RichTextBox` editing experience remain deferred.
- Added shared presenter recording/timing and pointer/ink intent/state planning, with WPF and Avalonia exposing thin adapter state for recording availability, elapsed-time reset, pointer tool selection, ink color, and ink width. The latest slideshow ink and presenter-summary slices add shared overlay render primitives and shared recording/ink summary evidence consumed by thin WPF/Avalonia renderers. Narration/audio capture, camera/media capture, full recording execution, subtitles/richer custom-show workflows, and PowerPoint baseline generation remain deferred.
- Added semantic-edit package-retention coverage for high-risk corpus decks with media, charts, SmartArt diagrams, picture crop, chart types, chart labels, comments, and speaker notes, preserved embedded chart workbook package sidecars when the modeled chart count still matches the source package, and added evidence that chart-data edits regenerate only the edited chart workbook/cache sidecars instead of retaining stale source workbook data. The latest package slices record source chart-part metadata on `ChartShape`, preserve workbook/externalData relationships by source part for non-sequential multi-chart decks, cover a richer in-memory authored chart workbook fixture with formulas, defined names, calc-chain/source workbook sidecars, content types, relationships, and unrelated custom XML retention, and add real `19-chart-labels.pptx` chart-data edit evidence for preserving neighboring authored `Sheet1!` formulas and workbook sidecars; broader PowerPoint-authored workbook formula/range parity and corpus depth remain deeper follow-ups.
- Added targeted PPTX retention for presentation/view/print settings. `PptxPackageWriter` preserves existing `ppt/presProps.xml`, `ppt/viewProps.xml`, `ppt/printerSettings/printerSettings1.bin`, the `presProps.xml.rels` printer-settings relationship, and the printer-settings content type when saving a loaded deck.
- Added shared notes-page preview planning, host evidence, portable notes-page PDF rendering, and visible export command routes. `PresentationNotesPagePreviewPlanner` owns notes-page range, title/text extraction, placeholder text, and page geometry; WPF and Avalonia refresh `LastNotesPagePreviewPlan` from the existing notes workflow. `PresentationNotesPagePdfExporter` now reuses that shared geometry to emit slide thumbnails plus speaker notes into portable PDF draw ops, while WPF Backstage and the Avalonia File ribbon expose thin native-save notes-page PDF commands over the shared planner/exporter. Native print execution/preview and PowerPoint-baseline notes-page visual evidence remain follow-ups.
- Added shared OMML math layout/rendering. OMML is parsed into a shared math node tree, including `m:limLow`/`m:limUpp` limit nodes, laid out into `MathBox` primitives, carried through slide draw ops, and rendered by both WPF and Avalonia through `MathBoxRenderPlanner`; this improves math display beyond raw fallback preservation, but it is still not a PowerPoint-authoritative math-baseline claim or complete equation parity.
- This worker branch, `codex/freep-proofing-sentence-capitalization-20260703`, adds a bounded shared proofing-engine slice: lowercase sentence starts at the start of a scope or after `.`, `!`, and `?` are now detected by `PresentationReviewWorkflowPlanner`, shown with a single-letter capitalization correction, and corrected through the existing shared proofing mutation route consumed by WPF and Avalonia panes. This extends the existing dictionary typo and adjacent repeated-word proofing coverage without claiming full grammar proofing parity.

PowerPoint baseline anchors used for current scope: Microsoft documents PowerPoint File > Export paths including PDF, video, and handouts, Presenter View with current/next slide and notes, recording narration/timings, print layouts for slides/handouts/notes, and modern comments. Those remain parity targets for FreeP:

- `https://support.microsoft.com/en-us/powerpoint/export-a-presentation`
- `https://support.microsoft.com/en-us/powerpoint/training/save-powerpoint-presentations-as-pdf-files`
- `https://support.microsoft.com/en-us/powerpoint/turn-your-presentation-into-a-video`
- `https://support.microsoft.com/en-us/powerpoint/training/use-presenter-view-in-powerpoint`
- `https://support.microsoft.com/en-us/powerpoint/training/record-a-slide-show-with-narration-and-slide-timings`
- `https://support.microsoft.com/en-us/powerpoint/training/print-your-powerpoint-slides-handouts-or-notes`
- `https://support.microsoft.com/en-us/powerpoint/modern-comments-in-powerpoint`

## Current Implementation Reality

FreeP is now a real shared-first implementation, not a stub shell. Several gaps from the June 27 report are closed or materially changed.

| Area | Current repo evidence | Current status |
| --- | --- | --- |
| Solution map | `FreeP.slnx` includes Core.Model, Core.IO, App.Presentation, WPF host/rendering/tests, Avalonia host/rendering/tests, ribbon definitions, localization, and `tools/FreeP.RenderCompare`. | Dedicated FreeP solution exists. |
| Shared model/IO | `freep/FreeP.Core.Model/**`, `freep/FreeP.Core.IO/**`, `shared/Free.Shared.Opc`, `shared/Free.Shared.Drawing`, `shared/Free.Shared.Pdf`. | PPTX model, reader/writer, package snapshot, OPC helpers, charts, SmartArt, comments, notes, OLE/math, transitions, alt-text title/decorative metadata, chart workbook sidecar preservation, chart source-part metadata for workbook/externalData retention, chart data-table fill/border/text style import/export, chart-data semantic regeneration of edited embedded workbook/cache sidecars, richer authored chart workbook formula fixture retention, preserved presentation/view/print settings, and PDF plumbing exist. |
| Shared presentation layer | `freep/FreeP.App.Presentation/**`. | App-neutral planners/controllers now cover persistence, file dialogs, slide composition, text layout including bullets, tab-stop segment placement, and OMML math layout including lower/upper limits, chart primitives plus series/gridline-style/data-table fill, border, and text-style plans consumed by renderers, effects, picture color effects, gestures, hit testing, slide pane policy, table-cell edit planning including initial selection, slideshow host/playback, animation playback session planning, presenter recording/ink intent planning plus slideshow ink overlay render primitives, export/print descriptors, notes-page preview, handout layout and PDF render planning, object insertion, dialogs, design/layout picker choices and UI plans, SmartArt layout, and comments/review/accessibility/alt-text/reading-order pane workflow planning. |
| Shared ribbon definitions | `freep/FreeP.Ribbon.Definitions/**`, `freep/FreeP.Ribbon.Definitions.Tests/**`, `docs/parity/freep-command-parity-inventory.*`. | WPF and Avalonia ribbon profiles are single-sourced with generated capability deltas. The current inventory reports 93 total commands, 87 shared, 0 actionable WPF/Avalonia missing commands, 0 explicit Avalonia gaps, 0 known-deferred commands, 6 platform-only Avalonia shell commands, and 0 command-id aliases. |
| WPF app | `freep/FreeP.App.Host/**`, `freep/FreeP.App.Rendering.Wpf/**`, `freep/FreeP.App.Host.Tests/**`. | WPF is mostly a host/renderer adapter over shared policy, but still has richer dialog and rich-text editing coverage than Avalonia. Slide pane context actions, table-cell editing including shared initial selection, presenter tool state, slideshow ink overlay primitive rendering, layout-picker UI/gallery chrome, visible alt-text and reading-order panes, notes-page preview state, Design commands, and animation running/stopped state now route through shared planners. The WPF renderer consumes the shared chart gridline stroke plan, shared chart data-table fill/border/text-style plan, and shared OMML math draw operations including limit boxes through neutral recursion. |
| Avalonia app | `freep/FreeP.App.Avalonia/**`, `freep/FreeP.App.Rendering.Avalonia/**`, `freep/FreeP.App.Avalonia.Tests/**`, `freep/FreeP.App.Rendering.Avalonia.Tests/**`. | Cross-platform shell and renderer exist. Avalonia consumes shared file workflow, ribbon definitions, slide pane planning/actions with first drag-reorder depth evidence, table-cell edit planning with active-cell highlight, plain-text in-canvas overlay, rich subrange formatting metadata routing, and shared initial selection, rendering planners, shared presenter state plus recording/ink adapter state and slideshow ink overlay primitive rendering, shared export planning, shared transition commands, shared Design commands with visible layout-picker UI/gallery chrome, shared Animations command planning with visible animation-pane UI over `AnimationPaneTimelinePlan`, shared animation running/stopped state, rendered comments-pane evidence, visible alt-text pane UI/apply routes, visible reading-order pane UI, and notes-page preview state. The Avalonia renderer consumes the shared chart gridline stroke plan, shared chart data-table fill/border/text-style plan, and shared OMML math draw operations including limit boxes through neutral recursion. No explicit Avalonia command gaps or known-deferred command slices remain in the generated inventory. |
| PPTX lifecycle | `freep/FreeP.App.Presentation/PresentationFilePersistenceWorkflow.cs`, `freep/FreeP.App.Host/FileCommands.cs`, `shared/Free.Shared.Shell.Avalonia/SisterAvaloniaFileCommandWorkflow.cs`. | `.pptx` is now the default lifecycle path; `.fxp` remains legacy-compatible. The old WPF `.pptx` lifecycle gap is no longer a foundational blocker. |
| Package retention | `freep/FreeP.Core.Model/PptxPackageSnapshot.cs`, `PptxPackageReader.cs`, `PptxPackageWriter.cs`, `PptxChartWriter.cs`, `freep/FreeP.App.Host.Tests/PptxPackageRetentionTests.cs`. | Preservation machinery exists, the 21 tracked corpus decks have a retention contract test, and seven high-risk corpus decks now have semantic modeled-shape edit/save/reopen retention coverage. Comments, speaker notes, preserved chart workbook package sidecars, non-sequential multi-chart workbook/externalData retention by source part, authored chart data-table fill/border/text style round-trip, chart-data edit workbook/cache regeneration, richer authored formula workbook fixture retention, real `19-chart-labels.pptx` middle-chart semantic edit retention, and retained `presProps`/`viewProps`/`printerSettings` now have explicit package coverage; broader semantic edit breadth, broader PowerPoint-authored workbook formulas/ranges, and corpus depth are still needed. |
| Render comparison | `tools/FreeP.RenderCompare/**`, `tools/FreeP.RenderCompare.Tests/**`, `tools/FreeP.RenderCompare/corpus/*.pptx`, `tools/FreeP.RenderCompare/PixelDiversity.cs`. | Harness supports WPF, Avalonia, PowerPoint, diff, pixel-diversity guards, explicit COM-unavailable reporting, and full-corpus summaries. Local PowerPoint COM is unavailable; the current local corpus summary is 21 decks total, 5 reference-ready, and 16 missing references. |

## Shared-First Boundary

Future parity work should start in shared or neutral code unless the difference is genuinely platform API glue.

| Layer | Owns | Must not own |
| --- | --- | --- |
| `FreeP.Core.Model` | Presentation semantics, shapes, text, charts, SmartArt, transitions, comments, notes, OLE/math, package snapshot data. | WPF/Avalonia controls, dialogs, input events, pixel drawing APIs. |
| `FreeP.Core.IO` | PPTX/FXP/PDF package semantics, PresentationML read/write, preserved parts/rels/content types, export serialization. | Host lifecycle prompts, renderer-specific drawing, native file dialogs. |
| `FreeP.App.Presentation` | Command planners, file persistence workflow, dialog policy, slide pane policy, slideshow/presenter models, render plans, gesture reducers, text/chart/effects planning. | Platform controls, WPF dependency properties, Avalonia controls, native windows. |
| `FreeP.Ribbon.Definitions` | Ribbon tabs/groups/controls, command ids, labels/keytips, WPF/Avalonia capability profiles. | Command implementation bodies and host widget construction. |
| WPF host/rendering | Owner windows, native dialogs, WPF focus/UIA, WPF `DrawingContext`/`RenderTargetBitmap`, media element, keyboard/mouse adapter, command binding to shared planners. | PowerPoint behavior decisions or duplicated render math. |
| Avalonia host/rendering | Cross-platform windows/dialogs, file pickers, Avalonia controls, Skia/Avalonia drawing, pointer/keyboard adapter, headless smoke plumbing. | PowerPoint behavior decisions or duplicated render math. |

## Remaining PowerPoint Parity Gaps

These are the current gaps after the large dedup effort. They are ordered by what should unblock the most reliable implementation work.

| Gap | Why it matters | Shared-first owner | Thin renderer/host owner | Evidence/gates |
| --- | --- | --- | --- | --- |
| FreeP WPF-vs-Avalonia command matrix | The generated matrix now exists and is the source of truth for command-surface work. Current state: 93 total, 87 shared, 0 actionable WPF/Avalonia missing commands, 0 explicit Avalonia gaps, 0 known deferred, 6 platform-only Avalonia shell commands, 0 command-id aliases. | Keep `tools/Generate-FreePCommandParityInventory.ps1` and generated docs current as command slices land. | WPF/Avalonia only classify adapter-only or platform-only commands. | `tools/Test-GeneratedDocs.ps1`; `FreePRibbonDefinitionProfileTests.cs`. |
| Ribbon depth and stubs | The explicit Avalonia command gaps and known-deferred command slices are closed in the generated matrix. Transition, Design, and Animations command IDs are exposed through WPF/Avalonia planner-backed routes, animation pane/timeline state, playback control descriptors, and deterministic Preview/Play From Selected/Play All/Stop session plans are shared, WPF and Avalonia now open visible animation panes over the shared timeline/playback plan with shared running/stopped state, and the layout-picker plan now exposes PowerPoint-shaped choice evidence including order, master grouping labels, placeholder counts, current selection, and no-current-slide apply gating. WPF and Avalonia now open visible layout-picker UI over the shared plan. Review/View/Accessibility/Recording and deeper workflow UI remain incomplete outside the current command matrix. | Shared ribbon definitions, command planners, layout/timing/animation models in `FreeP.App.Presentation`. | Host adapters render galleries, combo boxes, panes, and dialogs. | Command inventory deltas; `RibbonEditorCompleteness5BTests.cs`; `RibbonTransitionsAnimationsTests.cs`; `PresentationDesignCommandPlannerTests.cs`; `AnimationPanePlannerTests.cs`; new stub-failure tests. |
| Render harness references | Pixel-diversity guards and trust reporting are in place. Only decks 01-05 have tracked PowerPoint PNG refs; decks 06-21 lack refs because local PowerPoint COM is unavailable. | `tools/FreeP.RenderCompare` reference freshness checks, corpus manifest, full-corpus summary. | WPF/Avalonia renderers only provide images. | Run refs on a PowerPoint COM machine; compare all 21 decks. |
| Package fidelity corpus contract | The 21 tracked decks now have a package-retention contract, media/chart/SmartArt/comments/notes-focused decks have semantic modeled-shape edit coverage, chart workbook package sidecars are preserved for matching chart counts, source-part multi-chart workbook/externalData relationships are preserved, authored chart data-table fills plus modeled border/text style round-trip through `c:dTable`/`c:spPr`/`c:txPr`, and loaded-deck presentation/view/print settings preserve `presProps`, `viewProps`, `printerSettings`, relationships, and content types. Synthetic semantic-edit coverage now proves presentation-scoped custom XML parts, item relationships, payload parts, content types, first chart-data workbook/cache regeneration, and richer authored chart workbook formula fixture retention survive writer-owned presentation rewrites. It also proves a semantic chart-data edit regenerates only the edited chart workbook/ranges while preserving the unedited authored formula workbook byte-for-byte; the richer fixture now covers two authored chart series, quoted sheet names, global/local defined names, nontrivial workbook formulas, and regenerated `ChartData` ranges for both edited series. Real-corpus `19-chart-labels.pptx` coverage now proves editing chart 2 regenerates only that workbook/range set while charts 1 and 3 keep authored `Sheet1!` formula refs, original relationships, and workbook sidecars. Coverage remains thin for broader real PowerPoint-authored workbook formulas/ranges, full authored workbook parity, and semantic-edit corpus depth beyond the current source-part, in-memory fixture, and chart-labels routes. | `FreeP.Core.IO` package classification and writer merge rules; shared OPC helpers stay generic. | Hosts do not own package semantics. | Expand corpus open-save-reopen tests with targeted ZIP/OpenXML assertions after writer-owned edits. |
| Renderer orchestration duplication | Chart legend planning, chart axis-title layout/orientation, chart series fill/stroke/marker segment planning, chart gridline stroke planning and renderer consumption, major axis tick primitive planning and renderer consumption, secondary value-axis labels/ticks/title planning, chart data-table cell bounds, background fills, solid border stroke color/width/dash, and text style fields, text route planning, runtime autofit anchoring, bullet slot placement, tab-stop segment placement, stacked upright vertical glyph placement, drag reduction, and placeholder text inset inheritance are now shared. Solid and gradient data-table border visual rendering is covered; deeper chart/text/effects sequencing, labels, and edit-state planning still need shared-core work. | Continue moving chart scene plans, text route decisions, paragraph/bullet/tab decisions, and gesture/edit reducers into `FreeP.App.Presentation`. | Renderers map plans to WPF/Avalonia primitives. | `RendererNeutralDedupPlannerTests.cs`; `ChartRenderPlannerTests.cs`; `TextLayoutPlannerTests.cs`; `CanvasGesturePlannerTests.cs`; render tests. |
| Text, math, and WordArt fidelity | PowerPoint parity requires better coverage for measured text metrics, broader autofit, columns, vertical text beyond current stacked-upright modes, richer run effects, theme/default fonts, and WordArt. Placeholder text inset inheritance now resolves through shape, layout placeholder, and master placeholder before WPF/Avalonia render plans consume it; bullet slot placement, bullet typography source handling for paragraph and list-level `buSzPts`/`buSzTx`/`buFontTx`/`buClrTx`, tab-stop segment placement, runtime normal-autofit line-spacing anchoring, stacked upright glyph placement for `eaVert`/`wordArtVert`/`wordArtVertRtl`, and run-level WordArt/text-run `a:rPr/a:effectLst/a:glow` plus `a:softEdge` metadata and approximate shared render passes now come from shared paragraph/text planning. OMML math now parses and lays out shared math boxes consumed by both renderers, including `m:limLow`/`m:limUpp` limits, `m:eqArr` row alignment, `m:phant` transparent spacing, skewed `m:fPr/m:type="skw"` fractions, and `m:naryPr` hidden lower/upper limit suppression for `m:subHide`/`m:supHide` through shared `MathLayoutEngine`/`MathBoxRenderPlanner` paths, but it still needs PowerPoint-backed visual baselines and broader equation construct coverage. Glow/soft-edge rendering remains a conservative geometry approximation until PowerPoint COM baselines are available. `wordArtVertRtl` single-column ordering is conservative until a PowerPoint COM visual baseline is available. | `TextLayoutPlanner`, shared math layout, model/IO text defaults, placeholder inheritance, WordArt/warp planning. | Platform font measurement/drawing callbacks. | `TextLayoutPlannerTests.cs`; `BulletsAutofitTests.cs`; `WordArtTests.cs`; `TextColumnsGradOutlineTests.cs`; OMML/math render tests; PowerPoint-backed deck refs. |
| Charts fidelity | Chart read/write and primitive planning exist, and category/value axis-title layout/orientation, shared series fill/stroke/marker primitives, chart gridline stroke intents, major axis ticks, secondary value-axis labels/ticks/title geometry, chart data-table cell bounds, chart data-table background fills, solid border stroke color/width/dash, and text size/color/bold/italic/font-family now route through shared planning and WPF/Avalonia renderer consumption. PPTX `c:dTable`/`c:spPr`/`c:txPr` data-table fill, border, and text style import/export now round-trip through shared model state, and authored data-table package coverage pins that path. Authored series solid fill, line color/width/dash, marker symbol/size/fill/stroke, point fill/stroke/marker styles, PowerPoint-authored `a:gradFill`, and PowerPoint-authored `a:pattFill` for chart series, points, and markers now have package round-trip, shared-planner, compositor, and WPF/Avalonia renderer-consumption evidence. Preserved embedded chart workbook package sidecars reduce open/save loss when chart counts match, modeled chart-data edits now trigger regenerated workbook/cache sidecars, non-sequential multi-chart decks preserve workbook/externalData relationships by source chart part, and the richer in-memory authored formula-workbook fixture proves unedited authored formula workbooks survive while edited chart ranges are regenerated. The package corpus now also covers two-series authored workbook formulas with quoted sheet names, global/local defined-name references, nontrivial formula cells, regenerated multi-series `ChartData` ranges after semantic edits, and a real `19-chart-labels.pptx` middle-chart edit that preserves neighboring authored `Sheet1!` formula ranges and workbook sidecars. Solid and gradient data-table border visual rendering is covered, and chart axis `c:numFmt` format-code/source-linked metadata now round-trips and reaches renderer-neutral axis-label format plans. Broader real PowerPoint-authored workbook formulas/ranges, deeper type-specific chart decisions, full number/date format evaluation, corpus depth, and PowerPoint visual baselines remain high risk. | `ChartShape`, `PptxChartReader/Writer`, `ChartRenderPlanner`, package retention. | Draw primitives only. | `ChartDataCommandTests.cs`; `ChartRenderPlannerTests.cs`; `RendererNeutralDedupPlannerTests.cs`; `ChartTests.cs`; `ChartDataLabelsTests.cs`; corpus decks 06, 18, 19. |
| SmartArt and modern object fidelity | SmartArt parsing/layout/fallback exists, but live SmartArt data/style/color parts and editing/rendering are not PowerPoint-complete. OMML math now has shared layout/rendering including lower/upper limits, while OLE and other modern objects still rely on fallback previews or placeholders. | Core model/IO package semantics, `SmartArtLayoutEngine`, modern object fallback policy, shared math layout. | Render previews/placeholders only, except math draw operations consumed by WPF/Avalonia. | `SmartArtTests.cs`; `SmartArtLayoutTests.cs`; `ModernObjectsRoundTripTests.cs`; `OleMathRoundTripTests.cs`; corpus deck 09/14. |
| Effects, media, connectors, and pictures | Effects/3D/crop/motion-path support is modeled, but visual parity needs PowerPoint-backed evidence and more shared planning where renderers still diverge. Avalonia slideshow sound is deferred/no-op. | Shared effects/media/connector/picture planners and PresentationML IO. | Platform media playback, image decoding, and drawing. | `PictureCropEffectsTests.cs`; `MotionPathTriggerTests.cs`; `Bevel3dTests.cs`; render corpus decks 07, 08, 10, 11, 12, 15, 16. |
| PDF/export/print/backstage | PowerPoint supports File > Export to PDF/video/handouts and print layouts for slides/handouts/notes. FreeP now has shared export descriptors, PDF dialog/picker plans, Backstage export plan, print output-option descriptors/package execution planning, slide-range normalization, selected-slide PNG image export execution, WPF Backstage image export, Avalonia ribbon image export, Avalonia visible print-options pane projection, deferred video plans, and print layout plans for full-page slides, notes pages, and handouts. Handout layout geometry is shared for 1/2/3/4/6/9 slides per page, including 3-up writing-line areas, with WPF/Avalonia as thin plan consumers. `PresentationHandoutPdfExporter` turns that shared plan into portable PDF draw-op handout pages by scaling slide draw ops into slots, drawing borders, and emitting writing lines. Notes-page preview planning covers range, title/text extraction, and page geometry with WPF/Avalonia evidence, and `PresentationNotesPagePdfExporter` now turns that same shared plan into portable PDF pages with slide thumbnails, note boxes, speaker-note lines, and empty-note placeholders. WPF Backstage and the Avalonia File ribbon expose notes-page PDF output through a shared command/descriptor/picker route and thin native save adapters; PDF export routes consume the shared slide PDF path, and image export hooks are thin render-callback/folder-picker adapters over `PresentationImageExportExecutor`. Native print handoff planning now sits over printable PDF packages with shared package-ready metadata, suggested temporary file names, page/content/layout/range/options summaries, no-slide handling, WPF/Avalonia host-deferred capability routes, shared host-ready PDF validation, suggested document/job names, and atomic package materialization. Video export handoff planning now sits over the shared video frame package path with package-ready status, host encoder/capture capability metadata, and WPF/Avalonia host-deferred state. Remaining gaps are actual OS/native printer adapter execution, richer native print-dialog handoff, real MP4 encoder integration, narration/camera/media capture, full compositor/vector fidelity, richer remaining output-option UI, and PowerPoint-baseline handout/notes/print/video visual parity. | Shared export/backstage/print planners; `PresentationImageExportExecutor`; `PresentationPdfExporter`, `PresentationNotesPagePreviewPlanner`, `PresentationNotesPagePdfExporter`, `PresentationHandoutPdfExporter`, native print handoff/execution descriptor planning, and video export handoff planning should continue moving toward compositor/draw-op-backed slide, notes, handout, print package, and video frame-package output. | Native print dialogs/adapters, owner windows, platform file/folder pickers, renderer callbacks, native MP4 encoder/capture adapters. | `PresentationPdfExporterTests.cs`; `PresentationExportPlannerTests.cs`; PDF/render inspection. |
| Slide pane and editing parity | Slide pane context actions, enabled-state, drag-reorder action planning, thumbnail chrome tokens, and section-header chrome/grouping affordances are now shared and consumed by WPF/Avalonia. Avalonia has visible insertion feedback and planner-backed drop routing, with foreground pointer/drop screenshots and richer thumbnail bitmap comparison still open. Table-cell selected-state normalization, edit start, initial text selection, commit, undo, and cancel route through a shared planner consumed by WPF and Avalonia. Begin-edit selection now selects full populated cell text and places a collapsed caret at the start of empty cells through `InCanvasEditorTextSelection`. Active table-cell bold/italic/underline and paragraph-alignment formatting route through shared mutation plans consumed by WPF command fallback and Avalonia ribbon/overlay adapters. Active table-cell bullet toggle, numbering toggle, list preset, and indent/outdent now use the same shared planner/`EditingSession` route, with WPF command IDs and Avalonia overlay selection/result plans kept thin. Shared list preset descriptors cover disc bullets, Arabic numbering, upper/lower Roman numbering, and upper/lower alpha numbering. The latest shared rich-edit result plan preserves/clamps effective selections, reports selected run ranges, gives Avalonia a post-format rich-text overlay plan, and keeps WPF native `RichTextBox` formatting selection stable. Avalonia still needs richer editor-widget fidelity comparable to the WPF `RichTextBox` path, and visible PowerPoint bullet/numbering galleries plus image bullets remain open. | `SlidePanePlanner`, `TableCellEditPlanner`, editing/session/table-cell planners, gesture reducer. | WPF/Avalonia list/control realization and input wiring; rich editor widgets stay platform-local. | `SlidePanePolicySourceGuardTests.cs`; `SlidePaneTests.cs`; `TableCellEditPlannerTests.cs`; `CanvasEditingTests.cs`; `TableEditCommandTests.cs`; `RibbonEditorCompleteness5BTests.cs`; `SlideCanvasGeometryPlannerTests.cs`; `SlideCanvasAvaloniaTests.cs`; `MainWindowHeadlessTests.cs`; `docs/parity/freep-slide-pane-reorder-evidence-2026-07-02.md`; `docs/parity/freep-slide-pane-section-header-chrome-evidence-2026-07-04.md`. |
| Slideshow and presenter workflows | Shared presenter state now covers current slide, next slide, notes, elapsed time, fullscreen intent, monitor intent, recording/timing state, pointer/ink tool state, renderer-neutral slideshow ink overlay primitives for strokes and laser pointer dots, deterministic animation playback session state for Preview, Play From Selected, Play All, and Stop, first actual timed click-step visual playback execution in both WPF and Avalonia, shared presenter session-summary planning for recording/ink evidence, shared custom-show launch route planning for named shows plus All Slides fallback, and shared custom-show authoring helpers for create, rename, delete, and slide membership/order updates. The slideshow windows consume `SlideShowPlaybackPlanner.PlanAnimationStep` and execute planned Appear/Fade/FlyIn/Wipe/Zoom/Pulse/Spin/MotionPath effects through host-specific timed helpers, with fallback emphasis flash routes when no shape overlay exists. Advanced effect variants, recording/timeline authoring, narration/audio capture, camera/media capture, full recording execution, subtitles, rich visible custom-show authoring UI/deeper persistence, full PowerPoint visual baselines, and COM baselines remain deferred. | Shared slideshow/presenter/timing/recording/ink, custom-show launch/authoring, session-summary, and animation playback-session models and planners. | Fullscreen windows, monitor APIs, media element, cursor/input capture, recording devices, ink rendering adapters, timed visual playback helpers, and thin custom-show authoring adapters. | `SlideShowHostPlannerTests.cs`; `SlideShowPlaybackPlannerTests.cs`; `SlideShowPresenterToolPlannerTests.cs`; `SlideShowInkExecutionPlannerTests.cs`; `AnimationPanePlannerTests.cs`; `SlideShowCustomShowPlannerTests.cs`; `SlideShowPresenterSessionSummaryPlannerTests.cs`; `SlideShowTests.cs`; WPF/Avalonia slideshow/animation adapter tests. |
| Comments, accessibility, proofing, alt text, and reading order | PowerPoint includes modern comments, accessibility/alt text workflows, reading order, proofing, and review surfaces. FreeP now has a shared planner boundary for legacy comment pane actions, selected-comment descriptors, reply descriptors with mention counts, mention detail descriptors, add/edit/delete/reply intent payloads, Add/Edit/Resolve/Reopen/Delete mutation plans, shared comment mutation-plan application through `PresentationReviewWorkflowPlanner.TryApplyCommentMutationPlan`, post-mutation comment-selection normalization, accessibility issue summaries with action summaries/command hints, persistent alt-text request/update descriptors, alt-text pane field/action/validation plans, reading-order item/action/mutation plans, and proofing scopes, plus WPF/Avalonia adapters that expose the shared comments-pane, accessibility-summary, alt-text request/pane/apply, reading-order pane and Move Earlier/Move Later actions for top-level shapes and sibling children inside groups, and proofing plans as host state/commands. Avalonia has rendered comments-pane evidence: the comments command opens an in-app pane populated from shared comment descriptors, selected-thread chrome, reply rows, and action enablement. WPF consumes the same selected-comment/reply-chain/mention-detail plan for its comment strip and overlay indicators. WPF and Avalonia now have visible/selectable comments panes whose visible Add/Edit/Resolve/Reopen/Delete/Reply routes use the same shared mutation plans, first visible in-app alt-text panes over the shared title/description/decorative/apply plan, and visible reading-order panes over the shared item list. Top-level and nested group-child selected-shape reading-order moves mutate through shared `PresentationReviewWorkflowPlanner`/`EditingSession` code consumed by thin WPF/Avalonia buttons. Shape alt-text description, title, and decorative-object metadata now round-trip through PowerPoint `cNvPr descr`, `cNvPr title`, and `adec:decorative`, and decorative objects no longer raise missing-alt-text issues. Shared alt-text suggestions now include chart-data-aware deterministic hints for available chart type, series names, category samples, and value counts, plus picture metadata and table row samples. Shared proofing now covers dictionary typo suggestions, adjacent repeated-word detection/correction, sentence-start capitalization, whitespace-before-punctuation cleanup, missing-space-after-sentence-punctuation cleanup, missing spaces after comma/semicolon/colon list punctuation, and extra interior horizontal spaces through the same pane mutation route, with guards for decimals, times, URLs, web addresses, email/mailto tokens, existing whitespace, adjacent punctuation, line-start indentation, trailing whitespace, and newline boundaries. Shared accessibility now flags text-run hyperlinks without ScreenTips, unclear hyperlink display text, chart titles missing from chart shapes, non-decorative video media without closed captions/subtitles, filename-like alt text, duplicate non-decorative object alt text, blank non-header table body cells while keeping header-row blanks de-duplicated, and merged/split table cells with a shared `Review Table Structure` action. Media caption/subtitle track metadata is now modeled from slide relationships, captioned video media no longer produces the missing-caption diagnostic, modeled internal/external caption tracks can be emitted back to PPTX packages, and authored WebVTT/SRT caption bytes now feed shared transcript plans exposed by both hosts. It still lacks rich mention insertion UI, people-picker/notification routing, full modern comment UI parity, generated AI alt text, broader grammar-scale proofing, a full accessibility checker, real audio transcription/capture-generated transcripts, PowerPoint-authoritative review baselines, rich remediation UI, and complete table remediation UI parity. | `PresentationReviewWorkflowPlanner`, comments/review/accessibility/reading-order planners, model/IO for comments, alt text, media caption metadata, and caption transcript planning. | Pane/dialog realization, focus/UIA, and host command/state adapters. | `PresentationReviewWorkflowPlannerTests.cs`; `ReviewWorkflowAdapterTests.cs`; `MainWindowHeadlessTests.cs`; `PptxRoundTripTests.cs`; `SectionsCommentsTests.cs`; `MediaFieldsTests.cs`; `PresentationMediaTranscriptPlannerTests.cs`; new modern comments/accessibility/alt-text/reading-order UI tests. |

## Implementation Plan

Use small, isolated linked worktrees. Each worker should start from current `origin/main`, preserve unrelated work, and report exact validation. Do not put feature semantics directly in WPF or Avalonia host files when a shared planner/model can own them.

### Phase 0 - Evidence Gate First

Goal: make parity measurable before widening feature work. The first three evidence items are now complete on `main`; PowerPoint reference generation remains blocked on this machine by missing PowerPoint COM registration.

1. Done: fix `tools/FreeP.RenderCompare --avalonia-compare` so PowerPoint export failure returns failure.
2. Done: add machine prerequisite output that clearly separates "PowerPoint COM unavailable" from "FreeP parity failed".
3. Done: add a corpus manifest/full-corpus summary for the 21 tracked decks.
4. Generate missing PowerPoint reference PNGs for decks 06-21 on a machine with PowerPoint COM installed.
5. Done: add a generated FreeP WPF-vs-Avalonia command inventory modeled after the FreeX parity inventory.

### Phase 1 - Shared Planning Cleanup

Goal: remove the remaining behavior duplication before adding large parity surfaces. The first shared chart/text/gesture slices are complete; the remaining work is deeper fidelity and workflow planning, not the initial extraction.

1. Done: move chart legend scene orchestration into `ChartRenderPlanner`, leaving renderers as primitive painters for that route.
2. Done: move first text render route decisions into `TextLayoutPlanner`.
3. Done: move canvas drag state reduction into `CanvasGesturePlanner`, leaving event capture and cursor APIs platform-local.
4. Done for first chart axis-title slice: shared `ChartRenderPlanner` now reserves title bands and returns category/value axis-title orientation/bounds consumed by WPF and Avalonia renderers.
5. Done for first chart series-style slice: shared `ChartRenderPlanner` now returns series fill/stroke, line-segment, and marker primitives consumed by WPF and Avalonia renderers.
6. Done for first bullet placement and typography-source slices: shared text planning now returns bullet text/font/color/size and X/Y placement for body and column layouts, including paragraph/list-level `buSzPts`, `buSzTx`, `buFontTx`, and `buClrTx` resolution before WPF and Avalonia consume the shared plan.
7. Done for first tab-stop slice: shared `TextLayoutPlanner` now returns explicit/default left/right/center/decimal tab segment placement consumed by WPF and Avalonia renderers.
8. Done for first chart gridline renderer slice: WPF and Avalonia now consume `ChartMajorGridLinePrimitivePlan.GridLineStroke` when constructing gridline pens.
9. Done for first chart manual-layout slice: shared `ChartRenderPlanner` now applies bounded factor-mode plot manual layout, legend overlay plot-space behavior, and manual legend item bounds while WPF/Avalonia stay thin.
10. Continue expanding source guards so WPF/Avalonia cannot reintroduce duplicated chart/text/gesture math.

### Phase 2 - Command And Workflow Breadth

Goal: close visible WPF/Avalonia command gaps while keeping semantics shared.

1. Done: add the generated FreeP command matrix and classify gaps as shared, explicit Avalonia gaps, known deferred, platform-only, or command-id aliases.
2. Done for explicit gaps and known-deferred command slices: expand Avalonia ribbon profile so Home clipboard/font/arrange/editing/slides, Insert shapes/tables/charts/links, Transitions, Design, and Animations are shared.
3. Continue replacing WPF stubs for layout and timing controls with shared planners before exposing the same commands in Avalonia. Shared layout-picker choices now include slide-layout ids, display names, layout types, current selection, stable order, master labels, placeholder counts, master-group sections, thumbnail placeholder slots, and selected/disabled chrome consumed by both WPF and Avalonia. The next gap is authoritative PowerPoint visual baselines and deeper thumbnail fidelity, not the shared picker evidence model.
4. Continue from shared backstage/export descriptors into full platform panes and command flows only after the shared planner/executor contract is covered.

### Phase 3 - Visual Fidelity Waves

Goal: reduce measured PowerPoint diff one deck family at a time.

1. Text wave: decks 03, 13, 16, 17, 20.
2. Effects/pictures/custom geometry wave: decks 07, 08, 10, 11, 12, 15.
3. Chart wave: decks 06, 18, 19.
4. SmartArt/modern object wave: decks 09, 14 plus OLE/math fixtures.
5. Each wave must include shared planner/model/IO work first, WPF and Avalonia thin renderer updates second, and render-compare evidence last.

### Phase 4 - Presentation Workflows

Goal: close PowerPoint workflows beyond static slide editing.

1. Done for first presenter state: current/next slide, notes, timer, fullscreen intent, monitor intent, custom-show launch route planning, side-effect-free custom-show authoring summaries, and shared create/rename/delete/membership mutation helpers are shared; richer visible custom-show authoring UI, deeper persistence evidence, and PowerPoint baselines remain open.
2. Done for intent/state and first overlay render planning: recording/timing state, elapsed-time reset, pointer/ink tools, ink color, ink width, renderer-neutral ink overlay primitives for strokes and laser dots, and shared presenter session-summary evidence are shared with thin WPF/Avalonia adapters. Narration/audio capture, camera/media capture, full recording execution, subtitles, richer custom-show workflows, and PowerPoint baseline generation remain deferred.
3. Done for planning boundary: add shared comments/review/accessibility/alt-text workflow descriptors and focused tests.
4. Done for first host adapter seam and first visible alt-text pane UI: expose shared comments-pane, accessibility-summary, alt-text request/pane/apply, and proofing plans through WPF/Avalonia command/state adapters. Persistent shape alt text now round-trips through PowerPoint `cNvPr descr`. Alt-text title and decorative-object metadata now round-trip through `cNvPr title` and `adec:decorative`, and decorative shapes are exempt from missing-alt-text issues. The shared alt-text pane plan now covers title/description field state, validation, decorative toggling, apply/close actions, mutation normalization, and deterministic suggestions derived from picture metadata, table row samples, and chart-data metadata including chart type, series names, category samples, and value counts; WPF and Avalonia render first in-app panes over that plan. Generated AI alt text, rich mention/comment UI, grammar-scale proofing, full accessibility checks, and PowerPoint-authoritative review baselines remain open.
5. Done for first accessibility action slice: missing slide title, missing alt text, and missing hyperlink ScreenTip issues now carry shared action summaries and command hints consumed by WPF/Avalonia adapter tests.
   2026-07-04 follow-up: shared accessibility planning now emits a selectable, no-command diagnostic for blank non-header table body cells, excluding the marked header row so blank header cells stay covered only by the existing header-cell diagnostic. This is narrow accessibility-checker coverage, not full table accessibility parity or a rich remediation UI.
   2026-07-04 follow-up: shared accessibility planning also keeps the missing table-header-row diagnostic keyed to the shared table header metadata, exposes the existing shared Set Header Row action through the same checker rows, and avoids counting candidate header blanks as body-cell issues when no header row is declared.
6. Done for first Avalonia comments pane UI slice and selected-thread planner contract: the Review comments command renders shared-plan-backed comment cards and action buttons in a compact in-app pane, defaults the current-slide comment selection through `PresentationCommentPanePlan`, and proves selected-thread/action enablement in WPF and Avalonia focused tests.
7. Done for reading-order move slice: shared reading-order descriptors and visible WPF/Avalonia panes exist, and enabled Move Earlier/Move Later actions for a single selected top-level shape or sibling child inside a group apply through shared `PresentationReviewWorkflowPlanner` mutation plans and `EditingSession.MoveSelectedShapeInReadingOrder`. Focused shared, WPF, and Avalonia tests cover group-child sibling reorder and boundary disablement. Deeper review UI polish and PowerPoint-authoritative review baselines remain open.
8. Done for shared modern comment Resolve/Reopen/Delete execution: `TryApplyCommentMutationPlan` applies enabled comment mutation plans, and visible/selectable WPF/Avalonia comments panes route Resolve/Reopen/Delete through shared plans before refreshing host state.
9. Done for first modern comment reply-chain/action-state and visible Add/Edit execution slice: `SlideComment` now carries reply rows, `PresentationCommentPanePlan` exposes reply descriptors, reply counts, mention counts, and Reply action enablement, shared Add/Edit mutation plans apply through `PresentationReviewWorkflowPlanner.TryApplyCommentMutationPlan`, and WPF/Avalonia panes render shared reply rows plus visible add/edit inputs with thin host mutation routes. Rich mention UI, full modern comment UI parity, and PowerPoint-authoritative comment baselines remain open.
10. Done for first grammar-style proofing slices: shared proofing now detects lowercase sentence starts at the start of a proofing scope or after `.`, `!`, and `?`, whitespace before punctuation, missing spaces after sentence/list punctuation, and extra interior horizontal spaces, then applies corrections through the existing proofing correction mutation route consumed by WPF and Avalonia panes. Broader grammar/proofing parity remains open.

### Phase 5 - Export, Print, And Backstage

Goal: graduate from text-only export to PowerPoint-shaped output workflows.

1. In progress: promote `PresentationPdfExporter` toward compositor/draw-op-backed slide export. Basic background and shape rectangle draw ops are now included.
2. Done for descriptors: add export descriptors for PDF, image, video, and print. Image sequence export is now implemented through shared selected-slide PNG execution; video remains deferred explicitly.
3. Done for planning: add print layout planning for slides, notes pages, handouts, common slides-per-page options, slide-range normalization, page counts, slide-index mapping, slide slots, and 3-up writing-line areas.
4. Done for shared image execution and first UI flow: selected-slide PNG export normalizes ranges, emits deterministic names, writes atomically, and handles empty decks without invoking host renderers; WPF Backstage and Avalonia ribbon commands now route through native/platform folder pickers into the shared executor.
5. Done for first handout PDF render slice: shared `PresentationHandoutPdfExporter` builds portable PDF draw-op handout pages from the shared layout plan, including thumbnail borders and 3-up writing lines.
6. Done for first notes-page export workflow slice: shared preview planning covers current-slide range, extracted notes text, empty-deck handling, and page geometry; shared notes-page PDF rendering emits portable PDF bytes; WPF Backstage and Avalonia File ribbon commands now route to native save pickers through the shared notes-page PDF export descriptor. PowerPoint-baseline notes-page visual evidence remains deferred.
7. Done for shared print preview planning metadata plus native print and video handoff package planning: full-slide, notes-page, and handout print routes now expose preview plans, including notes overflow continuation pages; printable PDF packages expose shared package-ready print handoff metadata; video frame packages expose shared package-ready video handoff metadata; and WPF/Avalonia project host-deferred print/video state thinly. Actual OS printer adapter execution, native dialog ownership, MP4 encoder/capture backends, richer output UI, compositor/PDF fidelity, and PowerPoint visual baselines remain deferred.
8. Keep OS print dialogs/adapters, video encoding, narration/camera/media capture, and platform pickers host-specific; shared code owns handoff metadata, package readiness, and output intent planning.

## Worker Backlog

| Priority | Worker lane | Owned write scope | First success criteria | Suggested gates |
| --- | --- | --- | --- | --- |
| 1 | PowerPoint reference generation | `tools/FreeP.RenderCompare/**`, checked-in refs or documented external artifact path | Deck refs 06-21 are generated on a PowerPoint COM machine and full-corpus compare can distinguish visual diff from missing baseline. | `tools/FreeP.RenderCompare --corpus-summary`; full render compare on COM-capable machine. |
| 2 | Deep animation workflow fidelity | `FreeP.App.Presentation`, WPF/Avalonia host adapters/tests | Shared animation commands, timeline plans, inline trigger/duration/delay timing mutation plans, supported effect-options mutation plans, Preview/Play From Selected/Play All/Stop playback control descriptors, deterministic playback session plans, and first timed visual slideshow playback execution now drive WPF/Avalonia panes and slideshow windows through shared planner routes. Next slices should add richer advanced effects, recording/timeline authoring, authoritative PowerPoint COM visual baselines, and deeper persistence evidence for advanced timing/effect semantics. | `RibbonTransitionsAnimationsTests`; `PresentationAnimationCommandPlannerTests`; `AnimationPanePlannerTests`; `SlideShowHostPolicySourceTests`; `SlideShowPlaybackPlannerTests`; render/playback fixtures. |
| 3 | Package semantic edit corpus expansion | `freep/FreeP.Core.IO/**`, `freep/FreeP.App.Host.Tests/**`, corpus assertions | Chart workbook package sidecars, loaded-deck `presProps`/`viewProps`/`printerSettings`, presentation-scoped custom XML package parts/rels/content types, and non-sequential multi-chart workbook/externalData relationships are preserved across writer-owned semantic edits. Chart-data semantic edit coverage now regenerates an edited embedded workbook/cache sidecar while preserving unrelated custom XML package parts/rels/content types, the richer authored formula-workbook fixture proves only edited chart ranges are regenerated while an unedited authored formula workbook is preserved byte-for-byte, and `19-chart-labels.pptx` now proves the same edited-only regeneration behavior against a real three-chart corpus deck with neighboring authored `Sheet1!` formulas and sidecars preserved. The current fixture includes two-series authored formulas, quoted sheet names, global/local defined names, nontrivial formula cells, and regenerated ranges for both edited series. Next slices should target broader real PowerPoint-authored workbook formulas/ranges and corpus depth instead of the now-covered source-part multi-chart, in-memory rich-fixture, and chart-labels routes. | Focused `PptxPackageRetentionTests`, `PptxRoundTripTests`, ZIP/OpenXML assertions. |
| 4 | Deep text, math, and placeholder fidelity | `freep/FreeP.App.Presentation/**`, model/IO text defaults, WPF/Avalonia text render adapters/tests | Placeholder inset inheritance, bullet slot placement, paragraph/list-level bullet typography source handling for `buSzPts`, `buSzTx`, `buFontTx`, and `buClrTx`, tab-stop segment placement, runtime normal-autofit line-spacing anchoring, stacked upright glyph placement for `eaVert`, `wordArtVert`, and `wordArtVertRtl`, run-level WordArt/text-run glow and soft-edge metadata/render-pass routing, and shared OMML math layout/rendering including `m:limLow`/`m:limUpp` lower/upper limits, `m:eqArr` stacked equation arrays with direct `m:aln` row alignment points, `m:phant` hidden/reserved metric boxes plus bounded `m:transp` binary, relation, large-operator, and punctuation transparent phantom spacing, skewed `m:fPr/m:type="skw"` fractions, `m:naryPr` hidden lower/upper limit suppression, and `m:radPr/m:degHide` hidden radical degrees are shared and consumed by WPF/Avalonia. Next slices should cover measured bullet typography metrics, broader autofit cases, richer column flow, vertical text beyond the current stacked route, richer run effects, theme/default fonts, WordArt preset/gallery depth, additional equation constructs plus full OfficeMath `m:transp` spacing-table behavior beyond the bounded single-token route, and PowerPoint-backed math/WordArt visual baselines. | `TextLayoutPlannerTests`; `BulletsAutofitTests`; `WordArtTests`; OMML/math render tests; render-compare text/math decks. |
| 5 | Deep chart scene fidelity | `freep/FreeP.App.Presentation/**`, `freep/FreeP.Core.IO/**`, WPF/Avalonia rendering adapters/tests | Axis-title planning, first shared series fill/stroke/marker primitives, authored series solid fill/line/marker package IO including line dash, PowerPoint-authored chart series/point/marker gradient and pattern fill IO plus shared renderer consumption, chart gridline stroke planning, shared major axis tick primitive planning, secondary value-axis label/tick/title primitive planning, chart data-table cell bounds, background fills, modeled solid and gradient border/text style, WPF/Avalonia gridline/tick/secondary-axis/data-table consumption, PPTX data-table fill/border/text style import/export, `c:varyColors` IO plus fallback point-color planning, axis `c:numFmt` IO plus renderer-neutral axis-label format metadata, source-part workbook/externalData retention for non-sequential multi-chart decks, rich authored formula-workbook semantic edit retention, real `19-chart-labels.pptx` chart-data semantic edit package retention, and in-memory scatter/bubble formula retention for series names, X/Y values, and bubble sizes are in place. Next slices should cover broader real PowerPoint-authored workbook formula/range corpus depth, other type-specific chart decisions, and PowerPoint visual baselines. | `ChartRenderPlannerTests`; `RendererNeutralDedupPlannerTests`; `ChartDataCommandTests`; `ChartTests`; selected render-compare decks. |
| 6 | Table editing parity | `FreeP.App.Presentation`, `FreeP.App.Avalonia`, renderer adapters/tests | Shared table-cell state/start/initial-selection/commit/cancel planning is in place for WPF and Avalonia; active table-cell bold/italic/underline formatting now uses shared mutation plans and undo-aware cell-text commands consumed by WPF and Avalonia. Active table-cell paragraph alignment, bullet toggle, numbering toggle, indent/outdent, and bounded list presets for disc bullets plus Arabic/Roman/alpha numbering now route through shared `TableCellEditPlanner` plans with thin WPF/Avalonia command and overlay adapters. Avalonia has active-cell highlight plus a plain-text in-canvas overlay that mirrors the active-cell formatting route, carries rich subrange formatting metadata through the adapter/overlay tests, applies the shared initial selection plan, and consumes the shared post-format rich-text result plan while preserving selection. WPF preserves the native `RichTextBox` selection around formatting commands. Next slices should improve actual rich editor widget fidelity, richer live formatting state, visible list-gallery UI/image bullets, and WPF/Avalonia editing UX depth rather than the now-covered metadata, initial-selection, post-format selection, and bounded preset routes. | `TableCellEditPlannerTests`; `CanvasEditingTests`; `TableEditCommandTests`; `RibbonEditorCompleteness5BTests`; `SlideCanvasGeometryPlannerTests`; `SlideCanvasAvaloniaTests`; `MainWindowHeadlessTests`; WPF/Avalonia table-cell adapter tests. |
| 7 | Presenter recording and ink execution | `FreeP.App.Presentation`, WPF/Avalonia slideshow windows/tests | Recording/timing, pointer/ink intent/state planning, shared overlay render primitives for strokes and laser dots, shared recording-session execution state, shared presenter session-summary planning, shared custom-show launch route planning, and shared custom-show authoring helpers are complete enough for thin WPF/Avalonia adapter and renderer state. The latest recording/custom-show slices model session start/stop, per-slide recording segments, narration/camera capture requests, capture adapter readiness/device descriptors, deferred capture-unavailable actions, recording/ink summary evidence, and create/rename/delete/membership custom-show mutations exposed by both hosts. Next slices should implement real narration/audio capture, camera/media capture backends, captured media persistence, subtitles, richer visible custom-show authoring UI, deeper ink/custom-show persistence workflows, and PowerPoint baseline generation. | `SlideShowHostPlannerTests`; `SlideShowPlaybackPlannerTests`; `SlideShowPresenterToolPlannerTests`; `SlideShowRecordingExecutionPlannerTests`; `SlideShowInkExecutionPlannerTests`; `SlideShowCustomShowPlannerTests`; `SlideShowPresenterSessionSummaryPlannerTests`; WPF/Avalonia slideshow tests; COM-capable PowerPoint baseline runs. |
| 8 | Export/print/backstage depth | `FreeP.App.Presentation`, `FreeP.Core.IO`, WPF/Avalonia host adapters/tests | Existing shared export descriptors, selected-slide PNG execution, WPF/Avalonia folder-picker image export flows, shared print output-option descriptors/package execution planning, WPF/Avalonia print option pane projections, shared handout layout plans, first shared handout PDF draw-op rendering, shared notes-page preview/rendering, WPF/Avalonia notes-page PDF save routes, shared full-slide/notes-page/handout print-preview planning metadata including notes continuation pages, shared native print handoff package metadata with WPF/Avalonia host-deferred routes, shared host-ready printable PDF validation/materialization descriptors, and shared video export handoff package metadata with WPF/Avalonia host-deferred encoder/capture routes are in place. Next slices should implement actual OS/native printer adapters and dialog execution over the now-materialized package, real MP4 encoder/capture adapters, richer output UI, stronger compositor-backed PDF fidelity, and PowerPoint visual baselines. | `PresentationPdfExporterTests`; `PresentationExportPlannerTests`; PDF/render inspection. |
| 9 | Comments/accessibility UI realization | `FreeP.App.Presentation`, WPF/Avalonia host adapters/tests | WPF/Avalonia host adapters expose shared comments-pane selected-thread state/action enablement, reply-chain descriptors/action state, accessibility-summary/action summaries, persistent alt-text request/pane/apply, reading-order pane, top-level and nested group-child reading-order Move Earlier/Move Later mutation, shared modern comment Add/Edit/Resolve/Reopen/Delete/Reply execution, proofing plans, title metadata, decorative-object semantics, normalized alt-text mutation output, deterministic alt-text suggestions derived from image/table metadata, shared text-hyperlink ScreenTip and unclear-display-text issue detection, shared chart-title-missing diagnostics, shared low-quality alt-text checks for filename-like and duplicate descriptions, and shared video-without-captions diagnostics. Avalonia has rendered comments-pane evidence over the shared comment plan, WPF/Avalonia have visible/selectable comments panes routing Add/Edit/Resolve/Reopen/Delete/Reply through shared mutation plans, and WPF/Avalonia have first visible alt-text, reading-order, hyperlink, chart-title, low-quality-alt-text, and media-caption accessibility rows over shared plans. Shared proofing now includes adjacent repeated-word detection, single-word correction planning, sentence-start capitalization correction, whitespace-before-punctuation cleanup, and missing-space-after-sentence-punctuation cleanup in addition to dictionary typo corrections. Media caption/subtitle track metadata is now modeled, read from PowerPoint-style relationships, emitted back into PPTX packages when usable internal bytes or external targets exist, and parsed into shared WebVTT/SRT transcript plans when authored caption bytes are available, so captioned videos are not flagged as missing captions and hosts have renderer-neutral transcript evidence. Next slices should deepen those panes and add richer generated alt-text suggestions, rich mention/comment UI, broader grammar-scale proofing, broader accessibility checks beyond these initial issue types, real audio transcription/capture-generated transcripts, and PowerPoint-authoritative review baselines without duplicating policy. | `PresentationReviewWorkflowPlannerTests`; `ReviewWorkflowAdapterTests`; `MainWindowHeadlessTests`; `PptxRoundTripTests`; `MediaFieldsTests`; `PresentationMediaTranscriptPlannerTests`; focused WPF/Avalonia adapter tests. |

## Validation Strategy

For report-only updates, run docs/preflight gates. For implementation workers, prefer focused tests plus a serialized build because this repo has had shared-output and WPF concurrency flakes.

Recommended gates by lane:

- Docs/report lane: `git diff --check`; `powershell -ExecutionPolicy Bypass -File tools\Test-GeneratedDocs.ps1`; `powershell -ExecutionPolicy Bypass -File tools\Test-RepositoryPreflight.ps1`.
- Shared planner/code lane: focused project tests plus `dotnet build FreeP.slnx --configuration Release -m:1 /nr:false`.
- Host/render lane: focused WPF/Avalonia tests plus render harness smoke.
- PowerPoint visual lane: run `tools/FreeP.RenderCompare` on a PowerPoint COM machine; do not claim authoritative parity from this machine.

2026-07-04 presenter recording delta: shared `SlideShowRecordingExecutionPlanner` models recording-session start/stop, slide enter/leave, per-slide recording segments, narration/camera capture requests, and deferred capture-unavailable actions. WPF and Avalonia slideshow windows expose the same `RecordingExecutionState` and `RecordingExecutionActions`, so record-with-narration/camera intent now advances through host adapters even while real microphone/camera capture and captured-media persistence remain deferred.

2026-07-04 math delta: shared OMML parsing/layout now carries `m:eqArr` as a `MathNode.EqArray` with ordered row expressions plus invisible direct `m:aln` row alignment-point metadata. `MathLayoutEngine` lays equation arrays out as vertically stacked rows and shifts marked rows so their alignment points share the same x-coordinate while unmarked rows retain centered behavior. WPF and Avalonia consume the resulting neutral `MathBox` tree through their existing render recursion; PowerPoint COM visual baselines remain deferred.

2026-07-04 presenter recording artifact-contract delta: completed shared recording segments now carry deterministic narration/audio and camera/video artifact descriptors, including suggested filenames, content types, captured/deferred state, and host-specific deferred reasons. This gives WPF and Avalonia the same evidence contract for future capture backends without claiming real microphone/camera capture, captured-media persistence, subtitles, custom shows, or PowerPoint COM visual baselines.

2026-07-04 custom-show launch delta: `SlideShowCustomShowPlanner` now builds shared custom-show launch plans with route choices for named custom shows plus All Slides fallback, disabled-state messages for missing or empty custom shows, and WPF/Avalonia slideshow-window consumption through thin route descriptors. Custom-show authoring, deeper persistence, and PowerPoint visual baselines remain deferred.

2026-07-04 custom-show authoring delta: `SlideShowCustomShowPlanner` now also builds side-effect-free authoring summaries for existing shows and available slides, validates non-empty unique trimmed names, allocates the next available custom-show id, normalizes missing slide ids during explicit mutations, and applies shared create/rename/delete/slide-membership updates. WPF and Avalonia expose thin host adapter methods over the same shared planner. Rich visible authoring dialog parity, deeper persistence evidence, and PowerPoint visual baselines remain deferred.

2026-07-04 presenter session-summary delta: `SlideShowPresenterSessionSummaryPlanner` now combines recording-session active/completed state, per-slide segment/artifact counts, deferred capture reasons, committed ink stroke counts, active ink state, and laser pointer visibility into a shared summary consumed by WPF and Avalonia slideshow windows. Real narration/audio/camera capture, captured-media persistence, subtitles, and PowerPoint baselines remain deferred.

2026-07-04 presenter command/evidence inventory delta: `tools/Generate-FreePCommandParityInventory.ps1` now emits workflow-depth evidence rows for presenter recording execution, presenter ink execution, and presenter session-summary parity alongside the command matrix. The generated dashboard consumes those rows, so FreeP remains at 0 actionable WPF/Avalonia command gaps while remaining presenter work is narrowed to real capture backends, deeper ink/custom-show persistence workflows, subtitles/transcript evidence, and PowerPoint-authoritative baselines.

2026-07-04 modern comments delta: modern comment and reply author identity metadata is now preserved from PowerPoint-authored packages through the shared model, review pane descriptors, clone/mutation paths, and PPTX write-back. Imported `p188:cm` ids, reply ids, author ids, `userId`, and `providerId` values survive save/reopen, while new FreeP-authored reviewers still receive deterministic generated identities. This improves the comments/review persistence contract without claiming rich mention insertion UI, people-picker integration, cloud coauthor identity, or PowerPoint-authoritative review baselines.

2026-07-04 math delta: shared OMML parsing/layout now carries transparent `m:box` wrappers and visible `m:borderBox` wrappers through `MathNode.Box`/`MathNode.BorderBox`, preserving nested fraction/radical children instead of flattening them to fallback text. `m:borderBoxPr` side visibility maps `hideTop`, `hideBot`, `hideLeft`, and `hideRight` into shared visible-side line operations, and `strikeH`, `strikeV`, `strikeBLTR`, and `strikeTLBR` now emit the matching horizontal, vertical, and diagonal strike lines inside the same padded box. Padding/metrics stay owned by `MathLayoutEngine` and `MathBoxRenderPlanner`; WPF and Avalonia only draw the neutral `MathDrawOp.DrawLine` operations. PowerPoint COM visual baselines remain deferred.

2026-07-04 math delta: shared OMML parsing/layout now carries `m:sPre` as `MathNode.PreSubSup`, preserving nested base/sub/sup expressions and using empty unknown script fallbacks when `m:sub` or `m:sup` is absent. `MathLayoutEngine` places the reduced-size pre-superscript/pre-subscript stack to the left of the base, preserves the base baseline, and reports metrics that contain tall bases and scripts without clipping. WPF and Avalonia continue to consume the neutral `MathBox` tree through existing recursion; PowerPoint COM math visual baselines and broader equation alignment semantics remain deferred.

2026-07-04 math delta: shared OMML parsing/layout now carries `m:phant` as `MathNode.Phantom`, preserving nested `m:e` math instead of flattening to fallback text. Hidden phantoms reserve natural metrics without emitting child draw operations, while `zeroWid`, `zeroAsc`, and `zeroDesc` can remove reported width/ascent/descent from the neutral `MathBox` metrics. The `m:transp` flag now contributes bounded deterministic row spacing for transparent phantoms containing simple binary or relation operator runs; broader spacing-class behavior and PowerPoint COM math visual baselines remain deferred.

2026-07-04 math delta: shared OMML skewed fractions now keep `m:fPr/m:type="skw"` distinct from linear slash fractions. `MathLayoutEngine` lays numerator and denominator out as offset child boxes around a renderer-neutral diagonal `MathBox.Line`, and WPF/Avalonia consume the resulting shared `MathDrawOp.DrawLine` route without local renderer semantics. PowerPoint COM math visual baselines remain deferred.

2026-07-04 math delta: shared OMML matrix handling now preserves `m:mPr/m:mcs/m:mc/m:mcPr/m:aln` per-column alignment metadata with center as the default, lays out ragged `m:m` rows using the maximum row cell count so later-row cells are not dropped, and aligns each cell left/center/right inside its computed column width. WPF and Avalonia renderers continue to consume the neutral `MathBox` plan through thin `MathBoxRenderPlanner` recursion; complete equation alignment semantics and PowerPoint COM visual baselines remain deferred.

2026-07-06 math delta: shared OMML delimiter parsing/layout now honors `m:dPr/m:grow` as a CT_OnOff value. Absent/on values keep the existing auto-sized delimiter behavior, while explicit off/false/0 values keep delimiter glyphs at normal height without clipping tall inner expressions. WPF and Avalonia continue to consume the same neutral `MathBox.Bracket` plan; PowerPoint COM math visual baselines remain deferred.

2026-07-06 math delta: shared OMML accent and bar evidence now covers `m:acc` default/explicit accent characters, `m:bar` default overlines and bottom-position underlines, shared placement in `MathLayoutEngine`, and Avalonia rendering through the same `MathBoxRenderPlanner` glyph and horizontal-rule plan consumed by WPF. PowerPoint COM math visual baselines remain deferred.

2026-07-13 math delta: shared OMML function-application evidence now covers `m:func` / `m:fName` upright function-name normalization, argument styling, and deterministic name-to-argument spacing in the neutral `MathBoxRenderPlanner` glyph plan consumed by both WPF and Avalonia. PowerPoint COM math visual baselines and broader measured OfficeMath spacing remain deferred.

2026-07-04 accessibility delta: `PresentationReviewWorkflowPlanner` now flags blank table body/data cells as shared `PresentationAccessibilityCheckerPanePlan` rows with no remediation command, requiring only shape selection/navigation through the existing pane behavior. The check skips the marked header row so blank header cells continue to be reported only by the existing blank-header diagnostic. This improves one PowerPoint accessibility-checker parity slice without claiming full accessibility checker parity, PowerPoint-authoritative review baselines, rich remediation UI, or complete table accessibility parity.

2026-07-04 accessibility delta: `PresentationReviewWorkflowPlanner` keeps missing table header rows as shared accessibility diagnostics backed by `TableShape.Flags.FirstRow`, projects them through the existing WPF/Avalonia checker-pane routes, and preserves the existing shared Set Header Row remediation. Headerless tables no longer count blank candidate-header cells as body blanks, while declared header rows with blank cells remain covered by the existing blank-header-cell diagnostic.

2026-07-04 accessibility delta: `PresentationReviewWorkflowPlanner` now flags chart shapes without chart titles as shared warning rows categorized as Chart, shape-selectable, and labeled with an Add Chart Title action but no mutation command. WPF and Avalonia checker panes consume the same shared issue row; broader chart accessibility checks and PowerPoint-authoritative review baselines remain deferred.

2026-07-04 accessibility delta: `PresentationReviewWorkflowPlanner` now flags unclear hyperlink display text as shared warning rows categorized as Hyperlink, covering conservative generic phrases such as "click here" plus raw URL display text in normal shape text and table-cell text. WPF and Avalonia checker panes consume the same selectable rows and shared Edit Hyperlink action; broader hyperlink authoring and full accessibility-checker parity remain deferred.

2026-07-04 proofing delta: shared proofing now flags whitespace before punctuation and missing spaces after sentence punctuation, proposes deterministic text replacements, and applies those replacements through the existing proofing correction mutation route consumed by WPF and Avalonia panes. Decimal numbers, URLs, `www` addresses, and email/mailto tokens are guarded so common non-sentence punctuation is not overcorrected. Broader grammar/proofing parity remains deferred.

2026-07-05 proofing delta: shared proofing now flags extra interior horizontal spaces in slide text, speaker notes, comments, and replies when a run of two or more spaces/tabs is bounded by non-whitespace text on both sides. The suggested correction replaces the run with one literal space through the existing WPF/Avalonia proofing mutation route while preserving line-start indentation, trailing whitespace, and newline boundaries.

2026-07-04 native print handoff delta: `PresentationNativePrintHandoffPlan` now describes printable PDF package readiness, suggested temporary package names, page/content/layout/range/options summaries, no-slide handling, and host capability decisions in shared code over `PresentationPrintOutputPackagePlan`. Follow-up execution descriptor work validates host-ready PDF package bytes, records suggested document/job names, and materializes the printable package atomically for host handoff. WPF and Avalonia expose thin host-deferred adapters so UI/status no longer claims a real native print dialog is available on this machine. Actual OS print adapters, native dialog ownership, and PowerPoint print visual baselines remain deferred.

2026-07-04 OMML n-ary delta: shared OMML parsing now honors `m:naryPr/m:subHide` and `m:supHide`, so hidden lower/upper limits are omitted before the neutral `MathNode.Nary` reaches layout and renderer-neutral draw-op planning. WPF and Avalonia keep consuming the same shared math render plan; PowerPoint COM math baselines and broader equation construct coverage remain deferred.

2026-07-04 video export handoff delta: `PresentationVideoExportHandoffPlan` now describes shared video frame package readiness, host MP4 encoder capability, narration/camera/media capture capability, and host-deferred reasons over `PresentationVideoFramePackagePlan`. WPF and Avalonia expose thin host-deferred adapters/status over the same shared plan. Real MP4 encoding, capture backends, and PowerPoint video export baselines remain deferred.

2026-07-04 accessibility delta: `PresentationReviewWorkflowPlanner` now flags non-decorative video media without closed captions or subtitles as shared Media accessibility rows with a selectable read-only Select Media action label. WPF and Avalonia checker panes consume the same shared diagnostic. Follow-up caption-track work adds shared `MediaCaptionTrackInfo` metadata, reads relationship-backed PowerPoint caption/subtitle tracks from slide parts, and suppresses the missing-caption diagnostic for captioned videos. Caption authoring/emission and PowerPoint-authoritative accessibility baselines remain deferred.

2026-07-03 delta: shared runtime autofit line-spacing anchoring, shared chart data-table cell-bounds planning, chart data-table fill fidelity, chart data-table border/text style evidence including gradient border stroke rendering, shared print output-option planning, shared animation pane playback descriptors, deterministic animation playback session planning, visible Avalonia print-options pane projection, Avalonia table-cell rich subrange formatting metadata routing, source-part-based multi-chart workbook/externalData retention, deterministic alt-text suggestion planning, shared text-hyperlink ScreenTip issue detection, shared print preview planning, OMML limit parsing/layout through shared `MathNode.Limit`/`MathLayoutEngine`, richer authored chart workbook formula-retention coverage, authored chart series solid fill/line/marker import/export including line dash, authored chart series/point/marker gradient and pattern fill import/export plus shared WPF/Avalonia renderer consumption, shared slideshow ink overlay render primitives, shared table-cell initial selection planning, and shared reading-order group-child sibling mutation evidence are integrated and validated on `origin/main` through merge `6295865a2` plus follow-up slices. A follow-up 2026-07-03 execution-evidence pass confirms first WPF/Avalonia timed visual animation playback execution is already present through `SlideShowPlaybackPlanner.PlanAnimationStep` and concrete host effect helpers. Remaining text/autofit/math work is broader PowerPoint-measured typography, overflow coverage, broader equation constructs, and PowerPoint math visual baselines; remaining chart work is broader PowerPoint-authored workbook formulas/ranges, type-specific chart decisions, corpus depth, and visual-baseline comparison, not the now-covered authored series solid fill/line/marker IO, series/point/marker gradient or pattern fill IO/rendering, data-table fill, solid and gradient border/text styling, source-part multi-chart retention, or in-memory rich formula-retention routes; remaining comments/accessibility work is richer generated alt-text suggestions, rich mention/comment UI, broader grammar-scale proofing beyond first sentence-start and punctuation-spacing coverage, broader accessibility checks, and PowerPoint-authoritative review baselines, not the now-covered top-level or group-child reading-order move route; remaining print/export work is actual OS/native print adapters, richer output UI, video, stronger compositor/PDF fidelity, and PowerPoint visual baselines, not the now-covered shared native print handoff package metadata route; remaining animation/presenter work is advanced effect variants, recording/timeline authoring, narration/audio/video capture, subtitles/richer custom-show workflows, deeper timing/effect persistence evidence, full PowerPoint visual baselines, and COM baselines, not the now-covered first timed slideshow animation execution or shared ink overlay render primitive route; remaining table editing work is richer editor fidelity, not the initial selection or Avalonia subrange-format metadata routes.

2026-07-04 delta: run-level WordArt/text-run `a:rPr/a:effectLst/a:glow` and `a:softEdge` are now modeled separately from shape effects, round-trip through PPTX IO, resolve through shared `ResolvedRun` state, and render through shared `TextRunEffectRenderPlanner` passes consumed by thin WPF/Avalonia switch cases. Visual output is a conservative geometry/opacity approximation pending PowerPoint COM baselines; this does not close broader WordArt preset/gallery, typography, autofit, vertical text, or shape-effect parity.

2026-07-04 slide-pane thumbnail evidence delta: `tools/FreeP.RenderCompare --slide-pane-thumbnail-compare` now creates a focused WPF/Avalonia/PowerPoint thumbnail evidence lane with deterministic 320x180 render outputs, slide-pane target-size metadata, paired diff folders, and explicit `n/a` PowerPoint rows when COM is unavailable. This improves PowerPoint-baseline readiness for thumbnail bitmap comparison without changing the already-merged thumbnail chrome or section-header chrome; authoritative PowerPoint thumbnail baselines still require a COM-capable machine.

2026-07-04 native print execution delta: `PresentationPrintOutputPackageExecutor` now builds a shared `PresentationPrintOutputPackageExecutionDescriptor` over printable PDF packages, validating byte count, PDF header/EOF markers, package plan readiness, content type, extension, suggested document name, suggested print job name, and materialization eligibility. `MaterializePackageForHandoff` writes the host-ready package atomically. WPF and Avalonia store the same descriptor and continue to defer the actual native print dialog to future host adapters.

2026-07-04 table rich-edit delta: `TableCellEditPlanner` now preserves/clamps effective table-cell rich-edit selections, exposes selected run ranges, and returns post-format rich-text plans with the same selection metadata. Avalonia applies the shared result plan directly to its in-canvas table-cell overlay; WPF preserves the native `RichTextBox` selection around bold/italic/underline/font/color commands. Richer platform editor-widget parity remains open.

2026-07-04 table paragraph-format delta: `TableCellEditPlanner` now plans active table-cell paragraph alignment, bullet toggle, numbering toggle, bounded list presets, and indent/outdent in shared code, including disabled states, merged-cell normalization, selection-aware paragraph targeting, undoable `SetTableCellTextCommand` output, and result rich-edit metadata. `EditingSession`, WPF ribbon commands, and Avalonia command/overlay adapters consume those shared routes for left/center/right/justify alignment, bullets, basic Arabic-period numbering, upper/lower Roman numbering, upper/lower alpha numbering, and indentation. Visible list galleries, image bullets, and richer visible list controls remain deferred.

2026-07-04 accessibility delta: `PresentationReviewWorkflowPlanner` now flags filename-like alt text and duplicate normalized alt text reused by multiple non-decorative objects as shared Alt text accessibility rows. WPF and Avalonia checker panes select the same shape and open the same shared alt-text pane route; generated AI alt text, richer remediation UI, and broader accessibility-checker parity remain deferred.

2026-07-04 animation execution delta: `SlideShowPlaybackPlanner` now maps imported Split, Random Bars, Grow, and Shrink animation presets to renderer-neutral slideshow playback effect plans. WPF and Avalonia slideshow hosts consume the same shared effect kinds/timing/direction/scale metadata through thin effect helpers, adding deterministic split/reveal/bar/scale approximations beyond the earlier Appear/Fade/Fly In/Wipe/Zoom/Pulse/Spin/Motion Path route. Exact PowerPoint random-bar segmentation, richer advanced effect families, and PowerPoint visual baselines remain deferred.

2026-07-04 media caption-track delta: media caption/subtitle tracks are now represented in shared `MediaCaptionTrackInfo` metadata and read from PowerPoint-style slide relationships/metadata, including relationship id, target/source, content type, language, label, bytes for internal tracks, and external URI state. The shared accessibility planner now treats modeled caption tracks as evidence that a video is captioned, so WPF and Avalonia no longer show the missing-caption warning for captioned videos. Follow-up writer work emits modeled tracks as PPTX caption relationships, metadata, content types, and internal caption parts when bytes are available, while preserving external caption targets without embedding bytes. Follow-up transcript work adds `PresentationMediaTranscriptPlanner`, which parses authored internal WebVTT/SRT caption bytes into a renderer-neutral transcript plan with cue timing/text and status descriptors for unsupported, external, and byte-less tracks; WPF and Avalonia expose the same shared plan from their review/accessibility refresh paths. Real audio transcription and capture-generated transcripts remain open.

2026-07-04 comment mention delta: `PresentationReviewWorkflowPlanner` now emits renderer-neutral mention descriptors for comment roots and replies, including offsets, labels, display text, and normalized identity keys. WPF host-adapter tests cover the shared descriptor flow, while rich mention insertion UI, people-picker integration, notification routing, coauthor presence, and PowerPoint-authoritative baselines remain deferred.

2026-07-04 chart delta: `ChartShape.VaryColors` now preserves OOXML `c:varyColors` decisions across bar, line, pie, area, scatter, doughnut, radar, and bubble chart type elements. Shared chart planning uses that decision to vary fallback point colors for pie/doughnut/bubble/single-series cases while preserving explicit series and point fills. Follow-up axis number-format work carries `c:numFmt` format codes and source-linked flags through category, value, scatter/bubble X, and secondary value axes into renderer-neutral axis-label format metadata without duplicating policy in WPF/Avalonia. Real corpus assertions cover `06-charts.pptx`, `18-chart-types.pptx`, and `19-chart-labels.pptx`; full Excel-style number/date formatting evaluation remains deferred.

2026-07-04 math delta: shared OMML radical parsing now treats `m:radPr/m:degHide` as a CT_OnOff value, so bare/on/true values hide the radical degree and explicit off/false values preserve it. The hidden degree is removed before `MathBoxRenderPlanner` reaches WPF or Avalonia, keeping renderer behavior thin and shared.

Current integration stance: FreeP has a substantial shared codebase, generated command inventory with zero actionable WPF/Avalonia missing commands, zero explicit Avalonia command gaps, zero known-deferred command slices, and zero command-id aliases, 21-deck package-retention contracts plus first semantic-edit corpus coverage for media, charts, SmartArt, comments, and notes, preserved chart workbook package sidecars for matching chart counts, source-part-based multi-chart workbook/externalData retention, chart data-table fill/border/text-style import/export/rendering, authored chart series solid fill/line/marker import/export including line dash plus gradient/pattern fills for chart series, points, and markers, chart-data semantic regeneration of edited workbook/cache sidecars, richer authored formula workbook fixture retention that preserves the unedited authored workbook byte-for-byte, retained `presProps`/`viewProps`/`printerSettings`, trustworthy render-harness failure reporting, initial shared chart/text/gesture planners, shared chart axis-title layout/orientation, series fill/stroke/marker planning, chart gridline stroke planning, major axis tick primitive planning, secondary value-axis label/tick/title planning, chart axis number/date format metadata planning, and chart data-table bounds/background-fill/solid-and-gradient-border/text-style planning consumed by WPF and Avalonia renderers, shared placeholder text inset inheritance, shared bullet slot placement and tab-stop segment placement consumed by WPF and Avalonia, shared run-level glow/soft-edge WordArt/text-run effect metadata plus approximate render-pass routing consumed by WPF and Avalonia, shared OMML math layout/rendering including `m:limLow`/`m:limUpp` limit boxes, `m:eqArr` stacked equation arrays with direct `m:aln` row alignment points, and `m:naryPr` hidden lower/upper limit suppression consumed by WPF and Avalonia, shared slide-pane action planning plus Avalonia drag-reorder depth evidence, shared table-cell edit state/start/initial-selection/commit/cancel planning consumed by WPF and Avalonia with an Avalonia plain-text in-canvas overlay, shared active table-cell bold/italic/underline mutation planning and paragraph alignment consumed by WPF and Avalonia, Avalonia rich subrange formatting metadata routing through the table-cell edit adapter/overlay path, shared presenter/slideshow state plus recording/timing, pointer/ink intent planning, slideshow ink overlay render primitives and presenter session-summary planning consumed by WPF and Avalonia, shared custom-show launch and first custom-show authoring helper routes consumed by thin WPF/Avalonia adapters, and first timed slideshow animation playback execution through shared `SlideShowPlaybackPlanner` plans consumed by WPF/Avalonia effect helpers including deterministic Split, Random Bars, Grow, and Shrink approximations, shared export/backstage planning with WPF/Avalonia PDF and notes-page PDF export routes, shared selected-slide PNG image export execution with thin WPF/Avalonia render/folder-picker UI flows, shared export/print depth descriptors for video/slide/notes/handout workflows, shared notes-page preview planning and PDF rendering consumed by WPF and Avalonia, shared PowerPoint-style handout page layout planning consumed by WPF and Avalonia, shared handout PDF draw-op rendering, shared print preview planning for full-slide, notes-page, and handout routes including notes continuation pages, shared native print handoff package metadata with WPF/Avalonia host-deferred adapters, shared video export handoff package metadata with WPF/Avalonia host-deferred encoder/capture adapters, shared transition, Design, layout picker, and Animations command intents exposed through WPF/Avalonia, a visible shared-plan-backed WPF/Avalonia layout picker gallery with master grouping, thumbnail placeholders, current-choice chrome, and layout application, shared animation pane/timeline state plus inline trigger/duration/delay, supported effect-options mutation plans, playback controls, and deterministic playback session plans with visible WPF/Avalonia pane controls over `AnimationPaneTimelinePlan`, a shared comments/review/accessibility workflow planning boundary with accessibility issue action summaries, shared selected-comment pane state, and shared reply-chain descriptors/action state consumed by WPF/Avalonia, visible/selectable WPF/Avalonia comments panes whose Add/Edit/Resolve/Reopen/Delete/Reply routes apply through shared `PresentationReviewWorkflowPlanner` mutation plans with post-mutation selection normalization, persistent shape alt-text model/IO through PowerPoint `cNvPr descr`, `cNvPr title`, and `adec:decorative`, shared alt-text pane field/action/validation/mutation/suggestion planning, shared text-hyperlink ScreenTip issue detection, shared dictionary/repeated-word/sentence-start/punctuation-spacing proofing, shared video-caption-missing accessibility diagnostics, first visible WPF/Avalonia alt-text panes, and first visible WPF/Avalonia reading-order panes with shared top-level and group-child Move Earlier/Move Later mutation. It is not PowerPoint-parity complete. The next work should generate missing PowerPoint refs on a COM-capable machine, then continue deeper shared planner work for text typography/autofit/WordArt, additional equation constructs and remaining alignment semantics plus PowerPoint-measured math baselines, broader PowerPoint-authored chart workbook formula/range parity and corpus depth, type-specific chart decisions, actual OS/native print adapters and dialog execution, real MP4/capture adapters, richer output UI, richer compositor/PDF fidelity, richer table-cell editor fidelity including bullets and indent/outdent, deeper PowerPoint-measured layout thumbnail rendering, richer/PowerPoint-exact advanced animation effect variants, recording/timeline authoring, narration/audio/video capture, subtitles/caption-track authoring, richer custom-show workflows, richer comments/review UI including rich mention/comment UI, richer generated alt-text suggestions, broader grammar/proofing, broader accessibility checks, and PowerPoint-authoritative review baselines.

Latest 2026-07-07 update: shared OMML transparent phantom spacing now covers bounded single-token binary, relation, large-operator, and punctuation spacing classes, skewed OMML fractions now emit shared diagonal-line math layout, hidden OMML n-ary lower/upper limits and hidden radical degrees are suppressed through shared parsing/layout, custom-show launch routes and first shared custom-show authoring helpers now have WPF/Avalonia consumption, presenter session summaries now combine recording and ink evidence in shared code, shared native print and video export handoff planning now describe package readiness plus host-deferred WPF/Avalonia routes, printable native-print packages now have shared execution descriptors and atomic materialization, table-cell rich editing now preserves shared post-format selections/result plans, active table-cell paragraph alignment/bullets/numbering/indent now route through shared undoable plans, comment roots/replies now expose shared mention descriptors, chart `varyColors` decisions now round-trip and feed fallback point-color planning, chart axis `c:numFmt` metadata now round-trips and feeds shared axis-label format plans, caption tracks now read and emit package metadata/parts, authored WebVTT/SRT caption bytes now feed a shared transcript plan, shared slideshow playback now covers deterministic Split, Random Bars, Grow, and Shrink approximations in both WPF and Avalonia, and shared accessibility/proofing planning now emits chart-title-missing, unclear-hyperlink-text, caption-track-aware video-caption, sentence/list punctuation-spacing diagnostics, and merged/split-table diagnostics consumed by WPF and Avalonia checker/proofing panes. These reduce the remaining gap map but do not remove the need for full OfficeMath spacing-table typography, broader grammar semantics, actual OS/native print adapters, real MP4/capture adapters, richer visible slideshow/custom-show authoring, exact/richer advanced animation behavior, list galleries/presets/image bullets, rich mention insertion UI, real audio transcription/capture-generated transcripts, richer accessibility/proofing checks, or PowerPoint-authoritative baselines.

Latest 2026-07-13 chart delta: bar and column chart `c:gapWidth` and `c:overlap` now round-trip through the FreeP chart model, package reader/writer, and slide cloner, and the shared `ChartRenderPlanner` uses those values for both bar/column primitives and matching data-label bounds. WPF and Avalonia continue to consume renderer-neutral chart geometry rather than host-local spacing policy. PowerPoint-authoritative visual baselines, 3-D bar/column spacing, and broader type-specific chart visual fidelity remain deferred.

Latest 2026-07-05 update: shared proofing now also flags extra interior horizontal spaces in slide text, speaker notes, comments, and replies, with WPF and Avalonia consuming the same single-space correction route through the existing proofing pane mutation flow.

Latest 2026-07-05 update: shared caption-track authoring now supports create, replace, and delete operations for internal media caption tracks through `PresentationMediaTranscriptPlanner`. The shared route accepts typed cue descriptors or authored WebVTT/SRT transcript text, validates non-overlapping cue timing and internal package sources, emits deterministic `text/vtt` bytes into `MediaCaptionTrackInfo`, preserves external caption-track metadata as link-only state, and keeps WPF/Avalonia consumption on the existing shared transcript/accessibility/package paths. Real audio transcription, capture-generated transcripts, and rich visible caption-authoring UI remain deferred.

Latest 2026-07-05 update: visible custom-show authoring is now exposed in both WPF and Avalonia through a shared-command “Custom Shows” slideshow surface. The new thin host dialogs list authored custom shows, show slide counts, create named shows from checked deck-order slide rows, rename shows, update slide membership, delete shows, and start non-empty custom shows through the existing shared `SlideShowCustomShowPlanner` mutation/launch routes. Rich drag-reorder authoring, deeper PowerPoint workflow polish, and PowerPoint-authoritative custom-show visual baselines remain deferred.

Latest 2026-07-13 update: custom-show reorder authoring now has a shared mutation route. `SlideShowCustomShowPlanner.MoveCustomShowSlide` validates the selected custom-show slide occurrence by index and slide id, clamps target indexes deterministically, preserves duplicate slide ids by moving the selected occurrence, and returns the post-move selected index for thin dialogs. WPF and Avalonia expose matching thin adapter methods and visible Custom Shows dialog Move Up/Move Down controls over that shared route. Drag-reorder polish, richer custom-show workflow polish, and PowerPoint-authoritative custom-show baselines remain deferred.

Latest 2026-07-13 slide-pane update: slide-pane section context workflows now have focused WPF and Avalonia host evidence over the shared `SlideSectionPlanner` execution route. Add Section, Rename Section, and Remove Section are exercised through thin host adapters while preserving PowerPoint-style section removal semantics that keep slides in the deck. Richer PowerPoint slide-pane polish, deeper thumbnail visual baselines, and COM-backed PowerPoint references remain deferred.

Latest 2026-07-13 rich-editing update: shared table-cell rich-edit plans now expose a selected-list state that resolves visible bullet/numbering gallery presets, picture bullets, custom list formatting, and mixed list selections from the selected paragraph set. Avalonia post-format overlay refresh evidence now verifies that applying a visible numbering preset returns the same shared preset metadata as the ribbon gallery/catalog route. WPF and Avalonia remain on thin host adapters over the shared table-cell planner; richer editor-widget polish and PowerPoint-authoritative list/baseline comparisons remain deferred until a COM-capable machine is available.

Latest 2026-07-13 proofing update: shared proofing now flags repeated terminal punctuation in slide titles, shape text, table cells, speaker notes, comments, and replies. The diagnostic suggests deterministic single-mark replacements through `PresentationReviewWorkflowPlanner.TryApplyProofingCorrection`; broader grammar-scale proofing and PowerPoint-authoritative review baselines remain deferred.

Latest 2026-07-05 update: shared slideshow playback planning now maps imported PowerPoint `Box` animation presets to a renderer-neutral Box effect with deterministic center-out and center-in rectangle-clip semantics derived from the existing direction/kind metadata. WPF and Avalonia slideshow windows consume the same shared effect kind through thin clip animation helpers. Exact PowerPoint mask timing/geometry and broader advanced effect families remain deferred until PowerPoint visual baselines are available.

Latest 2026-07-05 update: shared slideshow playback planning now also maps imported PowerPoint `Blinds` animation presets to a renderer-neutral Blinds effect with deterministic horizontal/vertical stripe orientation derived from the existing animation direction metadata. WPF and Avalonia slideshow windows consume the same shared effect plan through thin stripe-mask helpers. Exact PowerPoint stripe count, easing, and visual baselines remain deferred.

Latest 2026-07-05 update: shared slideshow playback planning now maps imported PowerPoint `Checkerboard` animation presets to a renderer-neutral checkerboard effect with deterministic grid dimensions and horizontal/vertical reveal orientation derived from the existing animation direction metadata. WPF and Avalonia slideshow windows consume the same shared effect plan through thin checker-mask helpers. Exact PowerPoint grid segmentation, phase timing, easing, and visual baselines remain deferred.

Latest 2026-07-05 update: shared slideshow playback planning now maps imported PowerPoint `Diamond` animation presets to a renderer-neutral geometric-mask effect with deterministic centered diamond expand/collapse semantics derived from existing direction/kind metadata. WPF and Avalonia slideshow windows consume the same shared mask plan through thin path-clip helpers. Exact PowerPoint mask geometry, easing, and visual baselines remain deferred.

Latest 2026-07-05 update: shared presenter recording capture now has host-neutral microphone/camera device descriptors and capture adapter readiness contracts consumed by the existing recording execution backend. WPF and Avalonia slideshow windows expose their current deferred microphone/camera adapter readiness through the same shared shape, while focused tests cover fully ready, partially ready, deterministic, and host-deferred routes. Real OS device enumeration/capture, captured media persistence, and PowerPoint-native recording baselines remain deferred.
