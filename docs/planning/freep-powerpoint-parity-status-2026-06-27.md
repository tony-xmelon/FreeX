# FreeP PowerPoint Parity Status - Updated 2026-07-01

Status owner: FreeP parity orchestration. This report supersedes the 2026-06-27 pre-dedup FreeP snapshot. It is still a worker handoff and implementation plan, not a claim of full Microsoft PowerPoint parity.

Branch/worktree context for this update: `codex/freep-parity-report-accessibility-refresh-20260701`, based on `origin/main` commit `bc9d88092`. This update reflects the 2026-07-01 FreeP parity implementation wave through the integrated table-cell editing planner, Avalonia table-cell overlay, presenter recording/ink intent, image export execution, review workflow adapter, text placeholder fidelity, shared chart axis-title, chart series-style, chart gridline-style planning, bullet placement, and tab-stop planning, image export UI-flow, persistent alt-text, animation pane/timeline planning, shared layout-picker choices and evidence, shared handout layout planning, shared handout PDF render planning, shared accessibility issue action summaries, and the first alt-text title/decorative-object metadata slice.

## Current Evidence

- Primary repo root: `C:\Users\anton\OneDrive\Documents\FreeX\FreeX`.
- This report was refreshed from the current FreeP parity integration wave; local `main` may lag because integration is happening through isolated worktrees pushed to `origin/main`.
- `AGENTS.md` still requires isolated worktrees, no feature edits in `main`, frequent sync, subagents for independent work, and concrete validation before integration.
- `PowerPoint.Application` COM is not registered on this machine, so PowerPoint-authoritative visual baselines cannot be generated locally.
- The repo-wide parity scope at `docs/parity/2026-06-27-avalonia-wpf-parity-scope.md` has already been refreshed for the large dedup wave.

## 2026-07-01 Integration Checkpoint

The first post-dedup FreeP parity implementation wave is integrated on `origin/main` through FreeP commit `bc9d88092`, including `bc1d49b57` shared table-cell planning, `3451f903f` Avalonia table-cell overlay wiring, `322ada635` presenter recording/ink intents, `267cc43b2` shared image export execution, `12f5e7571` review workflow host adapters, `2cf014219` text placeholder inset inheritance, `e69302b84` shared chart axis-title planning, `c74c06aca` shared bullet slot placement, `cc66c5e20` image export UI flow, `cfc8af03` persistent shape alt text, `bb5187fab` shared animation pane/timeline planning, `f63758c53` shared handout layout planning, `c538fbf70` shared layout-picker choices, `07e38b06e` shared chart series-style planning, `d5e7ff7cf` shared handout PDF render planning, `4af5fd126` shared accessibility issue action summaries, `ab5caca20` shared tab-stop text planning, `b789ad9b2` shared chart gridline stroke planning, `3411d4649` deeper shared layout-picker evidence, and `bc9d88092` alt-text title/decorative-object metadata planning. It closed the initial evidence and shared-planning gates that were blocking reliable WPF/Avalonia parity work:

- Added a generated FreeP WPF/Avalonia command inventory at `docs/parity/freep-command-parity-inventory.md` and `.json`.
- Added 21-deck PPTX corpus retention contract tests for package-preservation evidence, including a deterministic FreeP-authored comments/notes fixture.
- Fixed `tools/FreeP.RenderCompare --avalonia-compare` trust reporting so PowerPoint export failure is not reported as success, and added corpus summary output.
- Moved chart legend, axis-title, and series-style planning, text render route planning, and canvas drag reduction into shared FreeP presentation-layer planners consumed by WPF and Avalonia.
- Expanded the Avalonia ribbon profile from the initial small surface to 87 shared command ids. The generated command inventory currently reports 93 total commands, 87 shared, 0 actionable WPF/Avalonia missing commands, 0 explicit Avalonia gaps, 0 known deferred commands, 6 platform-only Avalonia shell commands, and 0 command-id aliases.
- Added shared presenter/slideshow state for current slide, next slide, notes text, start time, elapsed time, fullscreen intent, and monitor intent, with thin WPF and Avalonia slideshow-window adapters.
- Added a shared export planner for PDF/image/print descriptors, PDF dialog/picker plans, command IDs, and Backstage export plan; WPF and Avalonia now consume the same PDF export route with thin native file-picker/write adapters.
- Added shared export/print depth plans for image and video descriptors, slide-range normalization, full-page slide printing, notes pages, PowerPoint-style handout slide-count options, and concrete handout page layout geometry.
- Added shared selected-slide PNG image export execution. `PresentationImageExportExecutor` owns slide-range normalization through the shared planner, deterministic output naming, atomic writes, empty-deck no-op behavior, and non-empty renderer output validation; WPF and Avalonia provide only thin render callback and folder-picker hooks.
- Added WPF and Avalonia image export UI flows over the shared executor. WPF exposes the shared Images export action through Backstage with a native folder picker, while Avalonia exposes the same command through the File ribbon group with a platform folder picker.
- Added shared PowerPoint-style handout layout planning for 1, 2, 3, 4, 6, and 9 slides per page, including page count, slide-index mapping, slide slots, and 3-up writing-line areas. WPF and Avalonia expose or refresh the shared plan as thin adapter state; actual handout PDF/print rendering and native print preview remain deferred.
- Added shared handout PDF render planning. `PresentationHandoutPdfExporter` now builds portable PDF draw-op pages from the handout layout plan, scales existing slide draw ops into thumbnail slots, adds thumbnail borders, and emits 3-up writing lines. It is shared production groundwork, not PowerPoint visual-baseline parity.
- Improved shared placeholder text fidelity by resolving text-frame insets through shape, layout placeholder, and master placeholder inheritance before WPF and Avalonia render plans consume them.
- Moved bullet slot placement into `TextLayoutPlanner`: bullets now carry shared text/font/color and X/Y placement from the paragraph plan, while WPF and Avalonia renderers consume `placement.Bullet` instead of recalculating hanging-indent math.
- Moved tab-stop layout into `TextLayoutPlanner`: explicit/default stops plus left, right, center, and decimal alignment now produce shared run segment placements consumed by WPF and Avalonia. A mixed-run decimal-tab guard keeps the decimal prefix measured with each segment's actual run style.
- Added shared chart gridline stroke planning. Chart scene output now carries major/minor gridline stroke intent through the shared planner boundary so WPF/Avalonia renderer work can stay thin when the drawing adapters consume it.
- Added a shared transition command planner and exposed Transition commands through WPF and Avalonia thin adapters. WPF and Avalonia now route transition gallery, duration, advance-after, advance-on-click, and apply-to-all commands through the shared planner.
- Added a shared Design command planner and exposed theme plus slide-size commands through WPF and Avalonia thin adapters. Custom slide size is represented as a shared callback intent pending the full dialog.
- Added shared design/layout picker choices for slide layouts, theme variants, and slide size presets with WPF/Avalonia thin picker-state consumers.
- Deepened shared layout-picker evidence: layout choices now carry stable display order, master grouping labels, placeholder counts, current-selection state, and no-current-slide apply gating, with WPF and Avalonia adapter tests proving both hosts consume the same enriched picker plan.
- Added actual WPF and Avalonia layout-picker UI evidence: the Layout command now opens a visible in-app picker strip populated from the shared `PresentationLayoutPickerPlan`, disables the current layout choice, applies selected layouts through `TryApplyLayoutChoice`, refreshes the canvas/slide pane, and collapses after apply. This proves the picker is no longer only recorded adapter state; PowerPoint-style thumbnail gallery visuals and richer grouping remain deferred.
- Added a shared Animations command planner and exposed effect, timing, ordering, and animation-pane commands through WPF and Avalonia thin adapters. Timing commands remain conservative when no selected value or selected-shape animation exists.
- Added shared animation pane/timeline planning for selected-slide animation rows, selected animation normalization, trigger/duration/delay display, timeline start/end calculations, reorder availability, and preview intent. WPF now renders its pane rows from the shared plan, and Avalonia exposes the same plan as headless adapter state; full Avalonia visual pane UI, richer effect-options editing, pane-level preview/playback controls, authoring workflows, and deeper PPTX timing/effect persistence evidence remain deferred.
- Added a shared comments/review/accessibility/alt-text workflow planner that describes legacy comment pane actions, comment mutation intents, accessibility issue summaries, issue action summaries, alt-text request/update descriptors, and proofing scopes.
- Added persistent shape alt-text model/IO for PowerPoint `cNvPr descr`, plus an undoable shared command seam and thin WPF/Avalonia adapter apply routes.
- Added the first alt-text metadata expansion: `AlternativeTextTitle` and `IsDecorative` live in the shared model, clone/undo paths, PowerPoint reader/writer, review planner, and WPF/Avalonia adapters. PowerPoint `cNvPr title` and Microsoft `adec:decorative` metadata now round-trip, decorative objects are excluded from missing-alt-text issues, and writer coverage pins the `a:hlinkClick` before `a:extLst` child order when a decorative linked shape is saved. Full alt-text pane/dialog UX, richer suggestions, reading-order workflows, full proofing execution, and full accessibility checking remain deferred.
- Added a shared table-cell edit planner for selected-cell normalization, edit start placement, commit/cancel routing, undo-aware commit, and enabled-state. WPF consumes it, and Avalonia now has active-cell highlighting plus an in-canvas plain-text edit overlay with shared commit/undo/cancel routing and merge-anchor normalization. Avalonia still lacks WPF `RichTextBox`-level inline formatting inside the overlay.
- Added shared presenter recording/timing and pointer/ink intent/state planning, with WPF and Avalonia exposing thin adapter state for recording availability, elapsed-time reset, pointer tool selection, ink color, and ink width. Narration/audio capture, camera/media capture, full recording execution, renderer-heavy ink drawing, and PowerPoint baseline generation remain deferred.
- Added semantic-edit package-retention coverage for high-risk corpus decks with media, charts, SmartArt diagrams, picture crop, chart types, chart labels, comments, and speaker notes.

PowerPoint baseline anchors used for current scope: Microsoft documents PowerPoint File > Export paths including PDF, video, and handouts, Presenter View with current/next slide and notes, recording narration/timings, print layouts for slides/handouts/notes, and modern comments. Those remain parity targets for FreeP:

- `https://support.microsoft.com/en-us/powerpoint/export-a-presentation`
- `https://support.microsoft.com/en-us/powerpoint/training/save-powerpoint-presentations-as-pdf-files`
- `https://support.microsoft.com/en-us/powerpoint/turn-your-presentation-into-a-video`
- `https://support.microsoft.com/en-us/powerpoint/training/use-presenter-view-in-powerpoint`
- `https://support.microsoft.com/en-us/powerpoint/training/record-a-slide-show-with-narration-and-slide-timings`
- `https://support.microsoft.com/en-us/powerpoint/training/print-your-powerpoint-slides-handouts-or-notes`
- `https://support.microsoft.com/en-us/powerpoint/modern-comments-in-powerpoint`

## Current Implementation Reality

FreeP is now a real shared-first implementation, not a stub shell. Several gaps from the June 27 report are closed or materially changed.

| Area | Current repo evidence | Current status |
| --- | --- | --- |
| Solution map | `FreeP.slnx` includes Core.Model, Core.IO, App.Presentation, WPF host/rendering/tests, Avalonia host/rendering/tests, ribbon definitions, localization, and `tools/FreeP.RenderCompare`. | Dedicated FreeP solution exists. |
| Shared model/IO | `freep/FreeP.Core.Model/**`, `freep/FreeP.Core.IO/**`, `shared/Free.Shared.Opc`, `shared/Free.Shared.Drawing`, `shared/Free.Shared.Pdf`. | PPTX model, reader/writer, package snapshot, OPC helpers, charts, SmartArt, comments, notes, OLE/math, transitions, alt-text title/decorative metadata, and PDF plumbing exist. |
| Shared presentation layer | `freep/FreeP.App.Presentation/**`. | App-neutral planners/controllers now cover persistence, file dialogs, slide composition, text layout including bullets and tab-stop segment placement, chart primitives plus series/gridline-style plans, effects, picture color effects, gestures, hit testing, slide pane policy, table-cell edit planning, slideshow host/playback, presenter recording/ink intent planning, export/print descriptors, handout layout and PDF render planning, object insertion, dialogs, design/layout picker choices, SmartArt layout, and comments/review/accessibility workflow planning. |
| Shared ribbon definitions | `freep/FreeP.Ribbon.Definitions/**`, `freep/FreeP.Ribbon.Definitions.Tests/**`, `docs/parity/freep-command-parity-inventory.*`. | WPF and Avalonia ribbon profiles are single-sourced with generated capability deltas. The current inventory reports 93 total commands, 87 shared, 0 actionable WPF/Avalonia missing commands, 0 explicit Avalonia gaps, 0 known-deferred commands, 6 platform-only Avalonia shell commands, and 0 command-id aliases. |
| WPF app | `freep/FreeP.App.Host/**`, `freep/FreeP.App.Rendering.Wpf/**`, `freep/FreeP.App.Host.Tests/**`. | WPF is mostly a host/renderer adapter over shared policy, but still has richer dialog and rich-text editing coverage than Avalonia. Slide pane context actions, table-cell editing, presenter tool state, and Design commands now route through shared planners. |
| Avalonia app | `freep/FreeP.App.Avalonia/**`, `freep/FreeP.App.Rendering.Avalonia/**`, `freep/FreeP.App.Avalonia.Tests/**`, `freep/FreeP.App.Rendering.Avalonia.Tests/**`. | Cross-platform shell and renderer exist. Avalonia consumes shared file workflow, ribbon definitions, slide pane planning/actions, table-cell edit planning with active-cell highlight and plain-text in-canvas overlay, rendering planners, shared presenter state plus recording/ink adapter state, shared export planning, shared transition commands, shared Design commands, and shared Animations command planning. No explicit Avalonia command gaps or known-deferred command slices remain in the generated inventory. |
| PPTX lifecycle | `freep/FreeP.App.Presentation/PresentationFilePersistenceWorkflow.cs`, `freep/FreeP.App.Host/FileCommands.cs`, `shared/Free.Shared.Shell.Avalonia/SisterAvaloniaFileCommandWorkflow.cs`. | `.pptx` is now the default lifecycle path; `.fxp` remains legacy-compatible. The old WPF `.pptx` lifecycle gap is no longer a foundational blocker. |
| Package retention | `freep/FreeP.Core.Model/PptxPackageSnapshot.cs`, `PptxPackageReader.cs`, `PptxPackageWriter.cs`, `freep/FreeP.App.Host.Tests/PptxPackageRetentionTests.cs`. | Preservation machinery exists, the 21 tracked corpus decks have a retention contract test, and seven high-risk corpus decks now have semantic modeled-shape edit/save/reopen retention coverage. Comments and speaker notes now have explicit corpus package coverage; broader semantic edit breadth is still needed. |
| Render comparison | `tools/FreeP.RenderCompare/**`, `tools/FreeP.RenderCompare.Tests/**`, `tools/FreeP.RenderCompare/corpus/*.pptx`, `tools/FreeP.RenderCompare/PixelDiversity.cs`. | Harness supports WPF, Avalonia, PowerPoint, diff, pixel-diversity guards, explicit COM-unavailable reporting, and full-corpus summaries. Local PowerPoint COM is unavailable; the current local corpus summary is 21 decks total, 5 reference-ready, and 16 missing references. |

## Shared-First Boundary

Future parity work should start in shared or neutral code unless the difference is genuinely platform API glue.

| Layer | Owns | Must not own |
| --- | --- | --- |
| `FreeP.Core.Model` | Presentation semantics, shapes, text, charts, SmartArt, transitions, comments, notes, OLE/math, package snapshot data. | WPF/Avalonia controls, dialogs, input events, pixel drawing APIs. |
| `FreeP.Core.IO` | PPTX/FXP/PDF package semantics, PresentationML read/write, preserved parts/rels/content types, export serialization. | Host lifecycle prompts, renderer-specific drawing, native file dialogs. |
| `FreeP.App.Presentation` | Command planners, file persistence workflow, dialog policy, slide pane policy, slideshow/presenter models, render plans, gesture reducers, text/chart/effects planning. | Platform controls, WPF dependency properties, Avalonia controls, native windows. |
| `FreeP.Ribbon.Definitions` | Ribbon tabs/groups/controls, command ids, labels/keytips, WPF/Avalonia capability profiles. | Command implementation bodies and host widget construction. |
| WPF host/rendering | Owner windows, native dialogs, WPF focus/UIA, WPF `DrawingContext`/`RenderTargetBitmap`, media element, keyboard/mouse adapter, command binding to shared planners. | PowerPoint behavior decisions or duplicated render math. |
| Avalonia host/rendering | Cross-platform windows/dialogs, file pickers, Avalonia controls, Skia/Avalonia drawing, pointer/keyboard adapter, headless smoke plumbing. | PowerPoint behavior decisions or duplicated render math. |

## Remaining PowerPoint Parity Gaps

These are the current gaps after the large dedup effort. They are ordered by what should unblock the most reliable implementation work.

| Gap | Why it matters | Shared-first owner | Thin renderer/host owner | Evidence/gates |
| --- | --- | --- | --- | --- |
| FreeP WPF-vs-Avalonia command matrix | The generated matrix now exists and is the source of truth for command-surface work. Current state: 93 total, 87 shared, 0 actionable WPF/Avalonia missing commands, 0 explicit Avalonia gaps, 0 known deferred, 6 platform-only Avalonia shell commands, 0 command-id aliases. | Keep `tools/Generate-FreePCommandParityInventory.ps1` and generated docs current as command slices land. | WPF/Avalonia only classify adapter-only or platform-only commands. | `tools/Test-GeneratedDocs.ps1`; `FreePRibbonDefinitionProfileTests.cs`. |
| Ribbon depth and stubs | The explicit Avalonia command gaps and known-deferred command slices are closed in the generated matrix. Transition, Design, and Animations command IDs are exposed through WPF/Avalonia planner-backed routes, animation pane/timeline state is shared, and the layout-picker plan now exposes PowerPoint-shaped choice evidence including order, master grouping labels, placeholder counts, current selection, and no-current-slide apply gating. Review/View/Accessibility/Recording and deeper workflow UI remain incomplete outside the current command matrix. | Shared ribbon definitions, command planners, layout/timing/animation models in `FreeP.App.Presentation`. | Host adapters render galleries, combo boxes, panes, and dialogs. | Command inventory deltas; `RibbonEditorCompleteness5BTests.cs`; `RibbonTransitionsAnimationsTests.cs`; `PresentationDesignCommandPlannerTests.cs`; `AnimationPanePlannerTests.cs`; new stub-failure tests. |
| Render harness references | Pixel-diversity guards and trust reporting are in place. Only decks 01-05 have tracked PowerPoint PNG refs; decks 06-21 lack refs because local PowerPoint COM is unavailable. | `tools/FreeP.RenderCompare` reference freshness checks, corpus manifest, full-corpus summary. | WPF/Avalonia renderers only provide images. | Run refs on a PowerPoint COM machine; compare all 21 decks. |
| Package fidelity corpus contract | The 21 tracked decks now have a package-retention contract, and media/chart/SmartArt/comments/notes-focused decks have semantic modeled-shape edit coverage. Coverage is still thin for embedded workbooks, custom XML, and view/print settings. | `FreeP.Core.IO` package classification and writer merge rules; shared OPC helpers stay generic. | Hosts do not own package semantics. | Expand corpus open-save-reopen tests with targeted ZIP/OpenXML assertions after writer-owned edits. |
| Renderer orchestration duplication | Chart legend planning, chart axis-title layout/orientation, chart series fill/stroke/marker segment planning, text route planning, bullet slot placement, tab-stop segment placement, drag reduction, and placeholder text inset inheritance are now shared. Deeper chart/text/effects sequencing, axis tick/gridline styling, labels, and edit-state planning still need shared-core work. | Continue moving chart scene plans, text route decisions, paragraph/bullet/tab decisions, and gesture/edit reducers into `FreeP.App.Presentation`. | Renderers map plans to WPF/Avalonia primitives. | `RendererNeutralDedupPlannerTests.cs`; `ChartRenderPlannerTests.cs`; `TextLayoutPlannerTests.cs`; `CanvasGesturePlannerTests.cs`; render tests. |
| Text and WordArt fidelity | PowerPoint parity requires better coverage for text metrics, bullet typography and level styling, autofit, columns, vertical text, run effects, theme/default fonts, and WordArt. Placeholder text inset inheritance now resolves through shape, layout placeholder, and master placeholder before WPF/Avalonia render plans consume it; bullet slot placement and tab-stop segment placement now come from shared paragraph planning. | `TextLayoutPlanner`, model/IO text defaults, placeholder inheritance, WordArt/warp planning. | Platform font measurement/drawing callbacks. | `TextLayoutPlannerTests.cs`; `BulletsAutofitTests.cs`; `WordArtTests.cs`; `TextColumnsGradOutlineTests.cs`; PowerPoint-backed deck refs. |
| Charts fidelity | Chart read/write and primitive planning exist, and category/value axis-title layout/orientation, shared series fill/stroke/marker primitives, and first chart gridline stroke intents now route through shared planning. PowerPoint chart axes, labels, embedded workbook/caches, data tables, and type-specific visuals remain high risk. | `ChartShape`, `PptxChartReader/Writer`, `ChartRenderPlanner`, package retention. | Draw primitives only. | `ChartDataCommandTests.cs`; `ChartRenderPlannerTests.cs`; `RendererNeutralDedupPlannerTests.cs`; `ChartTests.cs`; `ChartDataLabelsTests.cs`; corpus decks 06, 18, 19. |
| SmartArt and modern object fidelity | SmartArt parsing/layout/fallback exists, but live SmartArt data/style/color parts and editing/rendering are not PowerPoint-complete. Modern/OLE/math objects still rely on fallback previews or placeholders. | Core model/IO package semantics, `SmartArtLayoutEngine`, modern object fallback policy. | Render previews/placeholders only. | `SmartArtTests.cs`; `SmartArtLayoutTests.cs`; `ModernObjectsRoundTripTests.cs`; `OleMathRoundTripTests.cs`; corpus deck 09/14. |
| Effects, media, connectors, and pictures | Effects/3D/crop/motion-path support is modeled, but visual parity needs PowerPoint-backed evidence and more shared planning where renderers still diverge. Avalonia slideshow sound is deferred/no-op. | Shared effects/media/connector/picture planners and PresentationML IO. | Platform media playback, image decoding, and drawing. | `PictureCropEffectsTests.cs`; `MotionPathTriggerTests.cs`; `Bevel3dTests.cs`; render corpus decks 07, 08, 10, 11, 12, 15, 16. |
| PDF/export/print/backstage | PowerPoint supports File > Export to PDF/video/handouts and print layouts for slides/handouts/notes. FreeP now has shared export descriptors, PDF dialog/picker plans, Backstage export plan, slide-range normalization, selected-slide PNG image export execution, WPF Backstage image export, Avalonia ribbon image export, deferred video plans, and print layout plans for full-page slides, notes pages, and handouts. Handout layout geometry is shared for 1/2/3/4/6/9 slides per page, including 3-up writing-line areas, with WPF/Avalonia as thin plan consumers. `PresentationHandoutPdfExporter` now turns that shared plan into portable PDF draw-op handout pages by scaling slide draw ops into slots, drawing borders, and emitting writing lines. WPF/Avalonia PDF export routes consume the shared PDF path, and WPF/Avalonia image export hooks are thin render-callback/folder-picker adapters over `PresentationImageExportExecutor`. Remaining gaps are full compositor/vector fidelity, native print execution/preview, video encoding, notes-page rendering, and PowerPoint-baseline handout visual parity. | Shared export/backstage/print planners; `PresentationImageExportExecutor`; `PresentationPdfExporter` and `PresentationHandoutPdfExporter` should continue moving toward compositor/draw-op-backed slide, notes, and handout export. | Native print dialogs, owner windows, platform file/folder pickers, renderer callbacks. | `PresentationPdfExporterTests.cs`; `PresentationExportPlannerTests.cs`; PDF/render inspection. |
| Slide pane and editing parity | Slide pane context actions, enabled-state, and drag-reorder action planning are now shared and consumed by WPF/Avalonia. Table-cell selected-state normalization, edit start, commit, undo, and cancel route through a shared planner consumed by WPF and Avalonia. Avalonia now has active-cell highlighting and an in-canvas plain-text overlay, including merge-anchor normalization; the remaining gap is richer inline formatting comparable to the WPF `RichTextBox` path. | `SlidePanePlanner`, `TableCellEditPlanner`, editing/session/table-cell planners, gesture reducer. | WPF/Avalonia list/control realization and input wiring; rich editor widgets stay platform-local. | `SlidePanePolicySourceGuardTests.cs`; `SlidePaneTests.cs`; `TableCellEditPlannerTests.cs`; `SlideCanvasAvaloniaTests.cs`. |
| Slideshow and presenter workflows | Shared presenter state now covers current slide, next slide, notes, elapsed time, fullscreen intent, monitor intent, recording/timing state, and pointer/ink tool state, with thin WPF/Avalonia adapters. PowerPoint-grade narration/audio capture, camera/media capture, full recording execution, renderer-heavy ink drawing, subtitles, custom shows, and PowerPoint baseline generation remain deferred. | Shared slideshow/presenter/timing/recording/ink models and planners. | Fullscreen windows, monitor APIs, media element, cursor/input capture, recording devices, ink rendering. | `SlideShowHostPlannerTests.cs`; `SlideShowPlaybackPlannerTests.cs`; `SlideShowPresenterToolPlannerTests.cs`; `SlideShowTests.cs`; WPF/Avalonia slideshow adapter tests. |
| Comments, accessibility, proofing, and alt text | PowerPoint includes modern comments, accessibility/alt text workflows, proofing, and review surfaces. FreeP now has a shared planner boundary for legacy comment pane actions, add/edit/delete intent payloads, accessibility issue summaries with action summaries/command hints, persistent alt-text request/update descriptors, and proofing scopes, plus thin WPF/Avalonia adapters that expose the shared comments-pane, accessibility-summary, alt-text request/apply, and proofing plans as host state/commands. Shape alt-text description, title, and decorative-object metadata now round-trip through PowerPoint `cNvPr descr`, `cNvPr title`, and `adec:decorative`, and decorative objects no longer raise missing-alt-text issues. It still lacks modern resolved-thread state, a full alt-text pane/dialog UX, richer generated suggestions, reading-order workflows, a full proofing engine, a full accessibility checker, and complete review UI parity. | `PresentationReviewWorkflowPlanner`, comments/review/accessibility planners, model/IO for comments and alt text. | Pane/dialog realization, focus/UIA, and host command/state adapters. | `PresentationReviewWorkflowPlannerTests.cs`; `ReviewWorkflowAdapterTests.cs`; `MainWindowHeadlessTests.cs`; `PptxRoundTripTests.cs`; `SectionsCommentsTests.cs`; new modern comments/accessibility/alt-text UI tests. |

## Implementation Plan

Use small, isolated linked worktrees. Each worker should start from current `origin/main`, preserve unrelated work, and report exact validation. Do not put feature semantics directly in WPF or Avalonia host files when a shared planner/model can own them.

### Phase 0 - Evidence Gate First

Goal: make parity measurable before widening feature work. The first three evidence items are now complete on `main`; PowerPoint reference generation remains blocked on this machine by missing PowerPoint COM registration.

1. Done: fix `tools/FreeP.RenderCompare --avalonia-compare` so PowerPoint export failure returns failure.
2. Done: add machine prerequisite output that clearly separates "PowerPoint COM unavailable" from "FreeP parity failed".
3. Done: add a corpus manifest/full-corpus summary for the 21 tracked decks.
4. Generate missing PowerPoint reference PNGs for decks 06-21 on a machine with PowerPoint COM installed.
5. Done: add a generated FreeP WPF-vs-Avalonia command inventory modeled after the FreeX parity inventory.

### Phase 1 - Shared Planning Cleanup

Goal: remove the remaining behavior duplication before adding large parity surfaces. The first shared chart/text/gesture slices are complete; the remaining work is deeper fidelity and workflow planning, not the initial extraction.

1. Done: move chart legend scene orchestration into `ChartRenderPlanner`, leaving renderers as primitive painters for that route.
2. Done: move first text render route decisions into `TextLayoutPlanner`.
3. Done: move canvas drag state reduction into `CanvasGesturePlanner`, leaving event capture and cursor APIs platform-local.
4. Done for first chart axis-title slice: shared `ChartRenderPlanner` now reserves title bands and returns category/value axis-title orientation/bounds consumed by WPF and Avalonia renderers.
5. Done for first chart series-style slice: shared `ChartRenderPlanner` now returns series fill/stroke, line-segment, and marker primitives consumed by WPF and Avalonia renderers.
6. Done for first bullet placement slice: shared `TextLayoutPlanner` now returns bullet text/font/color and X/Y placement for body and column layouts, consumed by WPF and Avalonia renderers.
7. Done for first tab-stop slice: shared `TextLayoutPlanner` now returns explicit/default left/right/center/decimal tab segment placement consumed by WPF and Avalonia renderers.
8. Continue expanding source guards so WPF/Avalonia cannot reintroduce duplicated chart/text/gesture math.

### Phase 2 - Command And Workflow Breadth

Goal: close visible WPF/Avalonia command gaps while keeping semantics shared.

1. Done: add the generated FreeP command matrix and classify gaps as shared, explicit Avalonia gaps, known deferred, platform-only, or command-id aliases.
2. Done for explicit gaps and known-deferred command slices: expand Avalonia ribbon profile so Home clipboard/font/arrange/editing/slides, Insert shapes/tables/charts/links, Transitions, Design, and Animations are shared.
3. Continue replacing WPF stubs for layout and timing controls with shared planners before exposing the same commands in Avalonia. Shared layout-picker choices now include slide-layout ids, display names, layout types, current selection, stable order, master labels, and placeholder counts, and WPF/Avalonia now expose a visible picker strip over that plan; the next gap is PowerPoint-style thumbnail gallery grouping and richer selection chrome rather than the shared evidence model.
4. Continue from shared backstage/export descriptors into full platform panes and command flows only after the shared planner/executor contract is covered.

### Phase 3 - Visual Fidelity Waves

Goal: reduce measured PowerPoint diff one deck family at a time.

1. Text wave: decks 03, 13, 16, 17, 20.
2. Effects/pictures/custom geometry wave: decks 07, 08, 10, 11, 12, 15.
3. Chart wave: decks 06, 18, 19.
4. SmartArt/modern object wave: decks 09, 14 plus OLE/math fixtures.
5. Each wave must include shared planner/model/IO work first, WPF and Avalonia thin renderer updates second, and render-compare evidence last.

### Phase 4 - Presentation Workflows

Goal: close PowerPoint workflows beyond static slide editing.

1. Done for first presenter state: current/next slide, notes, timer, fullscreen intent, and monitor intent are shared; custom shows remain open.
2. Done for intent/state planning: recording/timing state, elapsed-time reset, pointer/ink tools, ink color, and ink width are shared with thin WPF/Avalonia adapters. Narration/audio capture, camera/media capture, full recording execution, renderer-heavy ink drawing, and PowerPoint baseline generation remain deferred.
3. Done for planning boundary: add shared comments/review/accessibility/alt-text workflow descriptors and focused tests.
4. Done for first host adapter seam: expose shared comments-pane, accessibility-summary, alt-text request/apply, and proofing plans through thin WPF/Avalonia command/state adapters. Persistent shape alt text now round-trips through PowerPoint `cNvPr descr`. Alt-text title and decorative-object metadata now round-trip through `cNvPr title` and `adec:decorative`, and decorative shapes are exempt from missing-alt-text issues. Modern comments, full panes/dialogs, richer suggestions, reading-order workflows, and proofing/accessibility execution remain open.
5. Done for first accessibility action slice: missing slide title, missing alt text, and missing hyperlink ScreenTip issues now carry shared action summaries and command hints consumed by WPF/Avalonia adapter tests.

### Phase 5 - Export, Print, And Backstage

Goal: graduate from text-only export to PowerPoint-shaped output workflows.

1. In progress: promote `PresentationPdfExporter` toward compositor/draw-op-backed slide export. Basic background and shape rectangle draw ops are now included.
2. Done for descriptors: add export descriptors for PDF, image, video, and print. Image sequence export is now implemented through shared selected-slide PNG execution; video remains deferred explicitly.
3. Done for planning: add print layout planning for slides, notes pages, handouts, common slides-per-page options, slide-range normalization, page counts, slide-index mapping, slide slots, and 3-up writing-line areas.
4. Done for shared image execution and first UI flow: selected-slide PNG export normalizes ranges, emits deterministic names, writes atomically, and handles empty decks without invoking host renderers; WPF Backstage and Avalonia ribbon commands now route through native/platform folder pickers into the shared executor.
5. Done for first handout PDF render slice: shared `PresentationHandoutPdfExporter` builds portable PDF draw-op handout pages from the shared layout plan, including thumbnail borders and 3-up writing lines.
6. Keep native print dialogs, video encoding, and print execution platform-specific.

## Worker Backlog

| Priority | Worker lane | Owned write scope | First success criteria | Suggested gates |
| --- | --- | --- | --- | --- |
| 1 | PowerPoint reference generation | `tools/FreeP.RenderCompare/**`, checked-in refs or documented external artifact path | Deck refs 06-21 are generated on a PowerPoint COM machine and full-corpus compare can distinguish visual diff from missing baseline. | `tools/FreeP.RenderCompare --corpus-summary`; full render compare on COM-capable machine. |
| 2 | Deep animation workflow fidelity | `FreeP.App.Presentation`, WPF/Avalonia host adapters/tests | Shared animation commands and timeline plans grow into full visual pane UI for Avalonia, richer effect-options editing, real pane-level preview/playback controls, recording/timeline authoring, and deeper persistence evidence for advanced timing/effect semantics. | `RibbonTransitionsAnimationsTests`; `PresentationAnimationCommandPlannerTests`; `AnimationPanePlannerTests`; render/playback fixtures. |
| 3 | Package semantic edit corpus expansion | `freep/FreeP.Core.IO/**`, `freep/FreeP.App.Host.Tests/**`, corpus assertions | Additional corpus decks, especially custom XML/view-print settings, preserve parts/rels/content types after writer-owned edits. | Focused `PptxPackageRetentionTests`, `PptxRoundTripTests`, ZIP/OpenXML assertions. |
| 4 | Deep text and placeholder fidelity | `freep/FreeP.App.Presentation/**`, model/IO text defaults, WPF/Avalonia text render adapters/tests | Placeholder inset inheritance, bullet slot placement, and tab-stop segment placement are shared and consumed by WPF/Avalonia. Next slices should cover bullet typography metrics and level styling, autofit, richer column flow, vertical text, run effects, theme/default fonts, and WordArt. | `TextLayoutPlannerTests`; `BulletsAutofitTests`; `WordArtTests`; render-compare text decks. |
| 5 | Deep chart scene fidelity | `freep/FreeP.App.Presentation/**`, `freep/FreeP.Core.IO/**`, WPF/Avalonia rendering adapters/tests | Axis-title planning, first shared series fill/stroke/marker primitives, and chart gridline stroke planning are in shared code. Next slices should wire gridline strokes through both renderers, then cover axis ticks, secondary-axis titles, data tables, richer authored series style import/export, embedded workbook/cache behavior, and type-specific chart decisions. | `ChartRenderPlannerTests`; `RendererNeutralDedupPlannerTests`; `ChartDataCommandTests`; `ChartTests`; selected render-compare decks. |
| 6 | Table editing parity | `FreeP.App.Presentation`, `FreeP.App.Avalonia`, renderer adapters/tests | Shared table-cell state/start/commit/cancel planning is in place for WPF and Avalonia; Avalonia has active-cell highlight plus a plain-text in-canvas overlay. Next slices should add richer inline formatting and editor fidelity comparable to WPF `RichTextBox`. | `TableCellEditPlannerTests`; `SlideCanvasAvaloniaTests`; WPF/Avalonia table-cell adapter tests. |
| 7 | Presenter recording and ink execution | `FreeP.App.Presentation`, WPF/Avalonia slideshow windows/tests | Recording/timing and pointer/ink intent/state planning is complete enough for thin WPF/Avalonia adapter state. Next slices should implement narration/audio capture, camera/media capture, full recording execution, renderer-heavy ink drawing, subtitles, custom shows, and PowerPoint baseline generation. | `SlideShowHostPlannerTests`; `SlideShowPlaybackPlannerTests`; `SlideShowPresenterToolPlannerTests`; WPF/Avalonia slideshow tests; COM-capable PowerPoint baseline runs. |
| 8 | Export/print/backstage depth | `FreeP.App.Presentation`, `FreeP.Core.IO`, WPF/Avalonia host adapters/tests | Existing shared export descriptors, selected-slide PNG execution, WPF/Avalonia folder-picker image export flows, shared handout layout plans, and first shared handout PDF draw-op rendering grow into native print execution/preview, notes-page rendering, video export, and stronger compositor-backed PDF fidelity. | `PresentationPdfExporterTests`; `PresentationExportPlannerTests`; PDF/render inspection. |
| 9 | Comments/accessibility UI realization | `FreeP.App.Presentation`, WPF/Avalonia host adapters/tests | First thin WPF/Avalonia host adapters expose shared comments-pane, accessibility-summary/action summaries, persistent alt-text request/apply, proofing plans, title metadata, and decorative-object semantics. Next slices should turn those plans into real panes/dialogs and add richer generated alt-text suggestions, reading-order workflows, modern resolved comments, proofing execution, and full accessibility checks without duplicating policy. | `PresentationReviewWorkflowPlannerTests`; `ReviewWorkflowAdapterTests`; `MainWindowHeadlessTests`; `PptxRoundTripTests`; focused WPF/Avalonia adapter tests. |

## Validation Strategy

For report-only updates, run docs/preflight gates. For implementation workers, prefer focused tests plus a serialized build because this repo has had shared-output and WPF concurrency flakes.

Recommended gates by lane:

- Docs/report lane: `git diff --check`; `powershell -ExecutionPolicy Bypass -File tools\Test-GeneratedDocs.ps1`; `powershell -ExecutionPolicy Bypass -File tools\Test-RepositoryPreflight.ps1`.
- Shared planner/code lane: focused project tests plus `dotnet build FreeP.slnx --configuration Release -m:1 /nr:false`.
- Host/render lane: focused WPF/Avalonia tests plus render harness smoke.
- PowerPoint visual lane: run `tools/FreeP.RenderCompare` on a PowerPoint COM machine; do not claim authoritative parity from this machine.

Current integration stance: FreeP has a substantial shared codebase, generated command inventory with zero actionable WPF/Avalonia missing commands, zero explicit Avalonia command gaps, zero known-deferred command slices, and zero command-id aliases, 21-deck package-retention contracts plus first semantic-edit corpus coverage for media, charts, SmartArt, comments, and notes, trustworthy render-harness failure reporting, initial shared chart/text/gesture planners, shared chart axis-title layout/orientation, series fill/stroke/marker planning, and chart gridline stroke planning, shared placeholder text inset inheritance, shared bullet slot placement and tab-stop segment placement consumed by WPF and Avalonia, shared slide-pane action planning, shared table-cell edit state/start/commit/cancel planning consumed by WPF and Avalonia with an Avalonia plain-text in-canvas overlay, shared presenter/slideshow state plus recording/timing and pointer/ink intent planning consumed by WPF and Avalonia, shared export/backstage planning with WPF/Avalonia PDF export routes, shared selected-slide PNG image export execution with thin WPF/Avalonia render/folder-picker UI flows, shared export/print depth descriptors for video/slide/notes/handout workflows, shared PowerPoint-style handout page layout planning consumed by WPF and Avalonia, shared handout PDF draw-op rendering, shared transition, Design, layout picker, and Animations command intents exposed through WPF/Avalonia, a visible shared-plan-backed WPF/Avalonia layout picker strip for selecting slide layouts, shared animation pane/timeline state with WPF pane consumption and Avalonia adapter state, a shared comments/review/accessibility workflow planning boundary with accessibility issue action summaries, persistent shape alt-text model/IO through PowerPoint `cNvPr descr`, `cNvPr title`, and `adec:decorative`, and thin WPF/Avalonia review workflow adapters over those shared plans. It is not PowerPoint-parity complete. The next work should generate missing PowerPoint refs on a COM-capable machine, then continue deeper shared planner work for text typography/autofit/WordArt, chart tick/gridline renderer consumption/data-table fidelity, authored chart style import/export, native print execution/preview, notes-page rendering, video export, richer compositor/PDF fidelity, rich inline table-cell editing parity, PowerPoint-style layout gallery thumbnails/grouping, full animation pane UI/playback/effect options, presenter recording/ink execution, modern comments/review UI, real alt-text pane UX, richer generated suggestions, reading-order workflows, proofing execution, and full accessibility surfaces.
